# Registre des migrations

Suivi **vivant** de l'application des migrations SQL sur le projet Supabase **cloud**
(`nsxzflxsgovriwwvflxe`). Une ligne par fichier de [`migrations/`](migrations/).

**Règle** (voir [CLAUDE.md](../CLAUDE.md#migrations-base-de-données)) : toute migration créée est
**poussée via `npm run db:push`** (jamais collée à la main dans la console — sinon l'historique
CLI `supabase_migrations.schema_migrations` diverge du schéma réel). Dès qu'une migration est
poussée avec succès, **cocher sa case** et renseigner la date.

- `[x]` = appliquée sur le cloud **et** connue de l'historique CLI (`db push` ou `migration repair`).
- `[ ]` = pas encore poussée.
- ⚠ = jouée à la main dans la console → schéma OK mais historique CLI à réconcilier
  (`npm run db:push:dry` la liste encore comme « à pousser » → `supabase migration repair --status applied <version>`).

| Poussée | Migration                                         | Date push  | Méthode                       |
| :-----: | ------------------------------------------------- | ---------- | ----------------------------- |
|   [x]   | `20260910214329_cardio_ux01_semaines_et_adaptation` | 10/09/2026 | CLI (`npm run db:push`, Florian) — US CARDIO-UX01 (R9 + R3-3) : **3 colonnes additives et nullables**, `sessions.week_index` et `planned_sessions.adapted_reps_pct` / `adapted_pace_delta_s`. `week_index` lève le constat **F35** : `sessions` ne portait aucune notion de semaine et `planProgram` répétait le même patron hebdo — un programme était **une semaine type répétée**, jamais un plan progressif, donc un « 10 km en 8 semaines » ne pouvait pas faire grandir sa sortie longue. ⚠️ **AUCUN défaut posé, délibérément** : `null` signifie « séance de la semaine type, répétée chaque semaine », c'est-à-dire le comportement actuel — **tous les programmes existants restent valides sans reprise**. Un `default 0` aurait fait de chaque séance existante une séance de la semaine 1 seulement, vidant les semaines 2 à 8 de tous les programmes déjà planifiés. Les deux colonnes `adapted_*` lèvent le constat **F36** (la carte d'adaptation conseillait sans pouvoir agir) et vivent sur l'**occurrence**, jamais sur le template : « j'allège aujourd'hui » ≠ « je change mon plan » (règle R3-3). ✅ **Aucune sync rule à redéployer** — `sessions` et `planned_sessions` sont déjà publiées et lues en `select *` : le réflexe « migration ⇒ sync rule à la main » ne vaut que pour une table **neuve**. 🔴 **Les 3 colonnes sont déclarées dans `powersync/schema.ts`** — sans ça l'écriture échoue en silence (panne CYCLE-01 du 31/07, répétée par HORAIRE-01). ✅ **Poussée, types régénérés, drapeau basculé** le 10/09/2026 : `ADAPTATION_WRITE_READY` est passé à `true` et le bouton « Appliquer aujourd'hui » est rendu. Les 3 colonnes sont **confirmées présentes dans `database.types.ts`** (diff de 9 lignes : 3 colonnes × Row/Insert/Update) — c'est la vérification qui compte, le push ayant émis le warning `pg-delta` habituel. Le drapeau est **conservé** plutôt que supprimé : il nomme la dépendance entre ce code et une colonne distante, et il donne un point de retour immédiat si la migration devait être annulée. Un test le vérifie (`SessionAdaptationCard.test.tsx`), avec 10 autres sur le chemin d'écriture — la carte a cessé d'être un affichage le jour où elle a commencé à écrire. ⚠️ **REDATÉE le 10/09/2026, de `20260910154350` à `20260910214329`.** Premier `db:push` **refusé** : « *Found local migration files to be inserted before the last migration on remote database* ». Cause : elle a été **écrite** à 15:43 sur une branche parallèle, pendant que NUTRI-UX01 créait et poussait 4 migrations dont la dernière porte 16:22 — la mienne se retrouvait donc à insérer **dans le passé** de l'historique distant. **Choix retenu : la redater plutôt que `--include-all`.** Les deux marchaient (elle est purement additive, sur des tables que la nutrition ne touche pas, donc l'ordre est sans effet fonctionnel), mais `--include-all` laisse un `schema_migrations` **non monotone** — exactement le genre de divergence que ce fichier existe pour éviter, et que le dépôt a déjà dû réconcilier une fois (note du 14/07). Redater était sans risque **parce qu'elle n'avait été appliquée nulle part** : c'est la condition, et elle ne sera plus vraie après le push. ⚠️ **Même warning CLI `pg-delta` / Docker qu'aux push de REPAS-01, VIE-01, DOUL-01 et HORAIRE-01** (`failed to cache migrations catalog`, image `edge-runtime` injoignable) : il porte sur la **mise en cache du catalogue**, pas sur l'application du SQL — `Finished supabase db push` est bien sorti, et `npm run db:types` le confirme colonne par colonne. 📌 **La leçon, pour la prochaine fois** : deux US en parallèle qui créent chacune des migrations produisent des horodatages entrelacés. Créer le fichier **au moment de pousser**, pas au moment de coder, évite le problème. |
|   [x]   | `20260705150000_init_conventions`                 | —          | CLI (historique OK)           |
|   [x]   | `20260706120000_socle_muscu_tables`               | —          | CLI (historique OK)           |
|   [x]   | `20260706120001_socle_muscu_rls`                  | —          | CLI (historique OK)           |
|   [x]   | `20260706130000_programmes_tables`                | —          | CLI (historique OK)           |
|   [x]   | `20260706130001_programmes_rls`                   | —          | CLI (historique OK)           |
|   [x]   | `20260706140000_nutrition_tables`                 | —          | CLI (historique OK)           |
|   [x]   | `20260706140001_nutrition_rls`                    | —          | CLI (historique OK)           |
|   [x]   | `20260706140002_personal_records`                 | —          | CLI (historique OK)           |
|   [x]   | `20260706150000_food_tables`                      | —          | CLI (historique OK)           |
|   [x]   | `20260706150001_food_rls`                         | —          | CLI (historique OK)           |
|   [x]   | `20260707120000_running_runs`                     | —          | CLI (historique OK)           |
|   [x]   | `20260707130000_recipes_bodyweight_tables`        | —          | CLI (historique OK)           |
|   [x]   | `20260707130001_recipes_bodyweight_rls`           | —          | CLI (historique OK)           |
|   [x]   | `20260707140000_nutrition_meals`                  | —          | CLI (historique OK)           |
|   [x]   | `20260711140000_food_micronutrients`              | —          | CLI (historique OK)           |
|   [x]   | `20260712090000_running_profiles`                 | 14/07/2026 | repair (jouée console)        |
|   [x]   | `20260712100000_running_session_content`          | 14/07/2026 | repair (jouée console)        |
|   [x]   | `20260712110000_planned_sessions`                 | 14/07/2026 | repair (jouée console)        |
|   [x]   | `20260712120000_running_pace_records`             | 14/07/2026 | repair (jouée console)        |
|   [x]   | `20260712130000_drop_sessions_running_target_chk` | 14/07/2026 | repair (jouée console)        |
|   [x]   | `20260713100000_admin_user_roles`                 | 14/07/2026 | repair (jouée console)        |
|   [x]   | `20260713110000_admin_editorial_exercises`        | 14/07/2026 | repair (jouée console)        |
|   [x]   | `20260713140000_admin_editorial_programs_rls`     | 14/07/2026 | repair (jouée console)        |
|   [x]   | `20260713150000_foods_import_key`                 | 14/07/2026 | repair (jouée console)        |
|   [x]   | `20260713160000_admin_editorial_foods_rls`        | 14/07/2026 | repair (jouée console)        |
|   [x]   | `20260714120000_seed_library_foods_ciqual`        | 14/07/2026 | CLI (`npm run db:push`)       |
|   [x]   | `20260714170000_admin_audit_log`                  | 14/07/2026 | CLI (`npm run db:push`)       |
|   [x]   | `20260715152227_nutrition_training_bonus_mode`    | 15/07/2026 | CLI (`npm run db:push`)       |
|   [x]   | `20260716134626_admin_users_view`                 | 16/07/2026 | CLI (`npm run db:push`)       |
|   [x]   | `20260716150753_user_bans`                        | 16/07/2026 | CLI (`npm run db:push`)       |
|   [x]   | `20260716173555_nutrition_adherence_margin`       | 16/07/2026 | CLI (`npm run db:push`)       |
|   [x]   | `20260716194509_profiles_weight_goal`             | 16/07/2026 | CLI (`npm run db:push`)       |
|   [x]   | `20260718125516_workouts_planned_session_link`    | 18/07/2026 | CLI (`npm run db:push`)       |
|   [x]   | `20260719230416_refonte_muscu_c2_saisie_enrichie` | 20/07/2026 | CLI (`npm run db:push`) — C2  |
|   [x]   | `20260720121317_refonte_muscu_c3_note_exercice`   | 20/07/2026 | CLI (`npm run db:push`) — C3  |
|   [x]   | `20260720200254_refonte_muscu_c3_superset_pairs`  | 20/07/2026 | CLI (`npm run db:push`) — C3 (recette) |
|   [x]   | `20260721074949_refonte_muscu_d_workout_templates`| 21/07/2026 | CLI (`npm run db:push`) — US-D |
|   [x]   | `20260722080703_muscf3_equipment_check`           | 22/07/2026 | CLI (`npm run db:push`) — MUSC-F3 |
|   [x]   | `20260722140518_muscf10c1_exercises_muscles_secondary` | 22/07/2026 | CLI (`npm run db:push`) — MUSC-F10c-1 |
|   [x]   | `20260722151024_muscf10c2_exercise_variants`      | 22/07/2026 | CLI (`npm run db:push`) — MUSC-F10c-2 (⚠ sync rules à redéployer) |
|   [x]   | `20260723100835_muscf13_workout_display_level`    | 23/07/2026 | CLI (`npm run db:push`) — MUSC-F13 (profiles `select *` → pas de redéploiement sync rules) |
|   [x]   | `20260723131921_conf02_account_deletion`          | 23/07/2026 | CLI (`npm run db:push`) — CONF-02 (table + RPC + `purge_expired_accounts` + **pg_cron** job `purge-deleted-accounts` + fix FK `user_bans.acted_by`→set null ; hors PowerSync) |
|   [x]   | `20260724112210_analytics_events`                 | 24/07/2026 | CLI (`npm run db:push`) — US 9.10 (table `analytics_events` append-only + RLS insert/select own + colonne `user_settings.analytics_enabled` opt-out ; FK cascade). ⚠️ **sync rule PowerSync `analytics_events` (bucket par `user_id`) à ajouter sur l'instance** avant recette |
|   [x]   | `20260724123616_analytics_events_publication`     | 24/07/2026 | CLI (`npm run db:push`) — US 9.10 **correctif** : `alter publication powersync add table public.analytics_events` (oublié dans `20260724112210` ; sans lui le déploiement des sync rules échoue « table not part of publication »). Pattern standard des tables synchronisées |
|   [x]   | `20260726202133_health_connect_enabled`           | 27/07/2026 | CLI (`npm run db:push`) — US CONF-06 (`user_settings.health_connect_enabled boolean not null default false`, **opt-in** : donnée de santé). Types régénérés (`db:types`). `user_settings` étant en `select *` dans les sync rules, **aucun redéploiement PowerSync attendu** — à confirmer en recette (critère 8) |

|   [x]   | `20260728132424_pas01_daily_steps`                | 28/07/2026 | CLI (`npm run db:push`) — US PAS-01 (table `daily_steps` + index unique partiel `(user_id, log_date)` + RLS own + colonne `profiles.daily_step_goal`). Types régénérés (`db:types`). ⚠️ **sync rule PowerSync `daily_steps` à déployer sur l'instance** avant recette |
|   [x]   | `20260728132601_pas01_daily_steps_publication`     | 28/07/2026 | CLI (`npm run db:push`) — US PAS-01 **complément** : `alter publication powersync add table public.daily_steps` (gardé par `pg_publication_tables`). Même pattern que `20260724123616` pour `analytics_events` : sans lui, le déploiement des sync rules échoue « table not part of publication » |
|   [x]   | `20260728185757_bien01_daily_wellbeing`            | 28/07/2026 | CLI (`npm run db:push`) — US BIEN-01 (table `daily_wellbeing` : `mood` / `energy` / `stress` en 1-5 **nullables** — saisie partielle acceptée, décision D3 — + index unique partiel `(user_id, log_date)` + index de lecture + RLS own sans `delete`). **Aucune colonne poids** : il reste dans `body_weight_entries`. Types régénérés (`db:types`). ⚠️ **sync rule PowerSync `daily_wellbeing` à déployer sur l'instance** avant recette |
|   [x]   | `20260728185759_bien01_daily_wellbeing_publication` | 28/07/2026 | CLI (`npm run db:push`) — US BIEN-01 **complément** : `alter publication powersync add table public.daily_wellbeing` (gardé par `pg_publication_tables`). Même pattern que `20260728132601` |
|   [x]   | `20260729075443_contenu01_seed_programmes_muscu`    | 29/07/2026 | CLI (`npm run db:push`) — US CONTENU-01 : **migration de données** (aucun changement de schéma). (A) dépublie les 2 programmes de **test** qui étaient `published` donc visibles dans l'app (`status → 'draft'`, réversible d'une ligne) ; (B+C) ajoute 2 programmes muscu bilingues — **Push / Pull / Legs** et **Half Body haut/bas** — avec séances et `exercise_plans` référençant les 16 exercices de bibliothèque. UUID déterministes + `on conflict do nothing` → rejouable. **Aucune sync rule à redéployer** : `programs`/`sessions`/`exercise_plans` sont déjà dans `shared_content` |
|   [x]   | `20260729080925_admin01_editorial_usage_counts`    | 29/07/2026 | CLI (`npm run db:push`) — US ADMIN-01 : fonction `editorial_usage_counts(kind, id)` `security definer` (`search_path` figé, garde `is_admin()`, `grant execute` à `authenticated` seulement). **Nécessaire** parce que la RLS interdit à un admin de compter les données des autres utilisateurs (`workout_sets_select` = `user_id = auth.uid()`). Aucune table modifiée. ⚠️ **sync rule `shared_content` à redéployer** : `exercises` et `exercise_translations` perdent leur filtre `deleted_at` |
|   [x]   | `20260729091950_mesur01_body_measurements`         | 29/07/2026 | CLI (`npm run db:push`) — US MESUR-01 : table `body_measurements` **normalisée** (une ligne par `(user_id, log_date, kind)`, décision D1 — la liste des mesures a vocation à bouger, une table large coûterait une migration par ajout) + index unique partiel + index de lecture `(user_id, kind, log_date desc)` + RLS own sans `delete`. Stockage **toujours en cm**. Types régénérés. ⚠️ **sync rule PowerSync `body_measurements` à déployer** (bucket `user_data`) |
|   [x]   | `20260729091953_mesur01_body_measurements_publication` | 29/07/2026 | CLI (`npm run db:push`) — US MESUR-01 **complément** : `alter publication powersync add table` (gardé par `pg_publication_tables`). Même pattern que `20260728185759` |
|   [x]   | `20260729095446_streak01_jokers`                   | 29/07/2026 | CLI (`npm run db:push`) — US STREAK-01 : table `streak_jokers` (une ligne par jour manqué couvert) + index unique partiel `(user_id, log_date)` + RLS own sans `delete`. **Table dédiée plutôt qu'un champ JSON** sur `user_settings` (décision D6) : décompte mensuel trivial et usage auditable. Types régénérés. ⚠️ **sync rule à déployer** (bucket `user_data`) |
|   [x]   | `20260729095705_streak01_jokers_publication`        | 29/07/2026 | CLI (`npm run db:push`) — US STREAK-01 **complément** : `alter publication powersync add table` |
|   [x]   | `20260729131013_obj01_personal_goals`              | 29/07/2026 | CLI (`npm run db:push`) — US OBJ-01 : table `personal_goals` + `check` sur `kind` (plutôt qu'un enum : un type d'objectif de plus ne demandera pas de migration de données) + `check (deadline >= start_date)` + index partiel `(user_id, deadline desc)` + RLS own sans `delete`. **Ni colonne `status` ni `progress`** (décision D5) : les deux sont dérivés de la fenêtre par `computeGoalProgress`, ce qui évite un écrivain à déclencher — une app mobile hors ligne n'offre aucun moment fiable pour clôturer les objectifs échus. `exercise_id` en `on delete set null` (pas cascade) : un exercice supprimé rend l'objectif non calculable, il ne l'efface pas. Types régénérés. ⚠️ **sync rule à déployer** (bucket `user_data`) |
|   [x]   | `20260729131107_obj01_personal_goals_publication`   | 29/07/2026 | CLI (`npm run db:push`) — US OBJ-01 **complément** : `alter publication powersync add table` (gardé par `pg_publication_tables`) |
|   [x]   | `20260729153757_ux05_intensity_scale`              | 29/07/2026 | CLI (`npm run db:push`) — US UX-05 : `user_settings.intensity_scale text not null default 'rpe'` + `check in ('rpe','rir')`. **Le RIR n'est jamais stocké** : `workout_sets.rpe` reste la seule vérité, `RIR = 10 − RPE` est calculé **à l'affichage** — même patron que `user_settings.units` (« stockage en SI, conversion à l'affichage »). Basculer d'échelle ne convertit donc rien en base et ne perd aucune donnée. Types régénérés. ✅ **Aucune sync rule à redéployer** : `user_settings` est déjà publiée et lue en `select *` — même situation que `health_connect_enabled` (20260726202133). **Une seule migration**, pas de complément de publication |

|   [x]   | `20260730230615_cycle01_menstrual_tracking`         | 31/07/2026 | CLI (`npm run db:push`) — US CYCLE-01 : **2 tables** `menstrual_periods` (début / fin / `source`) et `menstrual_daily_logs` (flux / symptômes `jsonb`), **volontairement indépendantes** — un jour de flux peut exister sans période déclarée, les lier imposerait un ordre de saisie que personne ne respecte. Découpage **calqué sur Health Connect** (`MenstruationPeriod` / `MenstruationFlow`) pour que l'étape 7 soit une correspondance directe. **Index unique partiel `(user_id) where ended_on is null`** = une seule période en cours (R2) : sans lui, un oubli de « fin » fausse silencieusement toutes les longueurs de cycle. Second index unique sur `(user_id, started_on)` → déduplication Health Connect déterministe. **La borne des 15 j (R3) n'est PAS un `check`** : c'est une clôture automatique applicative, un `check` rejetterait les imports plus longs qu'on veut au contraire accueillir puis signaler. + `user_settings.cycle_tracking_enabled` et `cycle_health_connect_enabled`, **tous deux `default false`** (aucun compte existant ne voit la fonctionnalité apparaître). RLS own sans `delete`. Types régénérés. ⚠️ **2 sync rules à déployer** (bucket `user_data`) |
|   [x]   | `20260730230617_cycle01_menstrual_tracking_publication` | 31/07/2026 | CLI (`npm run db:push`) — US CYCLE-01 **complément** : `alter publication powersync add table` pour les 2 tables, gardé par `pg_publication_tables`. Même patron que `20260728185759` |
|   [x]   | `20260801001204_nutrf2_portions_reference_aliments`  | 01/08/2026 | CLI (`npm run db:push`) — US NUTR-F2 **contenu** : renseigne `foods.portions` pour les **50** aliments de bibliothèque qui n'en avaient aucune (sur 80). Trouvé en recette device du 01/08/2026 : toutes les suggestions « combler un macro » sortaient à **exactement 200 g**, la borne de repli appliquée faute de portion connue — le plafonnement par portion, qui venait d'être ajouté, ne pouvait pas s'appliquer. `update` **par id**, donc idempotent et rejouable. Le catalogue source `supabase/scripts/enrich-ciqual/foods-catalog.json` est mis à jour dans le même commit : une régénération du seed conservera ces portions. **Aucun changement de schéma** → types inchangés, ✅ **aucune sync rule à redéployer** (`foods` déjà publiée, lue en `select *`) |
|   [x]   | `20260801211256_runf3_runs_planned_session_id`      | 01/08/2026 | CLI (`npm run db:push`) — US RUN-F3 (5.25) : `runs.planned_session_id uuid null`, additif — le lien entre une course et sa séance planifiée n'existait nulle part (ni colonne, ni paramètre de démarrage). Pas de contrainte FK formelle (cohérent avec le reste du schéma), validité garantie côté app. ✅ **Aucune sync rule à redéployer** (`runs` déjà publiée, lue en `select *`) |
|   [x]   | `20260801211306_runf3_runs_terrain`                 | 01/08/2026 | CLI (`npm run db:push`) — US RUN-F3 (D3) : `runs.terrain text null check in ('road','trail','track','treadmill')`, additif, saisie facultative sans rapport avec le GPS. ✅ **Aucune sync rule à redéployer** (`runs` déjà publiée, lue en `select *`) |
|   [x]   | `20260801215137_muscf1b_exercises_muscles_fine`     | 01/08/2026 | CLI (`npm run db:push`) — US MUSC-F1b (6.2) : `exercises.muscles_fine jsonb not null default '[]'`, **additive et indépendante** de `muscles_secondary` (deux espaces distincts, spec §0) — l'anatomie fine à 10 muscles ne remplace pas les 6 groupes larges consommés par 18 fichiers déjà livrés. ✅ **Aucune sync rule à redéployer** (`exercises` déjà publiée, lue en `select *`) |
|   [x]   | `20260802055147_debt_seed_exercices_programme_placeholder` | 02/08/2026 | CLI (`npm run db:push`) — **Dette technique** : les 16 exercices de bibliothèque et le programme placeholder « Full Body Débutant » (US1/US2 historiques de `supabase/seed.sql`) étaient déjà sur le cloud mais arrivés par un chemin non tracé (`seed.sql` n'est joué que par `db:reset`, qui exige Docker). Contenu repris à l'identique, `on conflict do nothing` — **aucune ligne modifiée**. ⚠️ **1ʳᵉ tentative en échec** : les traductions déjà en base portaient des `id` différents des UUID déterministes du seed mais le même `(exercise_id, lang)` → conflit sur la mauvaise colonne cible (`id`), corrigé en `on conflict (exercise_id, lang)`. Transaction proprement annulée par le CLI, aucune ligne partielle. Aucun changement de schéma → pas de régénération de types. |
|   [x]   | `20260802144220_runf1b_elevation`                   | 02/08/2026 | CLI (`npm run db:push`) — US RUN-F1b (5.32) : `runs.elevation_gain_m numeric null`, `runs.elevation_loss_m numeric null`, additives. Scalaires cumulés en direct par le tracker (comme `distance_m`/`duration_seconds`), jamais recalculés depuis `gps_track` — le codec de trace ne change pas. ✅ **Aucune sync rule à redéployer** (`runs` déjà publiée, lue en `select *`). |
|   [x]   | `20260802193437_runf2a_voice_announcements`         | 02/08/2026 | CLI (`npm run db:push`) — US RUN-F2a (5.19) : `running_profiles.voice_announcements_enabled boolean not null default false`, `running_profiles.voice_announcement_interval_m integer not null default 1000`, additives. Premier réglage de comportement de course exposé à l'utilisateur, désactivé par défaut. ✅ **Aucune sync rule à redéployer** (`running_profiles` déjà publiée, lue en `select *`). |
|   [x]   | `20260802213841_runf2c_session_intervals`           | 02/08/2026 | CLI (`npm run db:push`) — US RUN-F2c (5.9) : nouvelle table `session_intervals` (une ligne = un bloc de répétitions, comme `exercise_plans.target_sets`). RLS + trigger `updated_at` + publication PowerSync identiques au patron `exercise_plans`. ⚠️ **Table neuve → 2 sync rules à coller dans le dashboard PowerSync et déployer** (`docs/specs/technical/powersync-sync-rules.yaml`), contrairement aux migrations d'ajout de colonne précédentes. |
|   [x]   | `20260803061055_runf2d_interval_guidance`           | 03/08/2026 | CLI (`npm run db:push`) — US RUN-F2d (5.18) : `runs.interval_phase_index integer null`, `runs.interval_phase_start_distance_m integer null`, `runs.interval_phase_start_duration_s integer null` (progression du guidage fractionné, persistée pour un rattrapage silencieux au remontage de l'écran) ; `running_profiles.interval_guidance_enabled boolean not null default false`. Toutes additives. ✅ **Aucune sync rule à redéployer** (`runs` et `running_profiles` déjà publiées, lues en `select *`). |
|   [x]   | `20260803101009_activ01_dismiss`                    | 03/08/2026 | CLI (`npm run db:push`) — US ACTIV-01 (1.27) : `profiles.activation_path_dismissed_at timestamptz null`, additive. Fermeture explicite du widget « Parcours 7 jours pour démarrer ». ✅ **Aucune sync rule à redéployer** (`profiles` déjà publiée, lue en `select *`). |
|   [x]   | `20260804145909_repas01_meal_plan_shopping_list`    | 04/08/2026 | CLI (`npm run db:push`) — US REPAS-01 (4.27/4.28/4.29) : **3 tables neuves** `meal_plan_entries`, `shopping_lists`, `shopping_list_items`. RLS utilisateur (patron `personal_goals`, pas de `delete` — soft delete), triggers `updated_at`, index partiels, publication PowerSync. **`meal_key` sans `CHECK`** (clé libre, comme `food_entries.meal_type` depuis 4.15 : les repas sont personnalisables) et **snapshot des macros** sur l'entrée de planning — les ingrédients, eux, sont relus vivants à la génération de la liste (R6). ⚠️ **PAS de contrainte unique `(user_id, week_start_date)`, délibérément** (décision D6) : deux appareils générant la même semaine hors réseau produiraient une violation d'unicité qui **fait échouer l'upload PowerSync et bloque la file d'écriture** — la liste active est la plus récente par `generated_at`. Types régénérés (3 tables présentes). ⚠️ **Tables neuves → 3 sync rules à coller dans le dashboard PowerSync et déployer** (`docs/specs/technical/powersync-sync-rules.yaml`, bucket `user_data`). Warning CLI au push (`failed to cache migrations catalog`, certificat pg-delta absent) : concerne la mise en cache du catalogue, **pas** l'application du SQL — vérifié par la présence des 3 tables dans les types régénérés depuis le cloud. |
|   [x]   | `20260804150934_repas01_consumed_entry_ids`         | 04/08/2026 | CLI (`npm run db:push`) — US REPAS-01 **complément** : `meal_plan_entries.consumed_entry_ids jsonb null`, additive. Trace les `food_entries` créées par « J'ai mangé ça » pour qu'« Annuler » retire **exactement** ces lignes (règle R3). Les retrouver par `(log_date, meal_type, name)` supprimerait la mauvaise ligne dès qu'on a mangé deux fois la même chose dans la journée, ou renommé un aliment. Types régénérés. ✅ **Aucune sync rule à redéployer** (`meal_plan_entries` déjà couverte par la règle `select *` ajoutée par la migration précédente — à condition qu'elle ait bien été déployée). |

> **14/07/2026 — historique réconcilié.** Les 10 migrations des 12–13/07, jouées à la main dans la
> console, ont été marquées `applied` via `supabase migration repair`. `npm run db:push:dry` répond
> désormais « Remote database is up to date ». À partir d'ici, toute nouvelle migration passe par
> `npm run db:push` (plus jamais la console).
|   [x]   | `20260909120000_runf4_segment_progression`          | 09/09/2026 | CLI (`npm run db:push`, Florian) — US RUN-F4 (**mur M8**) : `session_intervals.fast_pace_progressive boolean not null default false`. Le seul mur de l'analyse du 04/09/2026 qui n'avait été affecté à **aucun** des 10 lots — signalé à Florian le 09/09, qui a demandé de finir la vague. Le besoin : « les 10 dernières minutes de 4:35 **vers** 4:25 » décrit une **rampe**, pas une tolérance ; la plage 4:25–4:35 disait « tiens-toi entre les deux », ce qui n'est pas la même consigne. ⚠️ **Un booléen, et surtout pas deux colonnes d'allure de plus** : les bornes existent déjà (`fast_pace_max` = départ, `fast_pace_min` = arrivée), et un second jeu de bornes pourrait **contredire** le premier. Le drapeau ne change que la LECTURE des deux colonnes en place. Additive avec défaut : aucune ligne existante ne change de sens. ✅ Aucune sync rule (`select *`). 🔴 Colonne déclarée dans `powersync/schema.ts`. |
|   [x]   | `20260905090000_runf4_sessions_consigne`            | 09/09/2026 | CLI (`npm run db:push`, Florian) — US RUN-F4 (lots A/G/I) : 8 colonnes additives sur `sessions` (plage d'allure cible **saisie**, RPE cible, objectif chrono, plan de passage jsonb, description, consigne, critère d'adaptation). Poussée par **Florian le 09/09/2026** (`db:push:dry` répond « Remote database is up to date ») ; types régénérés dans la foulée. ⚠️ **DROP de `sessions_session_type_check`, délibéré** : c'est la leçon écrite ici pour `pain_reports.zone` (DOUL-01) et `food_entries.meal_type` — un CHECK sur un enum **applicatif et évolutif** est un piège en offline-first, une valeur inconnue du serveur **bloque toute la file d'upload PowerSync**, pas seulement la ligne fautive. `SESSION_TYPES` (packages/shared) fait foi, validé par Zod. ✅ **Aucune sync rule** : `sessions` est déjà publiée et lue en `select *`, les colonnes descendent seules. 🔴 Les 8 colonnes sont déclarées dans `powersync/schema.ts` **et couvertes par un test d'écriture-relecture** (`run-structured-sql.test.ts`) — sans ça, l'écriture échoue en silence (panne CYCLE-01 du 31/07, répétée par HORAIRE-01). |
|   [x]   | `20260905090001_runf4_session_segments`             | 09/09/2026 | CLI (`npm run db:push`, Florian) — US RUN-F4 (lots A/B/C/D) : 11 colonnes additives sur `session_intervals`, qui devient une suite de **segments typés**. `kind` (`default 'work'` = le sens exact des lignes RUN-F2c déjà en base), `label`, allure absolue de la fraction, **chrono cible distinct de l'étendue** (`fast_target_time_*` — « 400 m en 1:38 », inécrivable jusqu'ici et forme de 12 séances sur 24), intensité de récupération, et l'imbrication (`group_key` + `group_reps`). ⚠️ **Pas de seconde table pour les groupes** : elle coûterait une publication, un bloc RLS et **une sync rule manuelle de plus** pour un seul niveau d'imbrication ; `group_reps` est dupliqué sur chaque membre, la valeur du **premier** fait foi. ⚠️ Pas de CHECK sur `kind` ni `recovery_kind`, même raison qu'au-dessus. ✅ Aucune sync rule (`select *`). 🔴 Colonnes déclarées dans `powersync/schema.ts` + test d'écriture-relecture. |
|   [x]   | `20260905090002_runf4_run_intervals`                | 09/09/2026 | CLI (`npm run db:push`, Florian) — US RUN-F4 (lot F) : **table neuve** `run_intervals`, une ligne par phase franchie — l'équivalent de l'onglet « Détail des tours » d'une montre. Le **prévu y est RECOPIÉ, pas joint** : modifier une séance planifiée ne doit jamais réécrire l'histoire d'une course déjà courue, d'où `block_id` **sans FK** (une FK cascade effacerait l'historique, une FK restrictive bloquerait l'édition du programme). Index **unique partiel** `(run_id, phase_index) where deleted_at is null` : le rattrapage silencieux de RUN-F2d (R8 bis) rejoue la progression au remontage de l'écran et dupliquerait les lignes — la garde est **aussi applicative** (`recordIntervalResult` vérifie avant d'insérer), parce qu'une violation de contrainte bloquerait la file d'upload entière. RLS utilisateur, pas de politique `delete`. 🔴 **Ajoutée à `EXPORT_TABLES` (RGPD)** — le test de complétude de l'export l'a exigé le jour même, exactement comme il avait rattrapé l'oubli de `session_intervals` par RUN-F2c. |
|   [x]   | `20260905090003_runf4_run_intervals_publication`    | 09/09/2026 | CLI (`npm run db:push`, Florian) — US RUN-F4 **complément** : `alter publication powersync add table`, gardé par `pg_publication_tables`. Patron `20260806090001` (pain_reports). 🔴 **Table neuve → 1 sync rule à coller dans le dashboard PowerSync et déployer À LA MAIN** (bucket `user_data`, ligne déjà ajoutée au YAML). Étape oubliée **trois fois** (BIEN-01, RUN-F2c, VIE-01) : sans elle, le réalisé par répétition reste local et ne remonte jamais, **sans aucune erreur visible**. |
|   [x]   | `20260905090004_runf4_programs_objectif`            | 09/09/2026 | CLI (`npm run db:push`, Florian) — US RUN-F4 (lot H) : 3 colonnes additives sur `programs` — `target_date`, `target_time_seconds`, `event_name`. Referme un paradoxe : RUN-14 sait **prédire** un temps de course (Riegel) et la 5.31 sait **recaler** l'allure de référence sur un record, mais on ne pouvait nulle part écrire « ma course est le 25/10/2026 et je vise 20:00 ». **`date` nu et non `timestamptz`** : même raisonnement que `planned_sessions.scheduled_date` (HORAIRE-01 D8) — une course le 25/10 reste le 25/10 quel que soit le fuseau. Le calendrier existait déjà : **il manquait l'ancre, pas le calendrier**. ✅ Aucune sync rule. 🔴 Colonnes déclarées dans `powersync/schema.ts` — **oubli commis puis rattrapé le jour même par les tests de `duplicateProgram`**, qui dérivent leur DDL du schéma local. |
|   [x]   | `20260905090005_runf4_session_translations`         | 09/09/2026 | CLI (`npm run db:push`, Florian) — US RUN-F4 (lot I) : **table neuve** `session_translations`, miroir structurel exact de `program_translations`. Le seed CONTENU-01 notait explicitement qu'elle n'existait pas : la bibliothèque était bilingue au niveau du **programme** et monolingue au niveau de la **séance**, ce qui contredit la décision G. ⚠️ **Additive, jamais substitutive** : `sessions.name` n'est ni supprimée ni migrée, la résolution est `COALESCE(langue courante, fr, sessions.name)` — les 3 programmes éditoriaux publiés et toutes les séances personnelles s'affichent **sans qu'une seule ligne soit écrite ici**. RLS « contenu partageable » (`owner_id` nullable), patron `program_translations`. 🔴 Ajoutée à `EXPORT_TABLES` (RGPD). ⚠️ **Aucune UI n'écrit encore dedans** — la plomberie est posée, la surface reste à faire. |
|   [x]   | `20260905090006_runf4_session_translations_publication` | 09/09/2026 | CLI (`npm run db:push`, Florian) — US RUN-F4 **complément** : publication logique, même patron que `…090003`. 🔴 **2ᵉ des 2 sync rules neuves de RUN-F4, à déployer À LA MAIN.** Celle-ci va dans le bucket **global/éditorial** (comme `program_translations`), **pas** dans `user_data` : les traductions de séances éditoriales ont `owner_id` NULL et doivent descendre à tout le monde. Sans elle, une séance traduite s'affiche en français pour tous, **sans aucune erreur visible**. |
|   [x]   | `20260812061859_planned_sessions_scheduled_time`     | 12/08/2026 | CLI (`npm run db:push`) — US HORAIRE-01 (roadmap 2.4) : `planned_sessions.scheduled_time time null`, additive. Heure **locale** facultative d'une séance planifiée. **`time` et non `timestamptz`** (D8) : même raisonnement que `scheduled_date`, déjà un `date` nu — une séance à 18 h doit rester à 18 h, un `timestamptz` la déplacerait au changement de fuseau. **Nullable sans défaut** (D1) : planifier « jeudi » sans heure est un usage normal, et un défaut inventerait des convocations pour des séances qui n'en attendaient pas. Pas d'index (la colonne ne filtre rien). ✅ **AUCUNE sync rule à redéployer** — `planned_sessions` est lue en `select *` ([YAML:89](../docs/specs/technical/powersync-sync-rules.yaml)), la colonne descend automatiquement, comme les 5 colonnes de `user_settings` dans le même cas. ⚠️ **Le cadrage de cette US affirmait le contraire et en faisait son risque n° 1 : c'était faux**, corrigé le jour même — exactement l'erreur qu'avait commise le cadrage de COLLIS-01. Le réflexe « migration ⇒ sync rule à la main » ne vaut que pour une **table neuve**. 🔴 Le vrai risque est ailleurs et il est traité : colonne déclarée dans `powersync/schema.ts` — sans elle l'écriture échoue et l'erreur est avalée, l'heure ne se posant jamais sans message (panne exacte de CYCLE-01, recette du 31/07/2026). ⚠️ Même warning CLI `pg-delta`/certificat qu'aux push de REPAS-01, VIE-01 et DOUL-01 : il porte sur la mise en cache du catalogue, pas sur l'application du SQL — vérifié par `npm run db:types` (`scheduled_time: string \| null` présent) et par `db:push:dry` (« Remote database is up to date »). |
|   [x]   | `20260804210516_muscpwr01_sbd_lifts`                | 04/08/2026 | CLI (`npm run db:push`) — US MUSCPWR-01 (catalogue MUSC-16/27/29) : `user_settings.sbd_lifts jsonb null`, additive. Désignation par l'utilisateur de **ses** trois mouvements de force (`{squat, bench, deadlift}` → id d'exercice), qui sert aussi d'**opt-in** au module force. **JSON et non 3 colonnes** : la liste peut s'étendre (strict-curl, overhead press) sans migration. Pourquoi une désignation et pas une reconnaissance par nom : celle-ci échouerait sur toute variante (« Squat barre basse ») et sur les exercices perso — or c'est précisément ce qu'utilise un pratiquant de force. ⚠️ **Seule écriture de toute l'US** : les trois analyses (%1RM, DOTS, total SBD + projection) sont **entièrement dérivées** — aucun score stocké, donc rien à recalculer, et corriger une série passée corrige immédiatement l'analyse. Types régénérés. ✅ **Aucune sync rule à redéployer** (`user_settings` déjà publiée, lue en `select *` — même situation que `intensity_scale` et `cycle_tracking_enabled`). Colonne déclarée dans le schéma PowerSync local **et couverte par un test d'écriture-relecture** : c'est la 3ᵉ colonne de cette table dans ce cas, les deux précédentes (`cycle_tracking_enabled`, `daily_step_goal`) ayant causé des pannes silencieuses. |
|   [x]   | `20260806090000_doul01_pain_reports`                | 06/08/2026 | CLI (`npm run db:push`) — US DOUL-01 (roadmap 1.29) : **table neuve** `pain_reports` (une ligne par (jour, zone), avec son niveau). RLS utilisateur, index unique **partiel** `(user_id, log_date, zone) where deleted_at is null` — une ligne soft-deletée ne doit pas empêcher d'en recréer une —, pas de politique `delete`. ⚠️ **`zone` volontairement SANS `CHECK`** : la liste des 18 zones est **applicative** et évolutive (D1 l'a déjà étendue des 10 muscles aux muscles **et** articulations) ; un `CHECK` imposerait une migration à chaque ajout et, surtout, **une violation bloquerait la file d'upload PowerSync** si un client plus récent écrivait une zone inconnue du serveur. Même raisonnement que `meal_key` (REPAS-01) et `food_entries.meal_type`. `level`, lui, **garde** son `CHECK` : 3 valeurs fermées, une valeur inconnue y serait un bug. ⚠️ **Donnée de santé** → export RGPD obligatoire (`data-export.ts`, fait) et opt-in strict (migration `…090002`). ⚠️ Même warning CLI qu'aux push de REPAS-01 et VIE-01 (`failed to cache migrations catalog`, certificat pg-delta absent) : il porte sur la **mise en cache du catalogue**, pas sur l'application du SQL — vérifié par `npm run db:types`, dont le diff ajoute **44 lignes et n'en retire aucune** (la table, ses 8 colonnes et la colonne d'opt-in). |
|   [x]   | `20260806090001_doul01_pain_reports_publication`    | 06/08/2026 | CLI (`npm run db:push`) — US DOUL-01 **complément** : `alter publication powersync add table`, gardé par `pg_publication_tables`. Patron `20260805140001` (real_life_periods). 🔴 **Table neuve → 1 sync rule à coller dans le dashboard PowerSync et déployer À LA MAIN** (`docs/specs/technical/powersync-sync-rules.yaml`, bucket `user_data`, ligne déjà ajoutée au YAML). Étape oubliée **deux fois** (BIEN-01, RUN-F2c) : sans elle les déclarations restent locales et ne remontent jamais, **sans aucune erreur visible**. |
|   [x]   | `20260806090002_doul01_pain_journal_opt_in`         | 06/08/2026 | CLI (`npm run db:push`) — US DOUL-01 : `user_settings.pain_journal_enabled boolean not null default false`, additive et gardée par `if not exists`. **Opt-in strict sur une donnée de santé** — un `default true` mettrait tous les comptes existants à collecter dès la première synchro. ✅ **Aucune sync rule pour cette colonne** (`user_settings` déjà publiée, lue en `select *`) — 5ᵉ colonne de cette table dans ce cas après `session_conflicts_enabled`, `sbd_lifts`, `intensity_scale` et `cycle_tracking_enabled`. 🔴 Colonne déclarée dans `powersync/schema.ts` **et** couverte par un test d'écriture-relecture (`pain-sql.test.ts`), plus les 4 points d'édition de `settings-repository.ts` et le schéma Zod — c'est la checklist que la panne silencieuse de CYCLE-01 a laissée. |
|   [x]   | `20260805140000_vie01_real_life_periods`            | 05/08/2026 | CLI (`npm run db:push`) — US VIE-01 (roadmap 1.28) : **table neuve** `real_life_periods` (une ligne = une période déclarée `[started_on, ends_on]`, **bornes incluses**). RLS utilisateur + index `(user_id, started_on desc)`, **pas de politique `delete`** (soft delete, patron `streak_jokers`). ⚠️ **AUCUNE contrainte de cohérence de plage, délibérément** : ni `check (ends_on >= started_on)`, ni `exclude using gist` contre les chevauchements — c'est la leçon de REPAS-01 (D6), **une violation de contrainte bloque la file d'upload PowerSync**, et deux appareils hors réseau peuvent déclarer des périodes qui se recouvrent. La validation est applicative (`validateRealLifePeriod`) et la lecture absorbe un chevauchement en prenant l'**union** des jours. ⚠️ **Pas de colonne « motif »** : ce serait une donnée de santé (« malade »), qui rouvrirait la politique de confidentialité **et** la déclaration Google Play « Health apps » — déjà passée à 6 types par CYCLE-01 et sur le chemin critique du lancement. Le motif n'a par ailleurs **aucun effet fonctionnel** (D1 : le fléchissement est identique quelle que soit la cause). |
|   [x]   | `20260805140001_vie01_real_life_periods_publication` | 05/08/2026 | CLI (`npm run db:push`) — US VIE-01 **complément** : `alter publication powersync add table`, gardé par `pg_publication_tables`. Patron `20260729095705` (streak_jokers). ⚠️ **Même warning CLI qu'au push de REPAS-01** (`failed to cache migrations catalog`, certificat pg-delta absent) : il concerne la **mise en cache du catalogue**, pas l'application du SQL — vérifié de la même façon, par `npm run db:types`, qui fait bien apparaître `real_life_periods` et ses 7 colonnes depuis le cloud. 🔴 **Table neuve → 1 sync rule à coller dans le dashboard PowerSync et déployer À LA MAIN** (`docs/specs/technical/powersync-sync-rules.yaml`, bucket `user_data`, ligne déjà ajoutée au YAML) : **c'est la seule étape encore ouverte**. Oubliée **deux fois** (BIEN-01, puis RUN-F2c qui reste bloquée avant recette pour cette raison) : sans elle les périodes restent locales et ne remontent jamais, **sans aucune erreur visible**. |
|   [x]   | `20260805081425_collis01_session_conflicts_opt_in` | 05/08/2026 | CLI (`npm run db:push`) — US COLLIS-01 (roadmap 3.57) : `user_settings.session_conflicts_enabled boolean not null default false`, additive et gardée par `if not exists`. **Opt-in strict du détecteur de collisions** entre séances planifiées (décision H — intégration sans imposition) : un `default true` aurait ouvert la fonctionnalité à tout le monde dès la première synchro, soit l'inverse d'un opt-in. **Seule écriture de toute l'US** — les conflits sont **entièrement dérivés** du planning, rien n'est stocké, donc déplacer une séance met à jour la détection immédiatement. Types régénérés. ✅ **Aucune sync rule à redéployer** (`user_settings` déjà publiée, lue en `select *` — 4ᵉ colonne dans ce cas après `sbd_lifts`, `intensity_scale` et `cycle_tracking_enabled`). ⚠️ Le premier cadrage affirmait le contraire et en faisait son risque n° 1 : **c'était faux**, et la relecture l'a démenti en citant la migration de la veille. Le vrai risque, lui, est traité : colonne déclarée dans le schéma PowerSync **local** — sans elle l'écriture échoue et `void updateSettings()` avale l'erreur, l'interrupteur restant éteint sans message (panne exacte de CYCLE-01, recette du 31/07/2026). |
|   [x]   | `20260910153006_nutri_ux01_hydration_and_plan_food` | 10/09/2026 | CLI (`npm run db:push`) — US NUTRI-UX01, **deux blocs en une migration** (scinder imposerait deux push pour une même US). ① **table neuve `water_entries`** (hydratation, R5 / catalogue NUTR-12, ouverte en V1 par arbitrage de Florian contre la spec §8) : une ligne **par ajout**, jamais un total — annuler doit défaire *le dernier geste*, et deux appareils hors réseau doivent **s'additionner** plutôt que s'écraser au dernier écrivain. RLS utilisateur, index partiel `(user_id, log_date)`, `check volume_ml between 1 and 5000`. Plus 2 colonnes **nullables** sur `nutrition_profiles` (`water_target_ml`, `glass_size_ml`) : `null` = jamais réglé → l'app applique ses défauts sans les figer, pour qu'un changement de défaut profite à ceux qui n'ont rien choisi. ② `meal_plan_entries` accepte `source_type` **`food`** et **`quick`** (R7.1) — le planning n'acceptait que recette et repas type, ce qui imposait de créer une recette avant de pouvoir planifier quoi que ce soit (~20 taps) ; + `food_id`, `quantity_g`, et une contrainte de cohérence source↔référence (sans elle une entrée `food` sans `food_id` passerait et la liste de courses la compterait non résolue en silence). 🔴 **Table neuve → 1 sync rule à coller dans le dashboard PowerSync et déployer À LA MAIN** (bucket `user_data`, ligne déjà au YAML). 🔴 Table déclarée dans `powersync/schema.ts` **et** enregistrée dans le `Schema` — sans quoi l'écriture locale échoue et l'erreur est avalée (panne exacte de CYCLE-01). ⚠️ Même warning CLI `pg-delta` qu'aux push de REPAS-01/VIE-01/DOUL-01 (cache du catalogue, Docker absent) : il ne porte pas sur l'application du SQL — vérifié par `npm run db:types`, qui fait apparaître `water_entries` et ses 7 colonnes depuis le cloud. |
|   [x]   | `20260910153158_nutri_ux01_water_entries_publication` | 10/09/2026 | CLI (`npm run db:push`) — US NUTRI-UX01 **complément** : `alter publication powersync add table`, gardé par `pg_publication_tables`. Patron `20260806090001` (pain_reports). 🔴 **C'est la publication, pas la sync rule** : celle-ci reste à déployer à la main. Étape oubliée **trois fois** (BIEN-01, RUN-F2c, VIE-01) — sans elle, les verres bus restent locaux et ne remontent jamais, **sans aucune erreur visible**. ✅ Les 2 colonnes de `nutrition_profiles` n'ont besoin de rien : table déjà publiée, lue en `select *`. |
|   [x]   | `20260910153450_nutri_ux01_activity_level_nullable`  | 10/09/2026 | CLI (`npm run db:push`) — US NUTRI-UX01 (R1.3) : `nutrition_profiles.activity_level` passe **nullable** (`drop not null` + `drop default`). ⚠️ **Ce n'est pas cosmétique.** La colonne était `not null default 'moderate'`, donc l'app ne pouvait pas distinguer « l'utilisateur a choisi *modérément actif* » de « personne n'a jamais posé la question ». Or ce champ est le **multiplicateur du TDEE** (×1,2 à ×1,9) : un sédentaire à qui l'on applique ×1,55 en silence reçoit un objectif **surestimé de ~614 kcal/jour**, de quoi annuler tout un déficit de sèche — et l'écran de réglage l'affichait comme une sélection radio, exactement comme s'il l'avait cochée. `null` signifie désormais « pas encore répondu » ; les lecteurs gardent leur repli `?? 'moderate'` (`effectiveActivityLevel`), c'est le **repli qui devient visible**. **Additive et sans perte** : les lignes existantes gardent leur valeur — un compte ayant déjà réglé quelque chose porte 'moderate' et sera considéré comme ayant choisi (on ne réécrit pas l'histoire) ; un compte n'ayant jamais ouvert ces réglages n'a **aucune ligne** et tombe naturellement dans « jamais répondu ». ✅ Aucune sync rule (`nutrition_profiles` déjà publiée, lue en `select *`). |
|   [x]   | `20260910162217_nutri_ux01_food_preparation_state`   | 10/09/2026 | CLI (`npm run db:push`) — US NUTRI-UX01 (R6.7) : `foods.preparation_state text check (in ('raw','cooked'))`, nullable. 100 g de riz **cru** = ~330 kcal, 100 g de riz **cuit** = ~110 : un facteur 3 sur l'aliment le plus courant d'une assiette française, et la première source d'erreur de saisie de la catégorie. La règle métier §8 exigeait « une mention **cru / cuit** sur les aliments concernés » — elle n'était appliquée nulle part, et un aliment perso ou scanné n'avait **aucun moyen** de la porter. 🔴 **Aucune donnée n'est migrée, délibérément** : les 80 aliments de bibliothèque portent déjà l'information **dans leur nom** (« Poulet (blanc, cuit) »), et l'app la **dérive** quand la colonne est nulle (`resolvePreparationState`, brique pure, 10 tests). La colonne porte l'état **déclaré**, le nom sert de repli — le badge apparaît donc immédiatement sur toute la bibliothèque, sans une seule ligne touchée. Sans ce repli, il ne se serait affiché que sur les aliments créés après cette US, c'est-à-dire là où l'utilisateur en a le moins besoin. ✅ Aucune sync rule (`foods` déjà publiée, `select *`). 🔴 Colonne déclarée dans `powersync/schema.ts`. |
|   [x]   | `20260912050404_muscuux02_summary_display_level`     | 12/09/2026 | CLI (`npm run db:push`) — US MUSCU-UX02 : `profiles.summary_display_level text default 'normal' check (in ('simplified','normal','detailed'))`, additive. Niveau de lecture du **bilan** de séance (Simple / Intermédiaire / Avancé). ⚠️ **Colonne distincte de `workout_display_level`, et c'est la décision D1 de l'US** : celle-là règle la densité de **saisie** sous la barre, où l'on veut le minimum de champs ; celle-ci règle la profondeur de **lecture** du bilan, consulté assis au calme. Rien n'impose que ce soit le même choix, et les confondre forcerait l'un des deux réglages à être mauvais. Mêmes valeurs et même défaut applicatif — `coerceWorkoutDisplayLevel` relit les deux, aucun nouvel enum. NULL → 'normal' à la lecture (le défaut SQL ne couvre que les lignes créées après la migration). ✅ **Aucune sync rule à redéployer** : `profiles` est déjà publiée et lue en `select *`, la colonne descend automatiquement — le réflexe « migration ⇒ sync rule à la main » ne vaut que pour une **table neuve**. 🔴 Colonne déclarée dans `powersync/schema.ts` : sans elle l'écriture échoue et `void upsertProfile()` avale l'erreur, le sélecteur revenant à sa valeur précédente sans message (panne exacte de CYCLE-01, recette du 31/07/2026). ⚠️ Même warning CLI `pg-delta` qu'aux push précédents (cache du catalogue, Docker absent) : il ne porte pas sur l'application du SQL — vérifié par `npm run db:types`, qui fait apparaître `summary_display_level` sur les 3 vues de `profiles`. |

| [x] | `20260912235121_corps02_body_visual_state` | 13/09/2026 | CLI : ajout nullable `user_settings.body_visual_state jsonb`, contrainte objet/null, RLS/bucket/export existants. Dry-run : cette seule migration ; application réussie, types cloud régénérés (Row/Insert/Update), second dry-run à jour. Warning cache pg-delta/Docker déjà connu, sans échec SQL. Aucune sync rule à redéployer. |
