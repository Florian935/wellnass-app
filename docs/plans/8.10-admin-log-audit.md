# US 8.10 — Log d'audit admin — Plan d'implémentation

> **Pour l'exécutant :** tâche par tâche (cases `- [ ]`), TDD, commits fréquents.
> Spec : [8.10-admin-log-audit.md](../specs/functional/us/8.10-admin-log-audit.md).
> Sous-skill recommandé : `superpowers:subagent-driven-development` ou `superpowers:executing-plans`.

**Goal :** un journal d'audit **append-only et non supprimable** des écritures éditoriales + rôles du
back-office, avec un écran `/audit` (super_admin) filtrable.

**Architecture :** logique pure (union d'actions, schéma Zod, mapping libellés) dans
`@wellness/shared/audit.ts` (testée Vitest) ; l'admin (`apps/admin`) fait l'I/O — `data/audit.ts`
(`logAudit`/`listAudit`), instrumentation des mutations existantes (rôles, exos, programmes,
aliments), écran de consultation. Table `audit_log` **web/admin uniquement** (hors PowerSync),
append-only (RLS + trigger). Capture **applicative best-effort** (clé anon, équipe de confiance).

**Tech Stack :** TypeScript, Zod (`@wellness/shared`), React + Vite + react-router (admin),
supabase-js, Vitest, PostgreSQL (Supabase).

---

## Convention de capture (à lire avant les tâches d'instrumentation)

Le `logAudit(...)` est appelé **dans la couche data** (`apps/admin/src/data/*.ts`), **après succès**
de la mutation (`error == null`), **jamais bloquant** (son échec n'affecte pas le retour de la
mutation). Deux cas selon que la fonction dispose du **libellé lisible** :

- **Libellé en main** (`saveExercise`/`saveFood`/`createEditorialProgram`/`updateProgramMeta` via
  `input.nameFr`) → `target_label` renseigné directement.
- **Fonctions à id seul** (`setStatus`, `archive*`, `revokeRole`) → **ajout d'un paramètre optionnel
  de contexte d'audit** (ex. `opts?: { label?: string }`, `revokeRole(id, opts?: { role, userId })`).
  Le paramètre est **additif** : les appelants existants restent valides, le **comportement de
  retour est inchangé** (conforme spec §7). L'écran appelant, qui détient déjà le nom, le passe.
  Si le libellé n'est pas fourni, `target_label = null` et l'écran de consultation retombe sur
  `target_table`/`target_id` (spec §8).

> **Écart assumé vs spec §7** (à signaler à la validation) : la spec supposait le nom FR « déjà en
> main dans la couche » pour l'archivage ; il ne l'est que pour create/update. D'où le paramètre de
> libellé optionnel pour `setStatus`/`archive*`/`revokeRole`. Décision honorant l'intention « libellé
> lisible » sans lookup supplémentaire.

**Discriminant create/update** : `*.create` si `input.id` **absent**, sinon `*.update`
(`saveFood` = branche explicite `if (input.id)` ; `saveExercise` = `input.id ?? uuid`).

---

## Structure des fichiers

- **Créer** `packages/shared/src/audit.ts` — `AuditAction`, `AUDIT_ACTIONS`, `auditEntrySchema`, `auditActionLabelKey` (pur).
- **Créer** `packages/shared/src/audit.test.ts` — tests.
- **Modifier** `packages/shared/src/index.ts` — `export * from './audit'`.
- **Créer** `supabase/migrations/<ts>_admin_audit_log.sql` — table + index + RLS + trigger d'immuabilité.
- **Créer** `apps/admin/src/data/audit.ts` — `logAudit`, `listAudit`, types.
- **Modifier** `apps/admin/src/data/roles.ts` — `grantRole` (`.select('id')` + retour `id`) + `logAudit` grant/revoke.
- **Modifier** `apps/admin/src/data/exercises.ts` — `logAudit` dans `saveExercise` / `setStatus` / `archiveExercise` (+ param libellé).
- **Modifier** `apps/admin/src/data/programs.ts` — `logAudit` dans `createEditorialProgram` / `updateProgramMeta` / `setStatus` / `archiveProgram` (+ param libellé).
- **Modifier** `apps/admin/src/data/foods.ts` — `logAudit` dans `saveFood` / `archiveFood` (+ param libellé) / `importFoods`.
- **Modifier** les écrans appelant `setStatus`/`archive*`/`revokeRole` pour passer le libellé (`ExercisesScreen`, `ProgramsScreen`, `FoodsScreen`, `RolesScreen`, + écrans d'édition si l'action y est déclenchée).
- **Créer** `apps/admin/src/screens/AuditScreen.tsx` — liste + filtres.
- **Modifier** `apps/admin/src/App.tsx` — route `/audit` (`RequireSuperAdmin`).
- **Modifier** `apps/admin/src/components/AdminLayout.tsx` — `NavLink` « Journal d'audit » (`isSuperAdmin`).
- **Modifier** `apps/admin/src/i18n/fr.ts` — section `audit` (+ `audit.action.*`).

---

## Task 1 : `@wellness/shared/audit.ts` (pur, TDD)

**Files:** Create `packages/shared/src/audit.ts` + `audit.test.ts` ; Modify `index.ts`.

Interface :
```ts
export type AuditAction =
  | 'role.grant' | 'role.revoke'
  | 'exercise.create' | 'exercise.update' | 'exercise.archive' | 'exercise.publish'
  | 'program.create' | 'program.update' | 'program.archive' | 'program.publish'
  | 'food.create' | 'food.update' | 'food.archive' | 'food.import';

export const AUDIT_ACTIONS: readonly AuditAction[] = [ /* même ordre que l'union */ ];

export type AuditEntryInput = {
  action: AuditAction;
  targetTable?: string | null;
  targetId?: string | null;
  targetLabel?: string | null;
  details?: Record<string, unknown>;
};

export const auditEntrySchema: z.ZodType<AuditEntryInput>;      // action ∈ union ; targetId uuid|null ; details défaut {}
export function auditActionLabelKey(action: AuditAction): string; // ex. 'audit.action.program.publish'
```

- [ ] **Step 1 — Test qui échoue** (`audit.test.ts`) :

```ts
import { describe, expect, it } from 'vitest';
import { AUDIT_ACTIONS, auditActionLabelKey, auditEntrySchema } from './audit';

describe('audit', () => {
  it('accepte une entrée minimale', () => {
    const r = auditEntrySchema.safeParse({ action: 'food.import' });
    expect(r.success).toBe(true);
    if (r.success) expect(r.data.details).toEqual({});
  });
  it('accepte une entrée complète', () => {
    const r = auditEntrySchema.safeParse({
      action: 'role.grant', targetTable: 'user_roles',
      targetId: '00000000-0000-0000-0000-000000000001',
      targetLabel: 'moderator → 42', details: { role: 'moderator', targetUserId: '42' },
    });
    expect(r.success).toBe(true);
  });
  it('rejette une action hors union', () => {
    expect(auditEntrySchema.safeParse({ action: 'food.delete' }).success).toBe(false);
  });
  it('rejette un targetId non-uuid', () => {
    expect(auditEntrySchema.safeParse({ action: 'food.archive', targetId: 'nope' }).success).toBe(false);
  });
  it('auditActionLabelKey renvoie une clé pour CHAQUE action (complétude)', () => {
    for (const a of AUDIT_ACTIONS) expect(auditActionLabelKey(a)).toMatch(/^audit\.action\./);
    expect(new Set(AUDIT_ACTIONS).size).toBe(AUDIT_ACTIONS.length); // pas de doublon
  });
});
```

- [ ] **Step 2 — Lancer, échec** : `npx vitest run packages/shared/src/audit.test.ts` → FAIL (module absent).
- [ ] **Step 3 — Implémenter** `audit.ts` : l'union + `AUDIT_ACTIONS` (mêmes valeurs, ordre stable) ;
  `auditEntrySchema = z.object({ action: z.enum([...AUDIT_ACTIONS]), targetTable: z.string().nullish(),
  targetId: z.string().uuid().nullish(), targetLabel: z.string().nullish(),
  details: z.record(z.unknown()).default({}) })` ; `auditActionLabelKey = (a) => `audit.action.${a}``
  (préfixe stable ; le mapping FR vit dans `i18n/fr.ts`, la fonction ne fait que produire la clé).
- [ ] **Step 4 — Lancer, succès** : même commande → PASS. Puis `export * from './audit';` dans `index.ts`.
- [ ] **Step 5 — Commit** : `git commit -m "feat(shared): module audit (actions, schéma Zod, clés libellés) — TDD"`.

---

## Task 2 : migration `audit_log`

**Files:** Create `supabase/migrations/<timestamp>_admin_audit_log.sql`.

> ⚠️ Timestamp postérieur à `20260714120000` (coordonner la plage avec Damien ; suggestion
> `20260714170000`). Contenu :

```sql
-- US 8.10 — log d'audit des actions admin (écritures éditoriales + rôles).
-- Table web/admin uniquement — HORS publication PowerSync (le mobile n'en a pas besoin).
-- Append-only & non supprimable : aucune policy update/delete + trigger d'immuabilité.
-- Réf. : docs/specs/functional/us/8.10-admin-log-audit.md.

create extension if not exists pgcrypto;

create table public.audit_log (
  id           uuid primary key default gen_random_uuid(),
  actor_id     uuid,                       -- PAS de FK cascade : la trace survit à la suppression du compte
  actor_email  text,
  action       text not null,
  target_table text,
  target_id    uuid,
  target_label text,
  details      jsonb not null default '{}',
  created_at   timestamptz not null default now()
);

create index audit_log_created_at_idx on public.audit_log (created_at desc);
create index audit_log_actor_idx      on public.audit_log (actor_id);
create index audit_log_action_idx     on public.audit_log (action);

alter table public.audit_log enable row level security;

-- Lecture : super_admin uniquement. (is_super_admin() / is_admin() définies en 20260713100000.)
create policy audit_log_select on public.audit_log
  for select using (public.is_super_admin());

-- Écriture : tout admin, en son propre nom (acteur honnête). AUCUNE policy update/delete → append-only.
create policy audit_log_insert on public.audit_log
  for insert with check (public.is_admin() and actor_id = auth.uid());

-- Filet d'immuabilité : verrouille même si une policy update/delete était ajoutée par erreur.
create or replace function public.audit_log_immutable()
  returns trigger language plpgsql as $$
begin
  raise exception 'audit_log est append-only (ni UPDATE ni DELETE)';
end;
$$;
create trigger audit_log_no_update before update on public.audit_log
  for each row execute function public.audit_log_immutable();
create trigger audit_log_no_delete before delete on public.audit_log
  for each row execute function public.audit_log_immutable();

-- NB : PAS d'ajout à la publication `powersync` (table admin/web uniquement).
```

- [ ] **Step 1** — Écrire la migration (ci-dessus), timestamp validé.
- [ ] **Step 2 — Commit** : `git commit -m "chore(db): migration audit_log (append-only, RLS super_admin, trigger immuabilité)"`.
- [ ] **Step 3 — Note checkpoint 🔴** (NE PAS appliquer ici) : Florian applique via `npm run db:push`,
  coche [MIGRATIONS.md](../../supabase/MIGRATIONS.md), puis `npm run db:types` (nouvelle table
  `audit_log` dans les types). Aucune sync rule (hors PowerSync).

---

## Task 3 : couche data admin — `data/audit.ts`

**Files:** Create `apps/admin/src/data/audit.ts`.

```ts
import { supabase } from '../lib/supabase';
import { auditEntrySchema, type AuditEntryInput, type Database } from '@wellness/shared';

export type AuditLogRow = Database['public']['Tables']['audit_log']['Row'];

/**
 * Journalise une action admin. BEST-EFFORT : ne lève jamais, ne bloque pas l'action métier.
 * Capte l'acteur depuis la session (clé anon). Un échec (validation/session/réseau/RLS) → warn + { error }.
 */
export async function logAudit(entry: AuditEntryInput): Promise<{ error: unknown }> {
  const parsed = auditEntrySchema.safeParse(entry);
  if (!parsed.success) {
    console.warn('[audit] entrée invalide, non journalisée', parsed.error);
    return { error: parsed.error };
  }
  const { data: userData } = await supabase.auth.getUser();
  const actor = userData?.user;
  const { error } = await supabase.from('audit_log').insert({
    actor_id: actor?.id ?? null,
    actor_email: actor?.email ?? null,
    action: parsed.data.action,
    target_table: parsed.data.targetTable ?? null,
    target_id: parsed.data.targetId ?? null,
    target_label: parsed.data.targetLabel ?? null,
    details: parsed.data.details ?? {},
  });
  if (error) console.warn('[audit] échec insert, action non tracée', error);
  return { error };
}

export type AuditFilters = {
  actorId?: string; action?: string; from?: string; to?: string; limit?: number; before?: string;
};

/** Liste paginée (curseur created_at desc). super_admin only (RLS). Tolérant aux erreurs. */
export async function listAudit(f: AuditFilters = {}): Promise<{ rows: AuditLogRow[]; error: unknown }> {
  let q = supabase.from('audit_log').select('*').order('created_at', { ascending: false });
  if (f.actorId) q = q.eq('actor_id', f.actorId);
  if (f.action) q = q.eq('action', f.action);
  if (f.from) q = q.gte('created_at', f.from);
  if (f.to) q = q.lte('created_at', f.to);
  if (f.before) q = q.lt('created_at', f.before);
  q = q.limit(f.limit ?? 50);
  const { data, error } = await q;
  if (error) return { rows: [], error };
  return { rows: data ?? [], error: null };
}
```

- [ ] **Step 1** — Écrire `data/audit.ts` (ci-dessus).
- [ ] **Step 2 — Commit** : `git commit -m "feat(admin): couche data audit (logAudit best-effort + listAudit paginé)"`.

> Typage : `Database['...']['audit_log']` requiert `db:types` (Task 2 appliquée). Tant que le
> checkpoint n'est pas fait, le typecheck admin de cette table sera KO — le noter dans le rapport
> final (typecheck vert **après** `db:types`).

---

## Task 4 : instrumenter `data/roles.ts`

**Files:** Modify `apps/admin/src/data/roles.ts` (+ `RolesScreen.tsx` pour le libellé de revoke).

- [ ] **Step 1** — `grantRole` : ajouter `.select('id').single()` à l'**insert** (cas 3) et réutiliser
  `revoked.data.id` (cas 2, réactivation) pour disposer de l'id d'attribution. Étendre le retour en
  `{ error, alreadyActive?, id?: string }` (**additif** — les appelants actuels ignorent `id`).
  ⚠️ `grantRole` a des `return` anticipés par branche (cas 1 no-op, cas 2 réactivation, cas 3 insert) :
  le `logAudit` (Step 2) doit être placé **dans chaque branche écrivante** (cas 2 et 3), avec l'id de
  cette branche — **pas** en un point de sortie unique en fin de fonction.
- [ ] **Step 2** — Après succès **et** si ce n'est **pas** un no-op (`!alreadyActive`), appeler :

```ts
await logAudit({
  action: 'role.grant', targetTable: 'user_roles', targetId: grantedId ?? null,
  targetLabel: `${role} → ${userId}`, details: { role, targetUserId: userId },
});
```

- [ ] **Step 3** — `revokeRole(id, opts?: { role?: AdminRole; userId?: string })` : param additif ;
  après succès :

```ts
await logAudit({
  action: 'role.revoke', targetTable: 'user_roles', targetId: id,
  targetLabel: opts?.role && opts?.userId ? `${opts.role} → ${opts.userId}` : null,
  details: opts ? { role: opts.role, targetUserId: opts.userId } : {},
});
```

- [ ] **Step 4** — `RolesScreen.tsx` : au handler de révocation, passer `{ role, userId }` de la ligne
  déjà affichée à `revokeRole(row.id, { role: row.role, userId: row.user_id })`.
- [ ] **Step 5 — Commit** : `git commit -m "feat(admin): audit des attributions/révocations de rôle"`.

---

## Task 5 : instrumenter `data/exercises.ts`

**Files:** Modify `apps/admin/src/data/exercises.ts` (+ `ExercisesScreen.tsx` / écran d'édition pour les libellés).

Mapping :

| Fonction | Action | target_id | target_label | details |
|---|---|---|---|---|
| `saveExercise(input)` | `input.id` ? `exercise.update` : `exercise.create` | `id` (retour) | `input.nameFr` | — |
| `setStatus(id, status, opts?)` | `status === 'published'` ? `exercise.publish` (sinon **pas de log** pour dépublication, hors périmètre) | `id` | `opts?.label ?? null` | `{ status }` |
| `archiveExercise(id, opts?)` | `exercise.archive` | `id` | `opts?.label ?? null` | — |

- [ ] **Step 1** — Instrumenter `saveExercise` après succès (`error == null`) : action selon `input.id`,
  `targetTable: 'exercises'`, `targetId: id`, `targetLabel: input.nameFr`.
- [ ] **Step 2** — `setStatus(id, status, opts?: { label?: string })` (param additif) : logguer
  `exercise.publish` **uniquement** au passage à `'published'`.
- [ ] **Step 3** — `archiveExercise(id, opts?: { label?: string })` (param additif) : logguer `exercise.archive`.
- [ ] **Step 4** — `ExercisesScreen.tsx` (et l'écran d'édition si publish/archive y est déclenché) :
  passer `{ label: row.nameFr }` aux appels `setStatus`/`archiveExercise`.
- [ ] **Step 5 — Commit** : `git commit -m "feat(admin): audit des exercices (create/update/publish/archive)"`.

---

## Task 6 : instrumenter `data/programs.ts`

**Files:** Modify `apps/admin/src/data/programs.ts` (+ `ProgramsScreen.tsx` / écrans d'édition).

| Fonction | Action | target_id | target_label | details |
|---|---|---|---|---|
| `createEditorialProgram(input)` | `program.create` | `id` (retour) | `input.nameFr` | — |
| `updateProgramMeta(id, input)` | `program.update` | `id` | `input.nameFr` | — |
| `setStatus(id, status, opts?)` | `program.publish` (au passage à `'published'`) | `id` | `opts?.label ?? null` | `{ status }` |
| `archiveProgram(id, opts?)` | `program.archive` | `id` | `opts?.label ?? null` | — |

- [ ] **Step 1** — Instrumenter les 4 fonctions après succès (mêmes règles que Task 5 ; params
  additifs `opts?: { label?: string }` sur `setStatus`/`archiveProgram`). Les sous-mutations
  (`addSession`/`updateSession`/…/`*ExercisePlan`) **ne sont pas** tracées (spec §2/§7).
- [ ] **Step 2** — Écrans appelants : passer `{ label: program.nameFr }` à `setStatus`/`archiveProgram`.
- [ ] **Step 3 — Commit** : `git commit -m "feat(admin): audit des programmes (create/update/publish/archive)"`.

---

## Task 7 : instrumenter `data/foods.ts`

**Files:** Modify `apps/admin/src/data/foods.ts` (+ `FoodsScreen.tsx` pour l'archivage).

| Fonction | Action | target_id | target_label | details |
|---|---|---|---|---|
| `saveFood(input)` | `input.id` ? `food.update` : `food.create` | `id` (retour) | `input.nameFr` | — |
| `archiveFood(id, opts?)` | `food.archive` | `id` | `opts?.label ?? null` | — |
| `importFoods(records)` | `food.import` | `null` | `null` | `{ count: created + updated, created, updated }` |

- [ ] **Step 1** — `saveFood` : après succès, action selon `input.id`, `targetLabel: input.nameFr`.
- [ ] **Step 2** — `archiveFood(id, opts?: { label?: string })` (param additif) : logguer `food.archive`.
- [ ] **Step 3** — `importFoods` : **lève** en cas d'erreur → placer le `logAudit` **après** les
  upserts réussis, avant le `return { created, updated }`. Une **seule** entrée `food.import` avec
  `details: { count: created + updated, created, updated }`, `targetTable: 'foods'`, `targetId: null`.
  ⚠️ Si `importFoods` comporte un **retour anticipé** sur `records.length === 0`, y placer **aussi**
  le `logAudit` (entrée `count: 0`, spec §10) — ou, plus simple, ne pas court-circuiter et laisser le
  chemin normal produire `{ created: 0, updated: 0 }` + une entrée. En pratique l'écran désactive
  « Importer » quand `valid.length === 0`, donc `count: 0` reste théorique, mais l'entrée doit exister
  si la fonction est appelée avec une liste vide.
- [ ] **Step 4** — `FoodsScreen.tsx` : passer `{ label: row.nameFr }` à `archiveFood`.
- [ ] **Step 5 — Commit** : `git commit -m "feat(admin): audit des aliments (create/update/archive/import)"`.

---

## Task 8 : écran de consultation `/audit`

**Files:** Create `apps/admin/src/screens/AuditScreen.tsx` ; Modify `App.tsx`, `AdminLayout.tsx`, `i18n/fr.ts`.

- [ ] **Step 1** — `i18n/fr.ts` : ajouter la section `audit` (premier niveau) : `title`, `nav`, colonnes
  (`date`, `actor`, `action`, `target`), filtres (`filterActor`, `filterAction`, `filterFrom`,
  `filterTo`, `reset`), `loadMore`, `loading`, `empty` (« Aucune action enregistrée »), `error`, et
  le **sous-objet `action`** mappant **chaque** valeur de `AUDIT_ACTIONS` à un libellé FR (ex.
  `'program.publish': 'Publication de programme'`). Rendu : `fr.audit.action[row.action]` avec repli
  sur `row.action` si clé inconnue.
- [ ] **Step 2** — `AuditScreen.tsx` (pattern `RolesScreen` : `useEffect` + `listAudit`, états
  `useState`, styles dérivés du thème, chaînes via `fr.audit.*`) :
  - state : `rows`, `loading`, `error`, filtres (`actorId`, `action`, `from`, `to`), pagination
    (accumulation + `before` = `created_at` de la dernière ligne).
  - `reload()` appelle `listAudit({ actorId, action, from, to })` ; « Charger plus » rappelle avec
    `before` et **concatène**.
  - Filtres : select acteur (valeurs distinctes présentes dans `rows` : `actor_email ?? actor_id`),
    select action (`AUDIT_ACTIONS`), deux `<input type="date">` (début/fin), bouton « Réinitialiser ».
  - Table : date+heure **`JJ/MM/AAAA HH:MM`** (helper de formatage local), acteur
    (`actor_email ?? actor_id`), action (`fr.audit.action[...]`), cible (`target_label ??
    `${target_table ?? '—'} ${target_id ?? ''}``). États chargement / vide / erreur.
- [ ] **Step 3** — `App.tsx` : ajouter
  `<Route path="/audit" element={<RequireSuperAdmin><AuditScreen /></RequireSuperAdmin>} />`
  (importer `AuditScreen`), sur le modèle de `/roles`.
- [ ] **Step 4** — `AdminLayout.tsx` : ajouter, sous condition `isSuperAdmin`, un
  `NavLink to="/audit"` (📜 `fr.audit.nav`), sur le modèle du lien `/roles`.
- [ ] **Step 5 — Commit** : `git commit -m "feat(admin): écran Journal d'audit (/audit) + route + nav super_admin + i18n"`.

---

## Task 9 : vérification d'ensemble

- [ ] **Step 1** — `npm run test --workspace @wellness/shared` (dont `audit`) : vert.
- [ ] **Step 2** — `npm run typecheck` (tous). ⚠️ `data/audit.ts` dépend du type `audit_log` : vert
  **après** le checkpoint `db:types` (Task 2). Le noter dans le rapport si non encore appliqué.
- [ ] **Step 3** — `npm run lint` : 0 erreur. `npm run build --workspace @wellness/admin` : OK.
- [ ] **Step 4** — Relire le diff : `logAudit` **jamais bloquant** (aucun `await logAudit` ne modifie
  le retour d'une mutation ni ne peut la faire échouer) ; aucune chaîne en dur non i18n côté admin ;
  RLS non contournée ; params d'audit bien **additifs** (appelants existants intacts) ; parité des
  libellés `audit.action.*` avec `AUDIT_ACTIONS` (chaque action a son libellé).

---

## Notes

- **Gate CLAUDE.md** : spec validée ; ce plan à valider (maquette a priori **écartée** — écran liste+
  filtres, précédents 8.5/8.2 ; confirmer à la validation). Code autorisé après validation.
- **Écart vs spec §7** signalé : paramètre de libellé optionnel sur `setStatus`/`archive*`/`revokeRole`
  (le nom n'est pas « en main dans la couche » pour ces fonctions) — additif, retour inchangé.
- **Checkpoint 🔴 Florian** (à l'impl terminée) : `npm run db:push` (migration `audit_log`) → cocher
  [MIGRATIONS.md](../../supabase/MIGRATIONS.md) → `npm run db:types` → recette web : déclencher une
  action de **chaque** type (grant/revoke, exo create/update/publish/archive, programme idem, aliment
  create/update/archive, import CSV) → vérifier les entrées dans `/audit` (acteur, action, cible,
  filtres, pagination) ; tenter un `update`/`delete` d'une entrée (SQL) → refus (trigger). Aucune
  sync rule à redéployer.
- **YAGNI** : pas de diff avant/après, pas d'export, pas de purge/rétention, pas d'audit des lectures
  ni des sous-éditions de programme, pas de dépublication tracée (`exercise`/`program` : seul le
  passage à `published` est loggé).
