# US 9.10 — Analytics produit first-party — Plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development (recommandé) ou
> superpowers:executing-plans. Steps en cases `- [ ]`.
> ⚠️ **Workflow projet** : ne PAS exécuter avant validation des 3 livrables (spec ✅ + plan + maquette).
> ⚠️ **Migration cloud** (`db:push`) + **sync rules PowerSync** = étapes de déploiement **contrôlées**
> (Task 6), pas lancées par un subagent à l'aveugle.

**Goal :** capter des événements d'usage anonymisés dans une table `analytics_events` (notre base Supabase),
offline-first via PowerSync, avec consentement opt-out et garde-fou anti-PII.

**Architecture :** migration (table append-only + RLS + colonne consentement) → schéma client PowerSync + type
partagé + mapping repository → service `track()` (helpers **purs testés** `sanitizeProps`/`buildEventRow` + I/O
non bloquant) → réglage opt-out + i18n → instrumentation ~15 points → déploiement + clôture.

**Tech stack :** Supabase (Postgres + RLS), PowerSync (`@powersync/react-native` schema + insert brut),
TypeScript, Zod (`@wellness/shared`), jest-expo (tests), `expo-application` + `Platform` (déjà installés via 1.22).

**Spec :** [docs/specs/functional/us/9.10-analytics-produit.md](../specs/functional/us/9.10-analytics-produit.md)

---

## Décisions d'implémentation (dérivées du contexte code)

- **`properties` stocké en `text` (JSON), pas `jsonb`.** PowerSync réplique une colonne `text` locale ; envoyée
  vers un `jsonb` Postgres, elle atterrit comme **chaîne JSON** (gotcha connu du bug piliers 8.8). Pour rester
  interrogeable en BI (`properties::json->>'pillar'`), la colonne Postgres est **`text`**. (Raffinement de la
  spec §2.1 qui disait « jsonb » — même intention, représentation honnête vis-à-vis de PowerSync.)
- **Insert dédié append-only** : ne PAS utiliser `insertWithSyncFields`/`txInsert`/`patch` (`_sql.ts`) — ils
  injectent `updated_at`/`deleted_at`, absents de la table. Un `powerSync.execute('INSERT …')` dédié.
- **`analytics_enabled` en `integer` (0/1)** côté PowerSync (convention booléens du schéma), `boolean` côté
  Postgres, `boolean` côté domaine. Mapping `null → true` (défaut opt-out ON, robuste aux lignes anciennes).
- **`track()` reçoit les valeurs impures injectées** (`id`, `occurredAt`, `appVersion`, `platform`) → `buildEventRow`
  reste **pur et déterministe** (testable).

## Structure des fichiers

**Créer :**
- `supabase/migrations/<horodaté>_analytics_events.sql` — table + RLS + colonne consentement.
- `apps/mobile/src/lib/analytics.ts` (+ `__tests__/analytics.test.ts`) — `sanitizeProps`, `buildEventRow` (purs,
  testés), `track()` (I/O non bloquant).
- `apps/mobile/src/data/repositories/analytics-repository.ts` — `insertAnalyticsEvent(row)` (insert brut).

**Modifier :**
- `apps/mobile/src/powersync/schema.ts` — table `analytics_events` + colonne `analytics_enabled` sur `user_settings`.
- `packages/shared/src/settings.ts` — `analyticsEnabled: z.boolean().default(true)`.
- `apps/mobile/src/data/repositories/settings-repository.ts` — mapping `analyticsEnabled` + accesseur `getAnalyticsEnabled()`.
- `apps/mobile/src/app/settings.tsx` — réglage « Statistiques d'usage ».
- `apps/mobile/src/app/(auth)/privacy.tsx` (ou clés `legal.privacy.*`) — mention analytics.
- `apps/mobile/src/i18n/locales/{fr,en}.json` — clés `settings.analytics.*` (+ mention confidentialité).
- **Instrumentation** : stores/écrans des ~15 points (Task 5).
- `supabase/MIGRATIONS.md`, `packages/shared/src/database.types.ts` (régénéré) — Task 6.

---

## Task 1 : Migration (fichier SQL, sans push)

**Files:** `supabase/migrations/<horodaté>_analytics_events.sql`

- [ ] **Step 1 : créer le fichier** : `npm run db:new analytics_events` (génère l'horodaté dans `supabase/migrations/`).
- [ ] **Step 2 : écrire le SQL** :

```sql
-- US 9.10 — analytics_events : événements d'usage anonymisés (first-party). Append-only.
create table public.analytics_events (
  id          uuid primary key,
  user_id     uuid not null references auth.users (id) on delete cascade,
  event_name  text not null,
  properties  text not null default '{}',   -- JSON sérialisé (text : cohérent PowerSync)
  app_version text,
  platform    text,
  occurred_at timestamptz not null,
  created_at  timestamptz not null default now()
);
create index analytics_events_user_id_idx on public.analytics_events (user_id);

alter table public.analytics_events enable row level security;
create policy "analytics_events_insert_own" on public.analytics_events
  for insert with check (auth.uid() = user_id);
create policy "analytics_events_select_own" on public.analytics_events
  for select using (auth.uid() = user_id);

-- Consentement analytics (opt-out : activé par défaut).
alter table public.user_settings add column analytics_enabled boolean not null default true;
```

- [ ] **Step 3 : NE PAS pousser** (déploiement contrôlé en Task 6). Vérifier seulement la présence du fichier.
- [ ] **Step 4 : commit** `feat(9.10): migration analytics_events + colonne consentement (SQL, non poussée)`

> ⚠️ Aucun `db:push` ici. La migration est appliquée sur le cloud en Task 6, sous contrôle.

---

## Task 2 : Schéma client PowerSync + type partagé + mapping repository

**Files:** `apps/mobile/src/powersync/schema.ts`, `packages/shared/src/settings.ts`, `apps/mobile/src/data/repositories/settings-repository.ts`

- [ ] **Step 1 : `schema.ts`** — ajouter la table (près des autres) et l'enregistrer dans `AppSchema` :

```ts
// ── US 9.10 : analytics produit (append-only, first-party) ─────────────────
const analytics_events = new Table({
  user_id: column.text,
  event_name: column.text,
  properties: column.text, // JSON sérialisé
  app_version: column.text,
  platform: column.text,
  occurred_at: column.text,
  created_at: column.text,
});
```
Ajouter `analytics_events,` dans `new Schema({ … })`. Et ajouter à la table `user_settings` la colonne :
```ts
  analytics_enabled: column.integer, // 0/1 — consentement analytics (US 9.10)
```

- [ ] **Step 2 : `packages/shared/src/settings.ts`** — étendre `userSettingsRowSchema` :
```ts
  /** Consentement analytics (opt-out : true par défaut). US 9.10. */
  analyticsEnabled: z.boolean().default(true),
```
- [ ] **Step 3 : lancer** `npm run test -w @wellness/shared` (schéma settings) + `npm run typecheck`.
- [ ] **Step 4 : `settings-repository.ts`** — mapping :
  - Type `SettingsDbRow` : ajouter `analytics_enabled: number | null;`.
  - `rowToSettings` : `analyticsEnabled: row.analytics_enabled == null ? true : row.analytics_enabled === 1,`.
  - `SettingsInput` : ajouter `'analyticsEnabled'` au `Pick`.
  - `inputToColumns` : `if ('analyticsEnabled' in input) columns['analytics_enabled'] = input.analyticsEnabled ? 1 : 0;`.
  - **Accesseur hors React** (pour `track()`), à exporter :
    ```ts
    /** Consentement analytics courant (hors contexte React). Défaut opt-out ON si non défini. */
    export async function getAnalyticsEnabled(): Promise<boolean> {
      const row = await getCurrentRow();
      return row?.analytics_enabled == null ? true : row.analytics_enabled === 1;
    }
    ```
    (Nécessite d'ajouter `analytics_enabled: number | null` au type `SettingsDbRow` renvoyé par `getCurrentRow`.)
  - Les **blocs de defaults** des chemins d'insert (`updateSettings`/`togglePillar`/`ensureSettings`) : **ne pas**
    y ajouter `analytics_enabled` (colonne absente localement → `null` → mappé `true` ; à la synchro, défaut
    Postgres `true`). Comportement opt-out ON préservé. ⚠️ Le **patch** de `updateSettings`, lui, inclut déjà
    `...columns` : si l'utilisateur bascule le réglage alors qu'aucune ligne n'existe encore, `analytics_enabled`
    sera correctement inséré via `inputToColumns` — c'est le comportement souhaité, ne pas l'empêcher.
- [ ] **Step 5 : lancer** `npm run typecheck` + `npm run lint` + `npm run test -w @wellness/mobile`.
- [ ] **Step 6 : commit** `feat(9.10): schéma PowerSync analytics_events + consentement (type + mapping)`

---

## Task 3 : Service `track()` + helpers purs + repository insert

**Files:** `apps/mobile/src/lib/analytics.ts`, `apps/mobile/src/lib/__tests__/analytics.test.ts`, `apps/mobile/src/data/repositories/analytics-repository.ts`

- [ ] **Step 1 : test qui échoue** (`analytics.test.ts`) — helpers purs + gating :

```ts
import { sanitizeProps, buildEventRow } from '../analytics';

describe('sanitizeProps', () => {
  it('ne garde que les clés autorisées et scalaires', () => {
    expect(sanitizeProps({ pillar: 'strength', weight: 82, note: 'secret', nested: { a: 1 } }))
      .toEqual({ pillar: 'strength' }); // weight/note/nested écartés (hors allowlist)
  });
  it('sans props → objet vide', () => {
    expect(sanitizeProps(undefined)).toEqual({});
  });
});

describe('buildEventRow', () => {
  it('assemble une ligne déterministe (properties sérialisé, PII écartée)', () => {
    const row = buildEventRow({
      id: 'e1', userId: 'u1', eventName: 'workout_completed',
      props: { pillar: 'strength', weightKg: 90 },
      appVersion: '1.0.0', platform: 'android', occurredAt: '2026-07-24T10:00:00.000Z',
    });
    expect(row).toEqual({
      id: 'e1', user_id: 'u1', event_name: 'workout_completed',
      properties: '{"pillar":"strength"}',
      app_version: '1.0.0', platform: 'android',
      occurred_at: '2026-07-24T10:00:00.000Z', created_at: '2026-07-24T10:00:00.000Z',
    });
  });
});
```

- [ ] **Step 2 : lancer → échec** `npm run test -w @wellness/mobile -- analytics`.
- [ ] **Step 3 : implémenter le pur** (`analytics.ts`) :

```ts
import { Platform } from 'react-native';
import * as Application from 'expo-application';
import { generateId } from '@/lib/id';
import { useAuthStore } from '@/stores/auth-store';
import { getAnalyticsEnabled } from '@/data/repositories/settings-repository';
import { insertAnalyticsEvent } from '@/data/repositories/analytics-repository';

/** Clés de propriétés autorisées (allowlist stricte anti-PII). Extensible au fil des événements. */
export const ALLOWED_PROP_KEYS = ['pillar'] as const;

export type AnalyticsEventRow = {
  id: string; user_id: string; event_name: string; properties: string;
  app_version: string | null; platform: string; occurred_at: string; created_at: string;
};

/** Ne conserve que les clés autorisées et les valeurs scalaires. PUR. */
export function sanitizeProps(props?: Record<string, unknown>): Record<string, string | number | boolean> {
  const out: Record<string, string | number | boolean> = {};
  if (!props) return out;
  for (const key of ALLOWED_PROP_KEYS) {
    const v = props[key];
    if (typeof v === 'string' || typeof v === 'number' || typeof v === 'boolean') out[key] = v;
  }
  return out;
}

/** Assemble la ligne d'événement. PUR (valeurs impures injectées → déterministe/testable). */
export function buildEventRow(input: {
  id: string; userId: string; eventName: string; props?: Record<string, unknown>;
  appVersion: string | null; platform: string; occurredAt: string;
}): AnalyticsEventRow {
  return {
    id: input.id,
    user_id: input.userId,
    event_name: input.eventName,
    properties: JSON.stringify(sanitizeProps(input.props)),
    app_version: input.appVersion,
    platform: input.platform,
    occurred_at: input.occurredAt,
    created_at: input.occurredAt,
  };
}

/**
 * Capte un événement d'usage. Respecte le consentement (no-op si OFF / pas de session),
 * assainit les propriétés (anti-PII), écrit offline-first (PowerSync). NE JETTE JAMAIS.
 */
export async function track(eventName: string, props?: Record<string, unknown>): Promise<void> {
  try {
    const userId = useAuthStore.getState().session?.user.id;
    if (!userId) return;
    if (!(await getAnalyticsEnabled())) return;
    const row = buildEventRow({
      id: generateId(), userId, eventName, props,
      appVersion: Application.nativeApplicationVersion ?? null,
      platform: Platform.OS,
      occurredAt: new Date().toISOString(),
    });
    await insertAnalyticsEvent(row);
  } catch (err) {
    console.warn('[analytics] track échec:', err);
  }
}
```

- [ ] **Step 4 : repository** (`analytics-repository.ts`) — insert brut append-only (PAS `insertWithSyncFields`) :

```ts
import { powerSync } from '@/powersync/system';
import type { AnalyticsEventRow } from '@/lib/analytics';

/** Insert append-only dans la table locale analytics_events (PowerSync synchronise). */
export async function insertAnalyticsEvent(row: AnalyticsEventRow): Promise<void> {
  const columns = Object.keys(row);
  const placeholders = columns.map(() => '?').join(', ');
  await powerSync.execute(
    `INSERT INTO analytics_events (${columns.join(', ')}) VALUES (${placeholders})`,
    columns.map((c) => (row as Record<string, unknown>)[c]),
  );
}
```
> Note import circulaire : `analytics.ts` importe le type depuis lui-même exporté et la repo importe le type
> depuis `analytics.ts`. Si le typecheck signale un cycle gênant, déplacer `AnalyticsEventRow` dans la repo (ou
> un petit fichier de types) et l'importer des deux côtés. À trancher à l'implémentation selon le typecheck.

- [ ] **Step 5 : lancer → succès** `npm run test -w @wellness/mobile -- analytics` + `npm run typecheck` + `npm run lint`.
- [ ] **Step 6 : commit** `feat(9.10): service track() + helpers purs (allowlist anti-PII) + insert append-only`

---

## Task 4 : Réglage opt-out + mention confidentialité + i18n

**Files:** `apps/mobile/src/app/settings.tsx`, `apps/mobile/src/app/(auth)/privacy.tsx` (ou i18n `legal.privacy`), `apps/mobile/src/i18n/locales/{fr,en}.json`

- [ ] **Step 1 : réglage** dans `settings.tsx` — section « Statistiques d'usage » (près de « Aide & support » /
  « Données »), une carte avec `Switch` piloté par `settings.analyticsEnabled` :
```tsx
<Text style={[styles.sectionTitle, { color: colors.textMuted, marginTop: 28 }]}>
  {t('settings.analytics.title')}
</Text>
<View style={[styles.card, { backgroundColor: colors.surface, borderColor: colors.border }]}>
  <View style={styles.row}>
    <View style={styles.rowGrow}>
      <Text style={[styles.rowLabel, { color: colors.text }]}>{t('settings.analytics.toggle')}</Text>
      <Text style={[styles.rowDesc, { color: colors.textMuted }]}>{t('settings.analytics.subtitle')}</Text>
    </View>
    <Switch
      value={settings?.analyticsEnabled ?? true}
      onValueChange={(next) => void updateSettings({ analyticsEnabled: next })}
      trackColor={{ true: colors.accent, false: colors.border }}
      thumbColor="#ffffff"
      accessibilityLabel={t('settings.analytics.toggle')}
    />
  </View>
</View>
```
- [ ] **Step 2 : mention confidentialité** — ajouter un court paragraphe dans le contenu `privacy` (suivre la
  structure existante : `LegalScreen` lit `legal.privacy.body` en `returnObjects` → ajouter une entrée FR+EN).
- [ ] **Step 3 : i18n FR** : `settings.analytics = { title: "Statistiques d'usage", toggle: "Autoriser les statistiques d'usage", subtitle: "Nous aide à améliorer l'app. Données anonymes, jamais tes données de santé." }`.
- [ ] **Step 4 : i18n EN** (parité) : `{ title: "Usage statistics", toggle: "Allow usage statistics", subtitle: "Helps us improve the app. Anonymous data, never your health data." }`. Traduire aussi le paragraphe confidentialité.
- [ ] **Step 5 : lancer** `npm run typecheck` + `npm run lint` + `npm run test -w @wellness/mobile` ; JSON valides + parité.
- [ ] **Step 6 : commit** `feat(9.10): réglage opt-out « Statistiques d'usage » + mention confidentialité + i18n`

---

## Task 5 : Instrumentation des ~15 points

**Files:** stores/écrans concernés (appels `track(...)`)

- [ ] **Step 1 : instrumenter** (appeler `track(nom, props?)` au bon endroit, sans dupliquer, sans logique métier) :
  - **Cycle de vie** : `app_opened` — au passage au premier plan (`AppState` 'active'), **throttlé ≥ 30 min**
    (garder un timestamp module ; ne pas ré-émettre si < 30 min). Point d'ancrage : `_layout.tsx` racine ou un
    hook dédié `useAppOpenedAnalytics`.
  - **Onboarding** : `onboarding_started` (entrée onboarding), `onboarding_completed` (fin/`onboardingCompletedAt`),
    `onboarding_skipped` (bouton « Passer »).
  - **Piliers** : `pillar_activated` `{ pillar }` dans `togglePillar` **uniquement à l'activation** (ajout).
  - **Muscu** : `workout_started` (démarrage séance), `workout_completed` (`finishWorkout`).
  - **Course** : `run_started` (démarrage), `run_completed` (fin de course enregistrée).
  - **Nutrition** : `food_logged` (ajout d'une entrée de repas).
  - **Adoption** : `stats_viewed` `{ pillar }` (ouverture écrans stats muscu/course/nutrition),
    `dashboard_customized` (entrée mode édition dashboard), `data_exported` (export CONF-01 réussi),
    `help_opened` (ouverture `/help`), `bug_reported` (action « Signaler un bug »).
- [ ] **Step 2 : vérifier** que chaque appel est bien **hors du chemin critique** (fire-and-forget `void track(...)`),
  n'ajoute pas de latence et ne bloque rien. Ne PAS instrumenter au-delà de cette liste (YAGNI).
- [ ] **Step 3 : lancer** `npm run typecheck` + `npm run lint` + `npm run test -w @wellness/mobile`.
- [ ] **Step 4 : commit** `feat(9.10): instrumentation analytics (socle + adoption, ~15 points)`

> Chaque point d'ancrage est vérifié contre le code réel au moment de l'implémentation (noms de fonctions/écrans
> exacts). Si un point est ambigu (ex. où capter `food_logged`), l'implémenteur remonte la question.

---

## Task 6 : Déploiement (migration cloud + sync rules) + suite complète + clôture

- [ ] **Step 1 : prévisualiser** `npm run db:push:dry` (liste la migration qui partira).
- [ ] **Step 2 : pousser** `npm run db:push` (applique sur le cloud) — **étape contrôlée**.
- [ ] **Step 3 : régénérer les types** `npm run db:types` (met à jour `packages/shared/src/database.types.ts`).
- [ ] **Step 4 : sync rules PowerSync** — ajouter le bucket `analytics_events` (par `user_id`) dans la config de
  l'**instance PowerSync** (dashboard/déploiement). Documenter l'action. ⚠️ Sans ça, les événements ne montent pas.
- [ ] **Step 5 : cocher** la migration dans `supabase/MIGRATIONS.md` (case + date).
- [ ] **Step 6 : suite complète** `npm run typecheck` + `npm run lint` + `npm run test` (shared + mobile) verts.
- [ ] **Step 7 : clôture** — `TODO.md` (livré → recette), `CHANGELOG.md`, statut roadmap **9.10 → ✅**
  (compteurs V0.8), push via `/commit`.

---

## Notes de test

- **Purs** (`sanitizeProps`, `buildEventRow`) : Jest — allowlist (PII écartée), sérialisation déterministe.
- **`track()`** : gating (no-op si consentement OFF ou pas de session) vérifiable en mockant `getAnalyticsEnabled`
  et `useAuthStore` + en asseyant que `insertAnalyticsEvent` n'est pas appelé ; chemin nominal → `insertAnalyticsEvent`
  appelé avec la ligne attendue. Non bloquant (un throw d'insert ne remonte pas).
- **Recette** (spec §9) : événement écrit (Supabase), opt-out respecté, anti-PII, offline, purge compte, i18n.

## Points d'attention

- **Migration cloud & sync rules = étapes contrôlées** (Task 6), jamais lancées à l'aveugle par un subagent.
- **`properties` = text JSON** (pas jsonb) — cohérent PowerSync ; BI parse `properties::json`.
- **Append-only** : insert dédié, jamais `insertWithSyncFields` (pas d'`updated_at`/`deleted_at`).
- **Non bloquant** : `track()` ne doit jamais casser un parcours (try/catch, fire-and-forget).
- **Ordre** : migration (1) → schéma/type/mapping (2) → service (3) → réglage/i18n (4) → instrumentation (5) →
  déploiement + clôture (6).
