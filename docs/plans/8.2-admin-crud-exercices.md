# Plan d'implémentation — Admin CRUD exercices éditoriaux (8.2)

> Exécuter avec `superpowers:subagent-driven-development`. Spec validée :
> [docs/specs/functional/us/8.2-admin-crud-exercices.md](../specs/functional/us/8.2-admin-crud-exercices.md).
> Branche : `feature/admin-8.2-exercices-crud`. **🔴 checkpoint cloud** : migration + sync rules + `db:types` (Florian).

**Objectif :** gérer les exercices éditoriaux depuis l'admin (liste, créer/éditer bilingue, brouillon/publié, archiver).

**Architecture :** migration (status + RLS admin) + sync rules (masquer brouillons) → `database.types` étendu →
couche data `exercises.ts` (supabase-js) → écrans liste + formulaire → nav/routing. Mobile inchangé.

## Maquette
**Aucune** (validé) : écrans admin (tableau + formulaire) sur le pattern F1/F2.

## Fichiers touchés
- Créer : `supabase/migrations/20260713110000_admin_editorial_exercises.sql`.
- Modifier : `packages/shared/src/database.types.ts` (ajout `status` à `exercises`), `docs/specs/technical/powersync-sync-rules.yaml`.
- Créer : `apps/admin/src/data/exercises.ts`, `apps/admin/src/screens/{ExercisesScreen.tsx, ExerciseEditScreen.tsx}`.
- Modifier : `apps/admin/src/App.tsx` (routes), `apps/admin/src/components/AdminLayout.tsx` (nav Exercices cliquable), `apps/admin/src/i18n/fr.ts`.

---

### Tâche 1 — Migration : status + RLS éditoriale (SQL) + types

**Fichier :** `supabase/migrations/20260713110000_admin_editorial_exercises.sql` (+ `database.types.ts`).

- [ ] **Colonne** : `alter table public.exercises add column status text not null default 'published'
  check (status in ('draft','published'));`
- [ ] **RLS `exercises`** — ⚠️ Postgres n'a PAS `create or replace policy` → **DROP puis CREATE** (noms exacts
  générés par `20260706120001_socle_muscu_rls.sql` : `exercises_select`/`exercises_insert`/`exercises_update`) :
  ```sql
  drop policy if exists exercises_select on public.exercises;
  create policy exercises_select on public.exercises for select
    using (owner_id = auth.uid() or (owner_id is null and status = 'published') or public.is_admin());
  drop policy if exists exercises_insert on public.exercises;
  create policy exercises_insert on public.exercises for insert
    with check (owner_id = auth.uid() or public.is_admin());
  drop policy if exists exercises_update on public.exercises;
  create policy exercises_update on public.exercises for update
    using (owner_id = auth.uid() or public.is_admin())
    with check (owner_id = auth.uid() or public.is_admin());
  ```
- [ ] **RLS `exercise_translations`** — DROP+CREATE `exercise_translations_insert`/`exercise_translations_update`
  avec `(owner_id = auth.uid() or public.is_admin())`. (Laisser `exercise_translations_select` inchangée.)
- [ ] **`database.types.ts`** : ajouter `status: string` à `exercises` Row, `status?: string` à Insert/Update
  (manuel ; idempotent avec un futur `db:types`).
- [ ] typecheck racine vert.
- [ ] **Commit** : `feat(admin): migration status exercices + RLS écriture éditoriale (is_admin)`.

### Tâche 2 — Sync rules : masquer les brouillons au mobile (🔴)

**Fichier :** `docs/specs/technical/powersync-sync-rules.yaml`.

- [ ] Bucket `shared_content`, ligne `exercises` : passer de
  `select * from exercises where owner_id is null and deleted_at is null`
  à `... and status = 'published'` (même pattern que `programs` déjà présent).
- [ ] Commentaire : `exercise_translations` n'a pas besoin de filtre `status` (filtrées de facto — le parent
  brouillon n'est pas synchronisé).
- [ ] **Commit** : `chore(powersync): filtre status=published des exercices éditoriaux (sync rules)`.

### Tâche 3 — Couche data admin (`apps/admin/src/data/exercises.ts`)

- [ ] `AdminExerciseRow` (row + noms FR/EN dérivés) ; `listEditorialExercises()` (exercices `owner_id is null`,
  `deleted_at is null`, join `exercise_translations` fr/en, tri récent→ancien) ; `getExercise(id)` (row + 2 trads).
- [ ] `saveExercise(input)` : create (UUID client) ou update — upsert `exercises` (owner_id NULL, source 'library',
  muscle_primary, equipment, status) + upsert des 2 `exercise_translations` (fr, en) **séquentiel** ; gestion
  d'erreur (retour typé pour surface FR). *(Note : partiel possible → message d'erreur permettant de retenter ;
  rollback best-effort optionnel.)*
- [ ] `setStatus(id, 'draft'|'published')` (update) ; `archiveExercise(id)` (soft-delete exercice **+** ses 2 trads).
- [ ] Réutiliser `MUSCLE_GROUPS` / types de `@wellness/shared` + `Database`.
- [ ] typecheck/lint verts.
- [ ] **Commit** : `feat(admin): couche data exercices (list/get/save/status/archive)`.

### Tâche 4 — Écrans + routing + nav + i18n

- [ ] **i18n** `fr.ts` : `exercises.{ navTitle, listTitle, search, filterGroup, filterStatus, statusDraft,
  statusPublished, colName, colGroup, colStatus, colDate, colActions, new, edit, archive, archiveConfirm, publish,
  unpublish, formTitleNew, formTitleEdit, groupLabel, equipmentLabel, nameFr, nameEn, instructionsFr, instructionsEn,
  save, saving, error, requiredBoth, back, empty, groupNames.{chest,back,legs,shoulders,arms,core} }`.
- [ ] `ExercisesScreen` (liste) : tableau (nom FR, groupe traduit, badge statut, date) + recherche nom + filtres
  groupe/statut + bouton « Nouvel exercice » + actions (éditer, publier/brouillon, archiver avec confirmation) ;
  états loading/vide/erreur.
- [ ] `ExerciseEditScreen` (formulaire créer/éditer) : select groupe (`MUSCLE_GROUPS`), équipement (optionnel),
  **nom FR + nom EN requis** (bloque l'enregistrement sinon → `requiredBoth`), instructions FR/EN (optionnelles),
  statut (brouillon par défaut à la création) ; `saveExercise` → retour liste ; erreurs FR.
- [ ] **Routing** (`App.tsx`) sous `RequireAdmin` + `AdminLayout` : `/exercises` (liste), `/exercises/new`,
  `/exercises/:id` (édition). **AdminLayout** : rendre l'entrée « Exercices » **cliquable** (NavLink) au lieu de
  « bientôt » (garder les autres en « bientôt »).
- [ ] typecheck/lint verts ; build admin OK.
- [ ] **Commit** : `feat(admin): écrans CRUD exercices + routing + nav (US 8.2)`.

## Definition of Done (rappel spec §7)
- [ ] Migration status + RLS (DROP+CREATE ; select publié/self/admin ; insert/update admin) ; `database.types` étendu.
- [ ] Sync rules : exercices éditoriaux filtrés `status='published'` ; **mobile inchangé**.
- [ ] Admin : liste (recherche/filtre), créer/éditer bilingue (FR+EN requis), brouillon/publié, archiver ; gated admin.
- [ ] i18n FR ; erreurs surfacées ; typecheck/lint verts ; build admin OK ; aucune vraie clé.
- [ ] 🔴 par Florian (bon projet) : migration + redéploiement sync rules + `db:types` + recette.
- [ ] Revues (conformité + qualité) + PR relue.

## Notes
- **Sécurité** : RLS = frontière ; clé anon ; soft-delete ; brouillons non synchronisés au mobile.
- **Réutilisation** : `MUSCLE_GROUPS`/Zod `@wellness/shared`, pattern data F2 (`roles.ts`), écrans F2 (RolesScreen), `fr.ts`.
- **Non-régression** : ne pas casser la lecture mobile (aucun changement `schema.ts`/repo mobile) ni la RLS des exercices utilisateur.
