# US 4.32 — Alerte croisée déficit + fort volume — Plan d'implémentation

> **Pour l'exécutant :** tâche par tâche (cases `- [ ]`), TDD, commits fréquents.
> Spec : [4.32-alerte-deficit-volume.md](../specs/functional/us/4.32-alerte-deficit-volume.md).
> Sous-skill : `superpowers:subagent-driven-development`.

**Goal :** déplacer l'alerte « déficit + fort volume » de l'écran Stats nutrition vers un **widget
dashboard conditionnel** aux règles renforcées (≥ 4 jours loggés, gating piliers, message avec `%`).

**Architecture :** logique pure `computeDeficitVolumeAlert` dans `@wellness/shared` (testée Vitest,
réutilise `shouldAlertDeficitVolume`/`averageIntake`) ; enregistrement d'un nouveau widget au registre
`dashboard.ts` ; hook `useDeficitVolumeAlert` (mobile) qui câble données + gating ; composant
`DeficitVolumeAlertCard` (rend `null` si pas d'alerte) ; retrait de l'ancienne alerte sur
`nutrition-stats.tsx`. **100 % client, offline — aucune migration, aucun cloud, aucune dépendance
native, pas de checkpoint 🔴.**

**Tech Stack :** TypeScript, Zod déjà en place, React Native/Expo, PowerSync (`useQuery`), Vitest.

---

## Structure des fichiers

- **Modifier** `packages/shared/src/bodyweight.ts` — ajouter `MIN_LOGGED_DAYS`, type `DeficitVolumeAlert`, `computeDeficitVolumeAlert` (réutilise `shouldAlertDeficitVolume`/`averageIntake` du même fichier).
- **Modifier** `packages/shared/src/bodyweight.test.ts` — tests de `computeDeficitVolumeAlert`.
- **Modifier** `packages/shared/src/dashboard.ts` — ajouter l'id `'deficit-volume'` à `DASHBOARD_WIDGET_IDS` + entrée `WIDGET_PILLARS` `['strength','nutrition']`.
- **Modifier** `packages/shared/src/dashboard.test.ts` — ajuster les attentes si le nombre/ordre de widgets est testé.
- **Modifier** `apps/mobile/src/data/repositories/dashboard-repository.ts` — hook `useDeficitVolumeAlert`.
- **Créer** `apps/mobile/src/components/dashboard/DeficitVolumeAlertCard.tsx` — widget (rend `null` si `!show`).
- **Modifier** `apps/mobile/src/components/dashboard/dashboard-widgets.tsx` — mapper `'deficit-volume'` → `DeficitVolumeAlertCard`.
- **Modifier** `apps/mobile/src/app/nutrition-stats.tsx` — **retirer** le bloc d'alerte + la requête volume + les imports/hooks devenus inutiles.
- **Modifier** `apps/mobile/src/i18n/locales/fr.json` + `en.json` — ajouter `home.deficitVolume.*`, **retirer** `stats.deficitAlert`.

---

## Task 1 : logique pure `computeDeficitVolumeAlert` (TDD)

**Files:** Modify `packages/shared/src/bodyweight.ts` + `bodyweight.test.ts`.

Ajouter (après `shouldAlertDeficitVolume`) :
```ts
export const MIN_LOGGED_DAYS = 4; // sur 7

export type DeficitVolumeAlert = {
  show: boolean;
  deficitPct: number; // écart sous l'objectif en %, arrondi (0 si pas d'alerte)
  loggedDays: number; // nb de jours loggés en entrée
};

/**
 * Règle d'affichage de l'alerte croisée déficit + fort volume (déterministe, pure).
 * `loggedDailyKcals` = kcal des SEULS jours loggés de la fenêtre 7 j (tableau épars).
 */
export function computeDeficitVolumeAlert(params: {
  loggedDailyKcals: ReadonlyArray<number>;
  targetKcal: number;
  weeklyVolume: number;
}): DeficitVolumeAlert {
  const { loggedDailyKcals, targetKcal, weeklyVolume } = params;
  const loggedDays = loggedDailyKcals.length;
  if (loggedDays < MIN_LOGGED_DAYS || targetKcal <= 0) {
    return { show: false, deficitPct: 0, loggedDays };
  }
  const avgDailyKcal = Math.round(loggedDailyKcals.reduce((s, k) => s + k, 0) / loggedDays);
  const show = shouldAlertDeficitVolume({ avgDailyKcal, targetKcal, weeklyVolume });
  const deficitPct = show ? Math.round(((targetKcal - avgDailyKcal) / targetKcal) * 100) : 0;
  return { show, deficitPct, loggedDays };
}
```

- [ ] **Step 1 — Test qui échoue** (`bodyweight.test.ts`, ajouter un `describe('computeDeficitVolumeAlert')`) :

```ts
import { computeDeficitVolumeAlert } from './bodyweight';

describe('computeDeficitVolumeAlert', () => {
  const base = { targetKcal: 2500, weeklyVolume: 9000 }; // volume ≥ 8000
  it('pas d\'alerte si < 4 jours loggés (même déficit + volume OK)', () => {
    const r = computeDeficitVolumeAlert({ loggedDailyKcals: [2000, 2000, 2000], ...base });
    expect(r.show).toBe(false);
    expect(r.loggedDays).toBe(3);
  });
  it('alerte si ≥ 4 jours, déficit ≥ 15 %, volume ≥ 8000', () => {
    // moyenne 2000 vs 2500 → déficit 20 %
    const r = computeDeficitVolumeAlert({ loggedDailyKcals: [2000, 2000, 2000, 2000], ...base });
    expect(r.show).toBe(true);
    expect(r.deficitPct).toBe(20);
  });
  it('moyenne calculée sur les jours loggés uniquement (pas de zéro fantôme)', () => {
    const r = computeDeficitVolumeAlert({ loggedDailyKcals: [2000, 2000, 2000, 2000], ...base });
    expect(r.show).toBe(true); // si un 0 était compté, la moyenne chuterait mais le contrat est épars
  });
  it('pas d\'alerte si déficit < 15 %', () => {
    // moyenne 2300 vs 2500 → 8 %
    expect(computeDeficitVolumeAlert({ loggedDailyKcals: [2300, 2300, 2300, 2300], ...base }).show).toBe(false);
  });
  it('pas d\'alerte si volume < 8000', () => {
    expect(computeDeficitVolumeAlert({ loggedDailyKcals: [2000, 2000, 2000, 2000], targetKcal: 2500, weeklyVolume: 5000 }).show).toBe(false);
  });
  it('pas d\'alerte si targetKcal <= 0', () => {
    expect(computeDeficitVolumeAlert({ loggedDailyKcals: [2000, 2000, 2000, 2000], targetKcal: 0, weeklyVolume: 9000 }).show).toBe(false);
  });
});
```

- [ ] **Step 2 — Lancer, échec** : `npx vitest run packages/shared/src/bodyweight.test.ts` → FAIL (fonction absente).
- [ ] **Step 3 — Implémenter** `computeDeficitVolumeAlert` (+ `MIN_LOGGED_DAYS`, type) comme ci-dessus. Est déjà exporté via le barrel `index.ts` (le fichier `bodyweight` y est ré-exporté — vérifier).
- [ ] **Step 4 — Lancer, succès** : même commande → PASS ; puis `npm run test --workspace @wellness/shared` (pas de régression).
- [ ] **Step 5 — Commit** : `git commit -m "feat(shared): computeDeficitVolumeAlert — règle alerte déficit+volume (TDD)"`.

---

## Task 2 : enregistrer le widget au registre dashboard

**Files:** Modify `packages/shared/src/dashboard.ts` (+ `dashboard.test.ts`).

- [ ] **Step 1** — Ajouter `'deficit-volume'` à `DASHBOARD_WIDGET_IDS` (fin de liste, après `'running-week'`) et l'entrée `WIDGET_PILLARS`:
  `'deficit-volume': ['strength', 'nutrition'],`.
  ⚠️ Rappel : `isWidgetAllowed` filtre en **« au moins un pilier actif »** (`.some()`). Le widget
  apparaîtra donc dès que muscu **OU** nutrition est actif ; l'exigence **« les deux »** est portée
  par le **hook** (Task 3), qui renvoie `show=false` si l'un manque → le widget se replie (`null`).
- [ ] **Step 2** — `dashboard.test.ts` : plusieurs assertions vont casser, **les mettre à jour** pour inclure `'deficit-volume'` — repérées à la revue : (a) l'assertion de **tableau exact** des IDs (~ligne 17) ; (b) **trois** contrôles de **longueur « 7 widgets »** → 8 ; (c) le cas « (a bis) » où un set de piliers **nutrition seule** fera désormais **aussi** apparaître `'deficit-volume'` (car `WIDGET_PILLARS` utilise `.some()`), changeant le tableau attendu. Lancer `npx vitest run packages/shared/src/dashboard.test.ts` → vert.
- [ ] **Step 3 — Commit** : `git commit -m "feat(shared): widget deficit-volume au registre dashboard (piliers muscu+nutrition)"`.

---

## Task 3 : hook `useDeficitVolumeAlert` (mobile)

**Files:** Modify `apps/mobile/src/data/repositories/dashboard-repository.ts`.

Réutiliser l'existant :
- **apports 7 j** : `useDailyTotals(daysAgo(7))` (renvoie `{ totals }`, tableau **épars** des jours loggés — même hook que `nutrition-stats.tsx`). `loggedDailyKcals = totals.map((d) => d.kcal)`.
- **cible de base** : `useNutritionSummary()` expose déjà la cible calorique **de base** (`target`, non ajustée jour-de-séance) — la réutiliser (ne PAS recalculer `tdee`/`targetCalories`). _Vérifier le nom exact du champ ; sinon calculer la base comme le fait `nutrition-stats.tsx` (tdee → targetCalories) via un helper partagé._
- **volume muscu 7 j** : **déplacer** la requête SQL de `nutrition-stats.tsx` (lignes ~75-81) ici :
  ```sql
  SELECT ws.reps, ws.weight_kg FROM workout_sets ws
  JOIN workouts w ON w.id = ws.workout_id AND w.deleted_at IS NULL
  WHERE ws.deleted_at IS NULL AND ws.done = 1 AND ws.set_type != 'warmup' AND w.started_at >= ?
  ```
  paramètre `[daysAgo(7) + 'T00:00:00.000Z']` ; `weeklyVolume = Σ reps × weight_kg`. (`daysAgo` helper local, cf. nutrition-stats — le dupliquer/mutualiser.)
- **gating piliers** : lire `activePillars` comme le fait déjà `useMostRecentRecord` (`settings?.activePillars`, cf. `dashboard-repository.ts:443`). Si `!(strengthActive && nutritionActive)` → renvoyer `{ show: false, deficitPct: 0, loggedDays: 0 }` **sans** exécuter la requête volume (ou l'ignorer).

- [ ] **Step 1** — Écrire `useDeficitVolumeAlert(): DeficitVolumeAlert` : compose les 3 lectures + gating, appelle `computeDeficitVolumeAlert({ loggedDailyKcals, targetKcal, weeklyVolume })`, renvoie le résultat. Réactif (`useQuery`/hooks existants). Lecture seule.
- [ ] **Step 2 — Vérifier** : `npm run typecheck --workspace @wellness/mobile` → vert.
- [ ] **Step 3 — Commit** : `git commit -m "feat(mobile): hook useDeficitVolumeAlert (câblage + gating piliers)"`.

---

## Task 4 : widget `DeficitVolumeAlertCard` + mapping

**Files:** Create `apps/mobile/src/components/dashboard/DeficitVolumeAlertCard.tsx` ; Modify `dashboard-widgets.tsx`.

- [ ] **Step 1** — `DeficitVolumeAlertCard.tsx` (mirroir de `RecordRecentCard`/`NutritionSummaryCard`) :
  - Signature `({ size }: { size?: WidgetSize }) => React.ReactElement | null` (le type widget autorise `null`).
  - `const alert = useDeficitVolumeAlert();`
  - `if (!alert.show) return null;` (widget conditionnel — pas de dismiss à coder).
  - Sinon rendre une carte (réutiliser le composant carte des autres widgets, style charte, icône alerte) avec `t('home.deficitVolume.message', { pct: alert.deficitPct })`. Titre `t('home.deficitVolume.title')`.
- [ ] **Step 2** — `dashboard-widgets.tsx` : importer `DeficitVolumeAlertCard`, ajouter au `WIDGET_COMPONENTS` : `'deficit-volume': DeficitVolumeAlertCard,`.
- [ ] **Step 3 — Vérifier** : `npm run typecheck --workspace @wellness/mobile` → vert.
- [ ] **Step 4 — Commit** : `git commit -m "feat(mobile): widget DeficitVolumeAlertCard (conditionnel) + mapping dashboard"`.

---

## Task 5 : i18n

**Files:** Modify `apps/mobile/src/i18n/locales/fr.json` + `en.json`.

- [ ] **Step 1** — Ajouter la section `home.deficitVolume` (aligné sur les autres clés `home.*`) :
  - FR : `title` (ex. « Nutrition & charge »), `message` = « Cette semaine : apports ~{{pct}} % sous ton objectif, avec un volume muscu élevé. Pense à manger assez pour bien récupérer. »
  - EN (miroir) : `message` = « This week: intake ~{{pct}}% below your target, with high lifting volume. Make sure you eat enough to recover well. »
- [ ] **Step 2** — **Retirer** `stats.deficitAlert` de `fr.json` et `en.json` (ligne ~372) — devient inutile (Task 6).
- [ ] **Step 3 — Vérifier parité manuellement** : ⚠️ **il n'existe pas de script/test de parité i18n** dans le repo. Comparer **à la main** les jeux de clés de `fr.json` et `en.json` : `home.deficitVolume.*` présent des deux côtés avec les mêmes sous-clés ; `stats.deficitAlert` retiré des deux ; aucune clé orpheline. (Diff simple des deux fichiers sur la zone touchée.)
- [ ] **Step 4 — Commit** : `git commit -m "i18n: home.deficitVolume (FR/EN) + retrait stats.deficitAlert"`.

---

## Task 6 : retrait de l'ancienne alerte sur `nutrition-stats.tsx`

**Files:** Modify `apps/mobile/src/app/nutrition-stats.tsx`.

- [ ] **Step 1** — Supprimer le **bloc d'affichage** de l'alerte (lignes ~101-107, le `{showDeficitAlert ? ... : null}`).
- [ ] **Step 2** — Supprimer le **calcul** de l'alerte (lignes ~59-86 : `week`/`avg7`, `objective`, `age`, `tdeeVal`, `weekTarget`, la `useQuery` volume, `weeklyVolume`, `showDeficitAlert`).
- [ ] **Step 3** — Nettoyer les **imports/hooks devenus inutiles** — vérifier au cas par cas ce qui n'est **plus** utilisé ailleurs dans le fichier avant de retirer : probablement `shouldAlertDeficitVolume`, `objectiveFromGoal`, `computeAge`, `tdee`, `targetCalories`, `useProfile`, `useNutritionProfile`, `useQuery`, et l'import top-level **`Ionicons`** (`@expo/vector-icons`) qui devient entièrement inutilisé une fois l'icône `warning-outline` retirée. **Conserver** ce qui sert encore (`averageIntake` pour `avg`, `useDailyTotals` pour le graphe d'apports, tout le bloc poids). Supprimer les styles `alert`/`alertText` s'ils ne servent qu'à l'alerte.
- [ ] **Step 4 — Vérifier** : `npm run typecheck --workspace @wellness/mobile` + `npm run lint` → 0 import inutilisé, vert. L'écran Stats ne doit plus afficher l'alerte.
- [ ] **Step 5 — Commit** : `git commit -m "refactor(mobile): retrait de l'alerte déficit+volume de nutrition-stats (migrée sur le dashboard)"`.

---

## Task 7 : vérification d'ensemble

- [ ] **Step 1** — `npm run test` (dont `bodyweight` + `dashboard` shared) : vert.
- [ ] **Step 2** — `npm run typecheck` (tous les workspaces) : vert.
- [ ] **Step 3** — `npm run lint` : 0 erreur.
- [ ] **Step 4** — Relire le diff : widget rend bien `null` hors alerte ; gating « les deux piliers » dans le hook ; message i18n paramétré, parité FR/EN ; `stats.deficitAlert` bien retirée ; aucune double alerte ; aucune chaîne en dur.

---

## Notes

- **Gate CLAUDE.md** : spec validée ; ce plan à valider (maquette **écartée** — carte texte, précédent 1.15/8.5). Code autorisé après validation.
- **Seuils** conservés (`DEFICIT_ALERT_RATIO = 0.15`, `HIGH_VOLUME_THRESHOLD = 8000`) — heuristiques, ajustables plus tard (volume relatif au user = hors périmètre).
- **Reste checkpoint recette (Florian)** : provoquer/lever la condition (déficit + volume, ≥ 4 jours loggés) → widget apparaît/se replie ; désactiver un pilier → widget disparaît ; **confirmer que l'alerte n'apparaît plus sur l'écran Stats**. Pas de checkpoint 🔴 cloud.
- **YAGNI** : pas de snooze, pas de réglage on/off dédié, pas de seuils configurables, pas de couche IA.
