# Running R4a — Historique + stats + courbe d'allure — Plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Un écran « Historique & progression » du pilier course : statistiques distance/temps (semaine/mois/début), courbe d'allure globale (30/90 j) + tendance, et liste chronologique des courses → détail existant. **Lecture seule** sur `runs`, **aucune migration, aucun nouveau build**.

**Architecture:** Logique d'agrégation **pure et testée** dans `@wellness/shared` (`run-stats.ts`). Hooks de lecture réactifs dans `run-repository.ts` (aucune écriture). Écran Expo Router réutilisant `ProgressLineChart` (existant, `react-native-gifted-charts`) + `useUnits`. Périodes de stats **calendaires** (semaine lundi→dimanche, mois calendaire), fenêtres de courbe **glissantes** (30/90 j).

**Tech Stack:** `@wellness/shared` (TS + Vitest), `apps/mobile` (Expo Router SDK 57, `useUnits`, i18next, `useQuery` PowerSync). Réutilise `localDayKey`/`startOfWeek`/`addDays` (`date.ts`), `useRunHistory` (`run-repository.ts`), `ProgressLineChart`, `Segment`/`Card`/`Screen`/`ScreenHeader`/`EmptyState`.

**Spec :** [5.28-5.29-running-r4a-historique-stats.md](../specs/functional/us/5.28-5.29-running-r4a-historique-stats.md) (validée). **Patrons :** `app/progress/index.tsx` (courbe + `ProgressLineChart`), `app/history/` + `app/nutrition-stats.tsx` (écran stats/historique), `run-repository.ts` (`useRunHistory`/`RunHistoryItem`/`SELECT_HISTORY`), `run/summary.tsx` (détail par id + formatage durée).

**Branche :** `feature/running-r4a-historique-stats`, depuis `dev` (spec `e8b8061`).

> **Décisions verrouillées (rappel spec) :**
> - **Lecture seule** : aucune écriture sur `runs`, aucune migration, aucune sync rule, **aucun rebuild** (charts déjà présents).
> - **Stats calendaires** (semaine lundi→dim via `startOfWeek`, mois = préfixe `AAAA-MM`) ; **courbe glissante** (30/90 j jusqu'à aujourd'hui). Dates comparées en clés `AAAA-MM-JJ` (`localDayKey`), sans dérive fuseau.
> - **Globales** (pas de découpage par type — les courses n'ont pas de `session_type`).
> - **Manuel** : compte en distance/temps/nombre ; entre dans la courbe uniquement si `avgPaceSPerKm` non nul. Courses **terminées** uniquement.
> - **Allure basse = rapide** : « tendance en progrès » = allure qui **diminue**.
> - **Non-régression** : `useRunHistory` est déjà consommé par le dashboard (streak / jour d'entraînement) — ne pas changer sa signature/champs ; ajouter de **nouveaux** hooks.

---

## Phase A — `@wellness/shared` : agrégation (TDD)

### Task 1 : `run-stats.ts` — stats + tendance d'allure

**Files:** créer `packages/shared/src/run-stats.ts` (+ `run-stats.test.ts`) ; exporter depuis `packages/shared/src/index.ts`.

Fonctions **pures**, alimentées par une forme minimale de course (le repo fournira la **clé de jour locale** `finishedAtDayKey`). Comparaisons de dates en chaînes `AAAA-MM-JJ` (lexicographique = chronologique). Réutilise `startOfWeek`/`addDays`/`localDayKey` de `./date`.

- [ ] **Step 1 : tests d'abord** (`run-stats.test.ts`, Vitest) :
```ts
import { describe, expect, it } from 'vitest';
import { aggregateRunStats, paceTrendPoints, paceTrend, type StatRun } from './run-stats';

// today = mercredi 15/07/2026 ; lundi de la semaine = 13/07 ; semaine = 13→19 ; mois = 2026-07
const runs: StatRun[] = [
  { finishedAtDayKey: '2026-07-13', distanceM: 5000, durationS: 1800, paceSPerKm: 360 }, // cette semaine
  { finishedAtDayKey: '2026-07-10', distanceM: 8000, durationS: 2400, paceSPerKm: 300 }, // ce mois, sem. préc.
  { finishedAtDayKey: '2026-07-15', distanceM: 3000, durationS: 1200, paceSPerKm: 400 }, // aujourd'hui
  { finishedAtDayKey: '2026-06-30', distanceM: 10000, durationS: 3000, paceSPerKm: null }, // mois préc., manuel sans allure
];

describe('aggregateRunStats', () => {
  it('semaine (lun→dim) : courses de la semaine courante', () => {
    expect(aggregateRunStats(runs, 'week', '2026-07-15')).toEqual({ totalDistanceM: 8000, totalDurationS: 3000, count: 2 });
  });
  it('mois calendaire courant', () => {
    expect(aggregateRunStats(runs, 'month', '2026-07-15')).toEqual({ totalDistanceM: 16000, totalDurationS: 5400, count: 3 });
  });
  it('depuis le début : toutes', () => {
    expect(aggregateRunStats(runs, 'all', '2026-07-15')).toEqual({ totalDistanceM: 26000, totalDurationS: 8400, count: 4 });
  });
  it('distance/durée nulles comptées comme 0 mais la course compte', () => {
    const r: StatRun[] = [{ finishedAtDayKey: '2026-07-15', distanceM: null, durationS: null, paceSPerKm: null }];
    expect(aggregateRunStats(r, 'all', '2026-07-15')).toEqual({ totalDistanceM: 0, totalDurationS: 0, count: 1 });
  });
});

describe('paceTrendPoints', () => {
  it('fenêtre 30 j, exclut hors fenêtre et sans allure, trié asc', () => {
    const pts = paceTrendPoints(runs, 30, '2026-07-15'); // fenêtre >= 2026-06-16
    expect(pts).toEqual([
      { dayKey: '2026-07-10', paceSPerKm: 300 },
      { dayKey: '2026-07-13', paceSPerKm: 360 },
      { dayKey: '2026-07-15', paceSPerKm: 400 },
    ]); // 30/06 exclu (hors 30 j ET allure nulle)
  });
});

describe('paceTrend', () => {
  it('allure qui diminue = improving', () =>
    expect(paceTrend([{ dayKey: 'a', paceSPerKm: 360 }, { dayKey: 'b', paceSPerKm: 350 }, { dayKey: 'c', paceSPerKm: 330 }, { dayKey: 'd', paceSPerKm: 320 }])).toBe('improving'));
  it('allure qui augmente = declining', () =>
    expect(paceTrend([{ dayKey: 'a', paceSPerKm: 320 }, { dayKey: 'b', paceSPerKm: 330 }, { dayKey: 'c', paceSPerKm: 350 }, { dayKey: 'd', paceSPerKm: 360 }])).toBe('declining'));
  it('variation < 2 % = stable', () =>
    expect(paceTrend([{ dayKey: 'a', paceSPerKm: 350 }, { dayKey: 'b', paceSPerKm: 351 }, { dayKey: 'c', paceSPerKm: 349 }, { dayKey: 'd', paceSPerKm: 350 }])).toBe('stable'));
  it('< 2 points = stable', () =>
    expect(paceTrend([{ dayKey: 'a', paceSPerKm: 350 }])).toBe('stable'));
});
```
- [ ] **Step 2 : lancer, vérifier l'échec** — `npm run test -w @wellness/shared` → FAIL.
- [ ] **Step 3 : implémenter** (`run-stats.ts`) :
```ts
import { addDays, localDayKey, startOfWeek } from './date';

export interface StatRun {
  finishedAtDayKey: string; // AAAA-MM-JJ (jour local de fin)
  distanceM: number | null;
  durationS: number | null;
  paceSPerKm: number | null;
}
export type StatPeriod = 'week' | 'month' | 'all';
export interface RunStats { totalDistanceM: number; totalDurationS: number; count: number; }
export interface PaceTrendPoint { dayKey: string; paceSPerKm: number; }
export type PaceTrendKind = 'improving' | 'declining' | 'stable';

/** Borne basse (incluse) de la période calendaire, en clé AAAA-MM-JJ. `null` = pas de borne (all). */
function periodStartKey(period: StatPeriod, todayKey: string): string | null {
  if (period === 'all') return null;
  const [y, m, d] = todayKey.split('-').map(Number);
  const today = new Date(y!, m! - 1, d!);
  if (period === 'week') return localDayKey(startOfWeek(today));
  return `${todayKey.slice(0, 7)}-01`; // 1er du mois courant
}

export function aggregateRunStats(runs: StatRun[], period: StatPeriod, todayKey: string): RunStats {
  const from = periodStartKey(period, todayKey);
  // Mois : borne haute = fin du mois courant ; semaine : lundi..dimanche. On borne par >= from ET <= todayEnd.
  // Pour week/month, la borne haute pertinente est la fin de période ; comme les courses passées uniquement
  // existent, borner en haut par le dernier jour de la période évite d'inclure une future (improbable) —
  // on filtre >= from et (period==='week' ? <= dimanche : même préfixe AAAA-MM).
  const inPeriod = (k: string): boolean => {
    if (period === 'all') return true;
    if (period === 'month') return k.slice(0, 7) === todayKey.slice(0, 7);
    // week
    const monday = from!;
    const [y, m, d] = monday.split('-').map(Number);
    const sunday = localDayKey(addDays(new Date(y!, m! - 1, d!), 6));
    return k >= monday && k <= sunday;
  };
  let totalDistanceM = 0, totalDurationS = 0, count = 0;
  for (const r of runs) {
    if (!inPeriod(r.finishedAtDayKey)) continue;
    totalDistanceM += r.distanceM ?? 0;
    totalDurationS += r.durationS ?? 0;
    count += 1;
  }
  return { totalDistanceM, totalDurationS, count };
}

export function paceTrendPoints(runs: StatRun[], days: number, todayKey: string): PaceTrendPoint[] {
  const [y, m, d] = todayKey.split('-').map(Number);
  const fromKey = localDayKey(addDays(new Date(y!, m! - 1, d!), -(days - 1)));
  return runs
    .filter((r) => r.paceSPerKm != null && r.finishedAtDayKey >= fromKey && r.finishedAtDayKey <= todayKey)
    .map((r) => ({ dayKey: r.finishedAtDayKey, paceSPerKm: r.paceSPerKm as number }))
    .sort((a, b) => (a.dayKey < b.dayKey ? -1 : a.dayKey > b.dayKey ? 1 : 0));
}

export function paceTrend(points: PaceTrendPoint[]): PaceTrendKind {
  if (points.length < 2) return 'stable';
  const n = points.length;
  const firstHalf = points.slice(0, Math.floor(n / 2));
  const secondHalf = points.slice(Math.ceil(n / 2));
  const avg = (xs: PaceTrendPoint[]) => xs.reduce((s, p) => s + p.paceSPerKm, 0) / xs.length;
  const m1 = avg(firstHalf), m2 = avg(secondHalf);
  const ratio = (m2 - m1) / m1;
  if (ratio < -0.02) return 'improving'; // allure diminue = plus rapide
  if (ratio > 0.02) return 'declining';
  return 'stable';
}
```
- [ ] **Step 3bis : ajouter `formatDurationHms`** (formatage de durée partagé — il n'en existe aucun dans `useUnits` ni `@wellness/shared` ; `run/summary.tsx:31` a une copie **locale non exportée**). Tests d'abord dans `run-stats.test.ts` :
```ts
import { formatDurationHms } from './run-stats';
describe('formatDurationHms', () => {
  it('null / négatif → ""', () => { expect(formatDurationHms(null)).toBe(''); expect(formatDurationHms(-5)).toBe(''); });
  it('< 1 h → M min SS s', () => expect(formatDurationHms(1830)).toBe('30 min 30 s')); // 30:30
  it('≥ 1 h → H h MM min SS s', () => expect(formatDurationHms(3930)).toBe('1 h 5 min 30 s'));
  it('0 → 0 min 0 s', () => expect(formatDurationHms(0)).toBe('0 min 0 s'));
});
```
  Implémentation (calquée sur le format de `run/summary.tsx`, unité-neutre FR/EN) :
```ts
export function formatDurationHms(totalSeconds: number | null | undefined): string {
  if (totalSeconds == null || totalSeconds < 0) return '';
  const s = Math.floor(totalSeconds % 60);
  const m = Math.floor((totalSeconds / 60) % 60);
  const h = Math.floor(totalSeconds / 3600);
  return h > 0 ? `${h} h ${m} min ${s} s` : `${m} min ${s} s`;
}
```
  (Le nouvel écran l'utilisera. **Ne pas** refactorer `run/summary.tsx` dans ce lot — hors périmètre ; sa copie locale reste.)
- [ ] **Step 4 : lancer, vérifier le succès.** Ajouter `export * from './run-stats';` dans `index.ts` (`./date` déjà réexporté). Tous les tests (stats + tendance + durée) verts.
- [ ] **Step 5 : commit** — `feat(shared): agrégation stats de course + tendance d'allure + formatDurationHms (testé)`

---

## Phase B — Repository : hooks de lecture

### Task 2 : `useRunStats` + `usePaceTrend`

**Files:** MAJ `apps/mobile/src/data/repositories/run-repository.ts`. READ `useRunHistory` (~252-263), `RunHistoryItem` (~55-66), `rowToHistoryItem`, et le bloc d'imports `@wellness/shared` (aujourd'hui `useQuery`, `appendToTrack`, `averagePace`, `RunSource` — **`localDayKey` n'y est PAS** : il faudra l'**ajouter** à l'import, avec les types/fonctions `run-stats`). Le pattern `localDayKey(new Date(finishedAt))` est déjà utilisé dans `dashboard-repository.ts` (référence).

Ne PAS modifier `useRunHistory` (consommé par le dashboard). Ajouter **de nouveaux hooks** qui réutilisent la même source (les courses terminées) et dérivent la **clé de jour locale** depuis `finishedAt` (ISO UTC → `localDayKey(new Date(finishedAt))`).

- [ ] Ajouter un mapping course → `StatRun` : `{ finishedAtDayKey: localDayKey(new Date(item.finishedAt!)), distanceM: item.distanceM, durationS: item.durationSeconds, paceSPerKm: item.avgPaceSPerKm }` (les courses terminées ont `finishedAt` non nul).
- [ ] **`useRunStats(period: StatPeriod)`** → `{ stats: RunStats; isLoading }` : consomme `useRunHistory()` (ou une requête équivalente), mappe en `StatRun[]`, `todayKey = localDayKey(new Date())`, retourne `aggregateRunStats(runs, period, todayKey)`.
- [ ] **`usePaceTrend(days: number)`** → `{ points: PaceTrendPoint[]; trend: PaceTrendKind; isLoading }` : idem, `paceTrendPoints(runs, days, todayKey)` + `paceTrend(points)`.
- [ ] Réexporter les types `StatPeriod`/`RunStats`/`PaceTrendKind` si utile côté écran (ou importer depuis `@wellness/shared`).
- [ ] Typecheck. **Commit** : `feat(mobile): hooks useRunStats + usePaceTrend (lecture seule, réutilise useRunHistory)`

> Aucune écriture, aucune nouvelle requête d'écriture. `useRunHistory` inchangé → dashboard non régressé.

---

## Phase C — i18n

### Task 3 : clés `running.history.*` (FR + EN)

**Files:** MAJ `apps/mobile/src/i18n/locales/{fr,en}.json`.
- [ ] Sous `running.history` : `{ title, empty, statsTitle, period, week, month, all, totalDistance, totalTime, runCount, paceTitle, days30, days90, trend, trendImproving, trendDeclining, trendStable }`. Ex. FR : title « Historique & progression », empty « Aucune course enregistrée. », week « Semaine », month « Mois », all « Depuis le début », totalDistance « Distance totale », totalTime « Temps total », runCount « Courses », paceTitle « Allure moyenne », days30 « 30 j », days90 « 90 j », trendImproving « En progrès », trendDeclining « En baisse », trendStable « Stable ». EN symétrique. Réutilise `running.*` existants si pertinent. Parité FR/EN, aucune chaîne en dur.
- [ ] **Commit** (groupable avec Task 4) : `feat(i18n): historique & stats de course (running.history.*)`

---

## Phase D — Écran

### Task 4 : `running-history/index.tsx` + entrée onglet Course

**Files:** créer `apps/mobile/src/app/running-history/index.tsx` + `_layout.tsx` ; MAJ `apps/mobile/src/app/(tabs)/running.tsx` (entrée) ; MAJ `apps/mobile/src/app/_layout.tsx` (enregistrer la route `running-history`, comme `running-planning`). READ `app/progress/index.tsx` (patron courbe `ProgressLineChart`), `app/nutrition-stats.tsx` / `app/history/` (patron écran stats/liste), `run/summary.tsx` (formatage durée), `(tabs)/running.tsx` (bloc `runningActive`).

- [ ] **`_layout.tsx`** (`running-history`) : `<Stack screenOptions={{ headerShown: false }} />` (calqué sur `running-planning/_layout.tsx`).
- [ ] **`running-history/index.tsx`** : `ScreenHeader` (titre `running.history.title`) + `Screen` scrollable. Trois sections :
  1. **Stats** : `Segment` période (`week`/`month`/`all`, libellés `running.history.{week,month,all}`), état local `period`. `const { stats } = useRunStats(period);` Affiche `totalDistance` (via `useUnits.formatDistance(stats.totalDistanceM/1000)`), `totalTime` (via **`formatDurationHms(stats.totalDurationS)`** de `@wellness/shared`, ajouté en Task 1), `runCount` (`stats.count`). Dans des `Card`.
  2. **Courbe d'allure** : `Segment` fenêtre (`days30`/`days90` → 30/90), état local `days`. `const { points, trend } = usePaceTrend(days);` `ProgressLineChart` avec `data = points.map(p => ({ label: <JJ/MM court>, value: <pace dans l'unité d'affichage, en secondes> }))` et `unit` = libellé allure (`useUnits` — « min/km » ou « min/mi »). Afficher le libellé de **tendance** (`trendImproving/Declining/Stable`). Si `points.length === 0`, masquer la courbe (le composant rend `null` sur data vide) et afficher un court message.
  3. **Liste** : `const { runs } = useRunHistory();` liste (map ou FlatList) → chaque item : date (JJ/MM/AAAA depuis `finishedAt`), distance (`useUnits.formatDistance`), allure (`useUnits.formatPace(avgPaceSPerKm)`), durée (`formatDurationHms`). **Tap → `router.push('/run/summary?id=' + item.id)`**. `EmptyState` (`running.history.empty`) si `runs.length === 0`. (Pour les libellés de dates courts de la courbe : calquer le `formatDateShort(iso) → JJ/MM` local de `app/progress/index.tsx`.)
  - **Note courbe (limitation acceptée)** : l'axe Y de `ProgressLineChart` est numérique (allure en secondes de l'unité d'affichage) — pas de format MM:SS sur l'axe. C'est acceptable au MVP : la **liste** et le **libellé de tendance** donnent la valeur précise. Une ligne **descendante = amélioration** (allure qui baisse) — le libellé de tendance lève l'ambiguïté.
- [ ] **Entrée** : dans `(tabs)/running.tsx`, bloc `runningActive`, ajouter un `Button` **« Historique »** (`running.history.title`) → `router.push('/running-history')`, à côté de « Mes programmes » / « Mon planning ».
- [ ] **Route racine** : enregistrer `running-history` dans `app/_layout.tsx` (comme `running-planning`).
- [ ] Typecheck + lint (`npm run lint -w @wellness/mobile`). **Commit** : `feat(mobile): écran historique & stats de course (stats, courbe d'allure, liste)`

> Toutes distances/allures/durées via `useUnits` / helper de durée partagé. Aucune unité ni chaîne en dur.

---

## Phase E — Vérification

### Task 5 : Qualité
- [ ] `npm run typecheck` (3 workspaces) ; `npm run lint -w @wellness/mobile` (pas de nouvelle erreur — 4 warnings charts-smoke tolérés) ; `npm run test` (shared Task 1 + suites existantes ; jest-expo mobile).
- [ ] Parité i18n FR/EN ; **0 doublon de clé** (détecteur sur `fr.json`/`en.json`) ; aucune unité/chaîne en dur.
- [ ] **Non-régression** : `useRunHistory` inchangé (signature + champs) → **dashboard streak / jour d'entraînement intacts** ; aucune écriture ajoutée ; muscu **et** running existants non régressés.
- [ ] Vérifier `packages/shared/src/index.ts` exporte `run-stats`.
- [ ] **Commit** éventuel.

### Task 6 : Revue finale (subagent-driven : spec puis qualité)
- [ ] Revue de conformité spec (DoD §6) puis revue qualité (agrégation correcte, dates calendaires vs glissantes, non-régression `useRunHistory`, offline-first lecture seule, hooks/React Compiler, courbe).

> **Pas de checkpoint 🔴 cloud** : R4a est en lecture seule, aucune migration, aucune dépendance native nouvelle → **aucun rebuild**. Vérif device possible sur le build courant.

---

## Ordre de build & commits

A (shared, TDD) → B (repo, hooks lecture) → C (i18n) → D (écran + entrée) → E (qualité + revue finale). Un commit par tâche, via `/commit`. **Ne jamais stager `apps/mobile/eas.json`** ni de secret. **Aucune migration, aucun `db:types`, aucun rebuild.**

## Definition of Done (spec §6)

Agrégation testée (shared) ✔A ; hooks de lecture `useRunStats`/`usePaceTrend` sans écriture ✔B ; i18n FR+EN ✔C ; écran (stats Semaine/Mois/Début + courbe 30/90 j + tendance + liste → détail existant + état vide, via `useUnits`) + entrée onglet Course ✔D ; `useRunHistory` inchangé (dashboard non régressé) + qualité verte, aucun rebuild ✔E ; maquette validée avant écran ; PR relue.
