# US 1.22 — Aide & support — Plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development (recommandé) ou
> superpowers:executing-plans. Steps en cases `- [ ]`.
> ⚠️ **Workflow projet** : ne PAS exécuter avant validation des 3 livrables (spec ✅ + plan + maquette).
> ⚠️ **Modules natifs ajoutés** (`expo-mail-composer`, `expo-application`) → **nécessite un nouveau dev build**
> (APK), un simple reload Metro ne suffit pas. Aucune migration, aucun serveur.

**Goal :** offrir un point d'entrée « Aide & support » dans les Réglages — FAQ embarquée bilingue + contact et
signalement de bug via le client mail natif.

**Architecture :** un module `support.ts` (mobile) isole la partie **pure testable** (`formatBugReportBody`) de
l'I/O natif (`collectSupportMeta` via `expo-application`/`expo-device`/`Platform` ; `contactSupport` via
`expo-mail-composer` avec fallback `Alert`). Un écran `/help` rend une FAQ en accordéon **mono-ouverture**
(composant local `FaqItem`) + deux boutons d'action. Une entrée dans les Réglages pointe vers `/help`.

**Tech stack :** TypeScript, React Native + Expo (SDK 57), Expo Router (`Stack.Screen`), i18next
(`returnObjects` pour la liste FAQ), jest-expo (tests), `expo-mail-composer`, `expo-application`, `expo-device`.

**Spec :** [docs/specs/functional/us/1.22-aide-support.md](../specs/functional/us/1.22-aide-support.md)

---

## Structure des fichiers

**Créer :**
- `apps/mobile/src/lib/support.ts` — `SUPPORT_EMAIL`, type `SupportMeta`, `formatBugReportBody` (pur),
  `collectSupportMeta` (I/O natif), `contactSupport` (mail natif + fallback).
- `apps/mobile/src/lib/__tests__/support.test.ts` — tests `formatBugReportBody` (pur) + `contactSupport`
  (chemin fallback, `MailComposer` mocké).
- `apps/mobile/src/components/FaqItem.tsx` — item FAQ contrôlé (question pressable + réponse dépliée).
- `apps/mobile/src/app/help.tsx` — écran Aide & support (FAQ accordéon mono-ouverture + 2 actions).
- `apps/mobile/src/app/__tests__/help-smoke.test.tsx` — smoke test de l'écran.

**Modifier :**
- `apps/mobile/package.json` — ajout `expo-mail-composer`, `expo-application` (via `npx expo install`).
- `apps/mobile/src/app/_layout.tsx` — enregistrer `<Stack.Screen name="help" … />`.
- `apps/mobile/src/app/settings.tsx` — section « Aide & support » (bouton → `/help`).
- `apps/mobile/src/i18n/locales/fr.json` + `en.json` — clés `settings.help.*`, `help.*`.

> **Pourquoi un `FaqItem` dédié et pas `CollapsibleCard` ?** `CollapsibleCard`
> ([components/CollapsibleCard.tsx](../../apps/mobile/src/components/CollapsibleCard.tsx)) est **non contrôlé**
> (chaque carte gère son état → ouverture indépendante, impossible d'imposer le mono-ouverture), son titre est
> `numberOfLines={1}` (une question FAQ peut être longue) et il est stylé pour des cartes de séance
> (summary/footer). Un `FaqItem` **contrôlé** (`expanded`/`onToggle` pilotés par le parent) est plus simple et
> juste.

---

## Task 1 : Deps + helper pur `formatBugReportBody`

**Files:** `apps/mobile/package.json`, `apps/mobile/src/lib/support.ts`, `apps/mobile/src/lib/__tests__/support.test.ts`

- [ ] **Step 1 : installer les modules natifs** (depuis `apps/mobile/`) :

Run: `npx expo install expo-mail-composer expo-application`
Attendu : `package.json` mis à jour, versions alignées SDK 57.

- [ ] **Step 2 : test qui échoue** (`support.test.ts`) — helper pur, `t` stubé (`(k) => k`) :

```ts
import { formatBugReportBody, type SupportMeta } from '../support';

const META: SupportMeta = {
  appVersion: '1.0.0', buildVersion: '42',
  osName: 'Android', osVersion: '14', deviceModel: 'Pixel 7', language: 'fr',
};
const t = ((k: string) => k) as any;

describe('formatBugReportBody', () => {
  it('inclut les 4 métadonnées et l’invite', () => {
    const body = formatBugReportBody(META, t);
    expect(body).toContain('help.bug.prompt');
    expect(body).toContain('1.0.0');
    expect(body).toContain('42');
    expect(body).toContain('Android 14');
    expect(body).toContain('Pixel 7');
    expect(body).toContain('fr');
  });
  it('est déterministe', () => {
    expect(formatBugReportBody(META, t)).toBe(formatBugReportBody(META, t));
  });
});
```

- [ ] **Step 3 : lancer → échec** `npm run test -w @wellness/mobile -- support` (module absent).
- [ ] **Step 4 : implémenter le pur + la constante** (`support.ts`) :

```ts
import type { TFunction } from 'i18next';

/** Adresse de destination du support. PLACEHOLDER — à remplacer par la vraie boîte avant le build. */
export const SUPPORT_EMAIL = 'support@example.com';

export type SupportMeta = {
  appVersion: string;
  buildVersion: string;
  osName: string;
  osVersion: string;
  deviceModel: string;
  language: string;
};

/**
 * Corps pré-rempli du mail de signalement de bug : invite libre + bloc technique délimité,
 * visible et effaçable par l'utilisateur. PUR (aucun I/O) → testé.
 */
export function formatBugReportBody(meta: SupportMeta, t: TFunction): string {
  return [
    t('help.bug.prompt'),
    '',
    '',
    `——— ${t('help.bug.techHeader')} ———`,
    `${t('help.bug.appLabel')} : ${meta.appVersion} (${t('help.bug.buildLabel')} ${meta.buildVersion})`,
    `${t('help.bug.systemLabel')} : ${meta.osName} ${meta.osVersion}`,
    `${t('help.bug.deviceLabel')} : ${meta.deviceModel}`,
    `${t('help.bug.languageLabel')} : ${meta.language}`,
  ].join('\n');
}
```

- [ ] **Step 5 : lancer → succès** `npm run test -w @wellness/mobile -- support` + `npm run typecheck`.
- [ ] **Step 6 : commit** `feat(1.22): deps mail + helper pur du corps de signalement de bug`

---

## Task 2 : I/O natif — `collectSupportMeta` + `contactSupport`

**Files:** `apps/mobile/src/lib/support.ts`, `apps/mobile/src/lib/__tests__/support.test.ts`

- [ ] **Step 1 : test du fallback** (mail indisponible → `Alert`, `MailComposer` mocké) — ajouter à
  `support.test.ts` :

```ts
import { Alert } from 'react-native';
import * as MailComposer from 'expo-mail-composer';
import { contactSupport } from '../support';

jest.mock('expo-mail-composer');

describe('contactSupport (fallback)', () => {
  it('affiche une Alert avec l’adresse si aucun client mail', async () => {
    (MailComposer.isAvailableAsync as jest.Mock).mockResolvedValue(false);
    const spy = jest.spyOn(Alert, 'alert').mockImplementation(() => {});
    const t = ((k: string) => k) as any;
    const res = await contactSupport('contact', t);
    expect(res).toEqual({ fallback: true });
    expect(spy).toHaveBeenCalled();
    spy.mockRestore();
  });
});
```

- [ ] **Step 2 : lancer → échec** `npm run test -w @wellness/mobile -- support` (`contactSupport` absent).
- [ ] **Step 3 : implémenter l'I/O** (ajouter à `support.ts`) :

```ts
import { Alert, Platform } from 'react-native';
import * as Application from 'expo-application';
import * as Device from 'expo-device';
import * as MailComposer from 'expo-mail-composer';
import { getAppLanguage } from '@/i18n';

/** Collecte les métadonnées techniques (non identifiantes). Jamais bloquante : valeurs manquantes → « — ». */
export function collectSupportMeta(): SupportMeta {
  return {
    appVersion: Application.nativeApplicationVersion ?? '—',
    buildVersion: Application.nativeBuildVersion ?? '—',
    osName: Device.osName ?? Platform.OS,
    osVersion: Device.osVersion ?? String(Platform.Version),
    deviceModel: Device.modelName ?? '—',
    language: getAppLanguage(),
  };
}

export type ContactKind = 'contact' | 'bug';
export type ContactResult = { ok: true } | { fallback: true };

/**
 * Ouvre le client mail natif pré-rempli (contact ou signalement de bug). Si aucun client mail
 * n'est disponible → fallback `Alert` affichant l'adresse (l'utilisateur peut la recopier).
 */
export async function contactSupport(kind: ContactKind, t: TFunction): Promise<ContactResult> {
  const isBug = kind === 'bug';
  const subject = t(isBug ? 'help.bug.subject' : 'help.contact.subject');
  const body = isBug ? formatBugReportBody(collectSupportMeta(), t) : undefined;

  if (!(await MailComposer.isAvailableAsync())) {
    Alert.alert(t('help.mailUnavailableTitle'), t('help.mailUnavailableBody', { email: SUPPORT_EMAIL }));
    return { fallback: true };
  }
  await MailComposer.composeAsync({ recipients: [SUPPORT_EMAIL], subject, body });
  return { ok: true };
}
```

> ⚠️ Vérifier au moment du code, contre https://docs.expo.dev/versions/v57.0.0/ : signatures
> `MailComposer.composeAsync` / `isAvailableAsync`, `Application.nativeApplicationVersion` /
> `nativeBuildVersion`, `Device.osName` / `osVersion` / `modelName`. Ajuster si l'API diffère.

- [ ] **Step 4 : lancer → succès** `npm run test -w @wellness/mobile -- support` + `npm run typecheck` + `npm run lint`.
- [ ] **Step 5 : commit** `feat(1.22): collecte métadonnées + ouverture mail natif (fallback Alert)`

---

## Task 3 : Composant `FaqItem` + écran `/help`

**Files:** `apps/mobile/src/components/FaqItem.tsx`, `apps/mobile/src/app/help.tsx`, `apps/mobile/src/app/__tests__/help-smoke.test.tsx`

- [ ] **Step 1 : `FaqItem.tsx`** — item contrôlé (le parent pilote `expanded`) :

```tsx
import { Pressable, StyleSheet, Text, View } from 'react-native';
import { Ionicons } from '@expo/vector-icons';
import { fontFamily } from '@/theme/fonts';
import { useTheme } from '@/theme/useTheme';

export function FaqItem({
  question, answer, expanded, onToggle,
}: { question: string; answer: string; expanded: boolean; onToggle: () => void }) {
  const { colors } = useTheme();
  return (
    <View style={[styles.card, { backgroundColor: colors.surface, borderColor: colors.border }]}>
      <Pressable
        accessibilityRole="button"
        accessibilityState={{ expanded }}
        accessibilityLabel={question}
        onPress={onToggle}
        style={styles.header}
      >
        <Text style={[styles.q, { color: colors.text }]}>{question}</Text>
        <Ionicons name={expanded ? 'chevron-down' : 'chevron-forward'} size={18} color={colors.textMuted} />
      </Pressable>
      {expanded ? <Text style={[styles.a, { color: colors.textMuted }]}>{answer}</Text> : null}
    </View>
  );
}

const styles = StyleSheet.create({
  card: { borderRadius: 14, borderWidth: 1, padding: 14, gap: 10 },
  header: { flexDirection: 'row', alignItems: 'center', gap: 10 },
  q: { flex: 1, fontFamily: fontFamily.bodySemi, fontSize: 15 },
  a: { fontFamily: fontFamily.body, fontSize: 14, lineHeight: 20 },
});
```

- [ ] **Step 2 : `help.tsx`** — accordéon **mono-ouverture** (`openIndex`) + 2 boutons d'action :

```tsx
import { useState } from 'react';
import { ScrollView, StyleSheet, Text, View } from 'react-native';
import { useTranslation } from 'react-i18next';
import { Button } from '@/components/Button';
import { FaqItem } from '@/components/FaqItem';
import { contactSupport } from '@/lib/support';
import { fontFamily } from '@/theme/fonts';
import { useTheme } from '@/theme/useTheme';

export default function HelpScreen() {
  const { t } = useTranslation();
  const { colors } = useTheme();
  const [openIndex, setOpenIndex] = useState<number | null>(null);
  const raw = t('help.faq.items', { returnObjects: true });
  // Défense : si la clé i18n manque (dev), returnObjects renvoie la chaîne de clé → éviter le crash de .map.
  const faq = (Array.isArray(raw) ? raw : []) as { q: string; a: string }[];

  return (
    <ScrollView style={{ backgroundColor: colors.background }} contentContainerStyle={styles.content}>
      <Text style={[styles.sectionTitle, { color: colors.textMuted }]}>{t('help.faq.title')}</Text>
      <View style={styles.stack}>
        {faq.map((item, i) => (
          <FaqItem
            key={i}
            question={item.q}
            answer={item.a}
            expanded={openIndex === i}
            onToggle={() => setOpenIndex((cur) => (cur === i ? null : i))}
          />
        ))}
      </View>

      <Text style={[styles.sectionTitle, { color: colors.textMuted, marginTop: 28 }]}>
        {t('help.contact.title')}
      </Text>
      <Text style={[styles.intro, { color: colors.textMuted }]}>{t('help.contact.intro')}</Text>
      <View style={styles.stack}>
        <Button label={t('help.contact.contactButton')} onPress={() => void contactSupport('contact', t)} />
        <Button
          label={t('help.contact.bugButton')}
          variant="ghost"
          onPress={() => void contactSupport('bug', t)}
        />
      </View>
    </ScrollView>
  );
}

const styles = StyleSheet.create({
  content: { padding: 20 },
  sectionTitle: {
    fontFamily: fontFamily.bodySemi, fontSize: 13, textTransform: 'uppercase',
    letterSpacing: 0.5, marginBottom: 10,
  },
  intro: { fontFamily: fontFamily.body, fontSize: 14, lineHeight: 20, marginBottom: 10 },
  stack: { gap: 10 },
});
```

> Vérifier le nom exact de la prop `variant` du composant [Button](../../apps/mobile/src/components/Button.tsx)
> (`ghost` utilisé ailleurs dans `settings.tsx`).

- [ ] **Step 3 : smoke test** (`help-smoke.test.tsx`) — l'écran rend FAQ + 2 boutons. ⚠️ **Ce test tourne
  AVANT que la Task 4 ait ajouté les clés i18n** : il **DOIT donc mocker `react-i18next`** (ne PAS importer le
  vrai `@/i18n`, sinon `t('help.faq.items', {returnObjects:true})` renvoie la chaîne de clé et `faq.map` ne
  planterait que grâce au garde `Array.isArray` — mais rien ne serait rendu). Le mock fournit un `t` qui
  renvoie un **tableau** `[{q,a}]` pour `help.faq.items` et les libellés pour les autres clés. **Mocker aussi
  `@/theme/useTheme`** (comme les smoke tests existants du dossier `__tests__`, pour ne pas dépendre de la
  chaîne thème/réglages). Modèle : voir un smoke test récent — mais NE PAS copier ceux qui font
  `import '@/i18n'`. Vérifier qu'au moins une question et les 2 boutons sont rendus.

- [ ] **Step 4 : lancer → succès** `npm run test -w @wellness/mobile` + `npm run typecheck` + `npm run lint`.
- [ ] **Step 5 : commit** `feat(1.22): écran Aide & support (FAQ accordéon + contact/bug)`

---

## Task 4 : Route + entrée Réglages + i18n

**Files:** `apps/mobile/src/app/_layout.tsx`, `apps/mobile/src/app/settings.tsx`, `apps/mobile/src/i18n/locales/fr.json`, `en.json`

- [ ] **Step 1 : enregistrer la route** dans `_layout.tsx` (même patron que `account-delete`/`profile`) :

```tsx
<Stack.Screen
  name="help"
  options={{
    presentation: 'modal',
    headerShown: true,
    title: t('help.title'),
    headerStyle: { backgroundColor: colors.surface },
    headerTitleStyle: { color: colors.text, fontFamily: typography.title.fontFamily },
    headerTintColor: colors.accent,
  }}
/>
```

- [ ] **Step 2 : section Réglages** — dans `settings.tsx`, ajouter une section « Aide & support » (ex. juste
  avant la section Données/export), sur le patron des autres sections :

```tsx
<Text style={[styles.sectionTitle, { color: colors.textMuted, marginTop: 28 }]}>
  {t('settings.help.title')}
</Text>
<View style={styles.stack}>
  <Button label={t('settings.help.button')} variant="ghost" onPress={() => router.push('/help')} />
</View>
```

- [ ] **Step 3 : i18n FR** (`fr.json`) — ajouter (JSON valide, à la bonne place hiérarchique) :

```jsonc
// settings.help
"help": { "title": "Aide & support", "button": "Aide & support" }
```
```jsonc
// racine "help" (nouvel objet de 1er niveau, à côté des autres écrans)
"help": {
  "title": "Aide & support",
  "faq": {
    "title": "Questions fréquentes",
    "items": [
      { "q": "L'application fonctionne-t-elle hors connexion ?", "a": "Oui. Tes données sont enregistrées sur l'appareil et se synchronisent automatiquement dès qu'une connexion est disponible." },
      { "q": "Mes données sont-elles synchronisées entre mes appareils ?", "a": "Oui : connecte-toi avec le même compte, tes données se synchronisent en arrière-plan." },
      { "q": "Comment activer ou désactiver un pilier (Muscu / Course / Nutrition) ?", "a": "Dans Réglages → Piliers actifs. Les onglets des piliers désactivés sont masqués et réapparaissent à la réactivation." },
      { "q": "Comment changer les unités ou la langue ?", "a": "Dans Réglages → Unités (métrique/impérial) et Langue (français/anglais)." },
      { "q": "Comment exporter mes données ?", "a": "Dans Réglages → Exporter mes données : tu récupères une copie complète de tes données au format JSON." },
      { "q": "Comment supprimer mon compte ?", "a": "Dans Réglages → Zone de danger → Supprimer mon compte. Un délai de grâce de 30 jours permet de revenir en arrière." },
      { "q": "L'application est-elle gratuite ?", "a": "Oui, l'application est entièrement gratuite." }
    ]
  },
  "contact": {
    "title": "Nous contacter",
    "intro": "Une question ou un problème ? Écris-nous, ton client mail s'ouvrira avec un message pré-rempli.",
    "contactButton": "Nous contacter",
    "bugButton": "Signaler un bug",
    "subject": "[Wellness] Question / contact"
  },
  "bug": {
    "subject": "[Wellness] Signalement de bug",
    "prompt": "Décris le problème rencontré (étapes, ce qui était attendu) :",
    "techHeader": "Informations techniques (à conserver)",
    "appLabel": "App", "buildLabel": "build", "systemLabel": "Système",
    "deviceLabel": "Appareil", "languageLabel": "Langue"
  },
  "mailUnavailableTitle": "Aucune application mail",
  "mailUnavailableBody": "Aucune application mail n'est configurée. Écris-nous à : {{email}}"
}
```

- [ ] **Step 4 : i18n EN** (`en.json`) — parité stricte (mêmes clés). Extraits : `help.title` = "Help &
  support" ; `faq.title` = "Frequently asked questions" ; `contact.contactButton` = "Contact us" ;
  `contact.bugButton` = "Report a bug" ; `contact.subject` = "[Wellness] Question / contact" ;
  `bug.subject` = "[Wellness] Bug report" ; `bug.techHeader` = "Technical information (please keep)" ;
  `bug.prompt` = "Describe the problem (steps, what you expected):" ; `mailUnavailableBody` = "No mail app is
  configured. Write to us at: {{email}}". Traduire les 7 Q/R FAQ à l'identique en sens.

- [ ] **Step 5 : vérifier** `npm run typecheck` + `npm run lint` + `npm run test -w @wellness/mobile` verts ;
  JSON i18n valides.
- [ ] **Step 6 : commit** `feat(1.22): route /help + entrée Réglages + i18n FR/EN`

---

## Task 5 : Parité i18n + suite complète + clôture

- [ ] **Step 1 : parité FR/EN** — vérifier que toutes les clés `settings.help.*` et `help.*` existent des deux
  côtés (mêmes chemins, `help.faq.items` = 7 entrées de part et d'autre).
- [ ] **Step 2 : suite complète** — `npm run typecheck` + `npm run lint` + `npm run test` (shared + mobile) verts.
- [ ] **Step 3 : revue finale** (offline OK ; aucune chaîne en dur ; `SUPPORT_EMAIL` centralisé ; découpage
  pur/I-O respecté ; fallback mail géré ; accessibilité des items FAQ).
- [ ] **Step 4 : clôture** — `TODO.md` (Code `[x]`, US → recette), `CHANGELOG.md`, statut roadmap **1.22 →
  ✅** (compteurs V0.8), push via `/commit`.

---

## Notes de test

- **`formatBugReportBody`** : pur, testé (présence des métadonnées + déterminisme).
- **`contactSupport`** : test du **chemin fallback** (`isAvailableAsync` → false → `Alert` + `{ fallback: true }`),
  `expo-mail-composer` mocké. Le chemin nominal (composeAsync) reste vérifié en recette device.
- **`collectSupportMeta`** : pas de test unitaire (I/O natif) — vérifié en recette.
- **Écran `/help`** : smoke test (FAQ + 2 boutons rendus).
- **Recette manuelle** (spec §9) : accès (offline), FAQ dépli/repli, contact (corps vide), bug (bloc technique),
  bloc effaçable, mail indisponible → Alert, i18n EN.

## Points d'attention

- **Modules natifs** (`expo-mail-composer`, `expo-application`) → **nouveau dev build requis** (pas de simple
  reload Metro). À intégrer au prochain build EAS dev.
- **`SUPPORT_EMAIL` = placeholder** : à remplacer par la vraie adresse **avant** le build de recette/prod
  (action Florian).
- **Docs Expo SDK 57** : confirmer les signatures des 3 modules (voir Task 2, Step 3) avant de figer le code.
- **Aucune donnée personnelle** transmise silencieusement : le bloc technique est visible/effaçable, pas d'UUID,
  pas de logs.
- **Ordre** : helper pur (1) → I/O natif (2) → écran (3) → route + Réglages + i18n (4) → clôture (5).
