# Plan d'implémentation — Notifications (V0.6, 2.6/2.8/1.17)

> Exécuter avec `superpowers:subagent-driven-development`. Spec validée :
> [docs/specs/functional/us/2.6-2.8-1.17-notifications.md](../specs/functional/us/2.6-2.8-1.17-notifications.md).
> Branche : `feature/notifications-v0.6`. Android-first, offline. **Nouveau build requis** (`expo-notifications`).

**Objectif :** rappel streak en danger (local), Ne pas déranger (fenêtre configurable + max/jour), gestion par type.

**Architecture :** logique pure dans `@wellness/shared` (prefs + helpers, testée) → couche native `lib/notifications.ts`
(expo-notifications) → hook `useStreakReminderScheduler` (AppState + activeToday/prefs) → section Réglages → init `_layout`.

## Maquette
Fournie (section « Notifications » des Réglages) : `design/notifications/notifications.html`. Validée avec le plan.

## Fichiers touchés
- Modifier : `packages/shared/src/settings.ts` (type `notifications` → `NotificationPrefs`) + `settings.test.ts` (2 tests à adapter).
- Créer : `packages/shared/src/notifications.ts` + `notifications.test.ts` ; exporter dans `index.ts`.
- Modifier : `apps/mobile/src/data/repositories/settings-repository.ts` (read/write via `parseNotificationPrefs`).
- Créer : `apps/mobile/src/lib/notifications.ts` (wrapper natif).
- Créer : `apps/mobile/src/data/repositories/notification-repository.ts` (`useNotificationPrefs`, `updateNotificationPrefs`, `useStreakReminderScheduler`).
- Modifier : `apps/mobile/src/app/settings.tsx` (section Notifications) + `_layout.tsx` (init + scheduler mount).
- Modifier : `apps/mobile/package.json` (`expo install`), `apps/mobile/app.json` (plugin + permission).
- Modifier : `fr.json`/`en.json` (`settings.notifications.*`, `notifications.streakDanger.*`).

---

### Tâche 1 — Prefs + helpers purs (`@wellness/shared`) — TDD

**Fichiers :** créer `notifications.ts` + `notifications.test.ts` ; modifier `settings.ts` + `settings.test.ts` ; export `index.ts`.

- [ ] **Étape 1 — Test rouge** (`notifications.test.ts`) :
  - `defaultNotificationPrefs()` = `{ streakDanger:true, reminderHour:20, dndEnabled:true, dndStartHour:22, dndEndHour:7, maxPerDay:3 }` ;
  - `parseNotificationPrefs` : `null`/`{}`/ancien `Record<string,boolean>` → défauts ; heures hors [0,23] bornées/rejetées → défaut ; champs partiels complétés ; types invalides ignorés ;
  - `isWithinDnd(hour, prefs)` : fenêtre simple (`start<end`) **et** enjambant minuit (`start>end` → `hour>=start || hour<end`) ; `dndEnabled:false` → toujours `false` ;
  - `shouldScheduleStreakReminder({ enabled, activeToday, nowHour, reminderHour, prefs })` → `true` seulement si
    `enabled && !activeToday && nowHour < reminderHour && !isWithinDnd(reminderHour, prefs)` ;
  - `canScheduleMore(countToday, prefs)` → `countToday < maxPerDay`.
- [ ] **Étape 2 — Vérifier l'échec.**
- [ ] **Étape 3 — Implémenter** `notifications.ts` (types + `defaultNotificationPrefs` + `parseNotificationPrefs`
  + helpers). Commenter que `reminderHour=20` est **hors** DND par défaut `[22,7)` (intentionnel).
- [ ] **Étape 4 — `settings.ts`** : remplacer `notifications: z.record(z.string(), z.boolean())` par le schéma
  `NotificationPrefs` (objet, `.default(defaultNotificationPrefs())`). **Adapter `settings.test.ts`** (les 2 tests
  qui affirmaient `Record<string,boolean>`).
- [ ] **Étape 5 — Vert** (`npm run test`). Export `index.ts`.
- [ ] **Étape 6 — Commit** : `feat(shared): NotificationPrefs + helpers de planification (notifications)`.

### Tâche 2 — Dépendance native + wrapper `lib/notifications.ts`

**Fichiers :** `package.json` (via `expo install`), `app.json`, créer `apps/mobile/src/lib/notifications.ts`.

- [ ] **Étape 1 — Installer** : depuis `apps/mobile`, `npx expo install expo-notifications` (version alignée **SDK 57**,
  `~57.x`). Vérifier `npx expo install --check`.
- [ ] **Étape 2 — app.json** : ajouter le plugin `expo-notifications` (icône/couleur par défaut si utile) et la
  permission `android.permission.POST_NOTIFICATIONS` (à côté de `RECEIVE_BOOT_COMPLETED`). Ne PAS toucher owner/projectId.
- [ ] **Étape 3 — Wrapper** (API **SDK 57** — se référer à https://docs.expo.dev/versions/v57.0.0/sdk/notifications/) :
  - `ensurePermissionAndChannel()` : `getPermissionsAsync` → `requestPermissionsAsync` si non déterminé ; crée le
    **canal Android** (`setNotificationChannelAsync('reminders', …)`). Retourne `granted: boolean`.
  - `scheduleStreakReminder(date: Date, content)` : `scheduleNotificationAsync({ content, trigger })` avec le
    **trigger date** conforme SDK 57 ; **identifiant stable** (`STREAK_REMINDER_ID`) pour l'idempotence.
  - `cancelStreakReminder()` : `cancelScheduledNotificationAsync(STREAK_REMINDER_ID)` (ou annulation par tag).
  - Configurer le handler d'affichage au premier plan si nécessaire. Gère permission refusée (no-op, pas de throw).
- [ ] **Étape 4 — typecheck/lint verts.**
- [ ] **Étape 5 — Commit** : `feat(mobile): expo-notifications + wrapper natif (permission, canal, schedule)`.

### Tâche 3 — Repository + scheduler

**Fichier :** créer `apps/mobile/src/data/repositories/notification-repository.ts`.

- [ ] **Étape 1 — `useNotificationPrefs()`** : `const { settings } = useSettings()` → `parseNotificationPrefs(settings?.notifications ?? null)`.
- [ ] **Étape 2 — `updateNotificationPrefs(patch: Partial<NotificationPrefs>)`** : lit prefs courantes, applique le
  patch, `updateSettings({ notifications: next })`.
- [ ] **Étape 3 — `useStreakReminderScheduler()`** (monté une fois, ex. dans `_layout`) :
  - lit `useStreakData().activeToday` + `useNotificationPrefs()` ;
  - fonction `apply()` : si permission accordée → `shouldScheduleStreakReminder({...})` vrai →
    `scheduleStreakReminder(dateAujourdHui(reminderHour), contenu i18n)` ; sinon `cancelStreakReminder()` ;
  - appelée : (a) au montage / quand `activeToday`|prefs changent ; (b) **au retour au premier plan** via
    `AppState.addEventListener('change', s => s === 'active' && apply())` (nettoyer l'abonnement au démontage).
  - Idempotent (id stable → au plus un rappel).
- [ ] **Étape 4 — typecheck/lint verts.**
- [ ] **Étape 5 — Commit** : `feat(mobile): planification du rappel streak (prefs + AppState)`.

### Tâche 4 — Section Réglages + i18n

**Fichiers :** `settings.tsx` ; `fr.json`/`en.json`.

- [ ] **Étape 1 — i18n** `settings.notifications.{ title, streakReminder, reminderTime, dnd, dndStart, dndEnd, hint,
  permissionDenied }` + `notifications.streakDanger.{ title, body }` en FR **et** EN (parité).
- [ ] **Étape 2 — Section Notifications** (pattern piliers : `Card` + lignes) :
  - `Switch` **Rappel streak** (`streakDanger`) ;
  - **sélecteur d'heure** de rappel (`reminderHour`) — **stepper JS 0-23** (composant `HourStepper` réutilisable, pur, sans dépendance native) ;
  - `Switch` **Ne pas déranger** (`dndEnabled`) + steppers **début**/**fin** (`dndStartHour`/`dndEndHour`) ;
  - chaque changement → `updateNotificationPrefs(...)` (persiste) **et** re-`apply()` (via le hook/scheduler).
  - Si permission refusée → message `permissionDenied` (informatif, non bloquant).
- [ ] **Étape 3 — typecheck/lint/tests verts, parité i18n.**
- [ ] **Étape 4 — Commit** : `feat(mobile): section Notifications dans les Réglages (2.8/1.17)`.

### Tâche 5 — Init app (permission + canal + scheduler)

**Fichier :** `_layout.tsx`.

- [ ] **Étape 1 — Effet d'init** après settings prêts (session active) : `ensurePermissionAndChannel()` (une fois).
- [ ] **Étape 2 — Monter `useStreakReminderScheduler()`** dans `RootNavigator` (après settings), pour la
  (re)planification à l'ouverture / au premier plan / après activité.
- [ ] **Étape 3 — Vérifier** : au démarrage, permission demandée ; rappel planifié si inactif ; annulé si actif ;
  changements de prefs appliqués ; pas de crash si permission refusée.
- [ ] **Étape 4 — typecheck/lint/tests verts.**
- [ ] **Étape 5 — Commit** : `feat(mobile): init notifications + montage du scheduler (V0.6)`.

## Definition of Done (rappel spec §6)
- [ ] Helpers purs testés (défauts, bornes heures, DND enjambant minuit, règle streak, max/jour) ; `settings.test.ts` adapté.
- [ ] `expo-notifications` + permission + canal + plugin app.json ; permission refusée gérée.
- [ ] Rappel streak : ponctuel si inactif+activé+heure non passée+hors DND ; annulé si actif ; ≤ 1 en attente ;
      replanifié à l'ouverture (AppState) et après activité.
- [ ] DND configurable (défaut 22-7) + max/jour (défaut 3) ; section Réglages (switches + steppers d'heure).
- [ ] `notifications` (schéma enrichi) : parse tolérant ; aucune migration ; i18n FR/EN parité.
- [ ] typecheck/lint/tests verts. **Nouveau build** → recette device. Revues (conformité + qualité) + PR relue.

## Notes
- **API expo-notifications SDK 57** : suivre la doc versionnée (structure `trigger`, `setNotificationChannelAsync`).
  Ne pas copier d'exemples SDK ≤ 50.
- **Parse fallback** : `parseNotificationPrefs(parseJsonColumn(row.notifications, null))` (défauts garantis), plutôt
  que de laisser `{}` en fallback d'un schéma strict.
- **AppState** : `import { AppState } from 'react-native'` — nouveau (seul `supabase.ts` l'utilisait). Nettoyer l'abonnement.
- **Réutilisation** : `settings-repository` (persistance), pattern Réglages, `useStreakData`.
