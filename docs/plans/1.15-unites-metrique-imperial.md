# US 1.15 — Unités métrique / impérial — Plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Câbler le réglage `settings.units` (metric / imperial) sur **tout l'affichage ET toute la saisie** des grandeurs physiques (poids charges + corporel, distance, allure, taille), **sans toucher au stockage** (toujours SI).

**Architecture:** Approche A validée en spec. La **logique pure** (conversions, parsing, formatage M:SS, ft/in) vit dans [`packages/shared/src/units.ts`](../../packages/shared/src/units.ts) (déjà partiellement présente, tout testé Vitest). Un **hook mince** `apps/mobile/src/hooks/useUnits.ts` lie cette logique au réglage (`useSettings().units`) et à la **locale i18n** (formatage des nombres via `Intl.NumberFormat`), et renvoie des formateurs/parseurs déjà liés. Les écrans consomment le hook et ne contiennent **plus aucune unité codée en dur**. Le symbole d'unité **sort des chaînes i18n**.

**Tech Stack:** identique au projet — `@wellness/shared` (TS pur + Vitest), `apps/mobile` (Expo SDK 57, Expo Router, i18next, PowerSync `useQuery` réactif via `useSettings()`), `jest-expo` pour le smoke test du hook. Aucune nouvelle dépendance.

**Spec :** [1.15-unites-metrique-imperial.md](../specs/functional/us/1.15-unites-metrique-imperial.md) · **Patterns à copier :** helpers existants de `units.ts`, composant [`TextField`](../../apps/mobile/src/components/TextField.tsx), écrans muscu/running/nutrition livrés.

**Branche :** `feature/1.15-unites-metrique-imperial`, depuis `dev` (déjà créée ; spec commitée `c2c0e84`).

**Périmètre (rappel spec §1) :** poids charges (séance, résumé, historique, progression, détail programme), poids corporel (onboarding, profil, profil nutritionnel, stats/pesée), distance running (course en direct, résumé), allure (dérivée distance), taille (onboarding, profil, profil nutritionnel).

**Hors périmètre :** aucun changement de stockage / migration / sync ; aucun couplage langue↔unité ; pas de nouvel écran. **Pas de checkpoint 🔴 cloud** (US 100 % client, validable sans Damien).

---

## Rappels de patron

- **Stockage toujours SI** (kg, km, cm). On convertit **en lecture** (affichage) et **à l'enregistrement** (saisie). PowerSync/Supabase non touchés.
- **Réactivité** : le réglage est lu via `useSettings()` (réactif PowerSync) ; changer l'unité rafraîchit les écrans sans redémarrage. Le hook n'ajoute pas d'état, il dérive de `useSettings()` + `useTranslation()`.
- **Formatage des nombres** selon la **locale i18n courante** (virgule FR / point EN) → **`Intl.NumberFormat(locale, …)`** dans le hook, **jamais** `toFixed` dans le JSX.
- **Anti-dérive d'arrondi (spec §3, point de vigilance revue #3)** : pour chaque **champ éditable**, mémoriser à l'ouverture la **chaîne affichée initiale** ; à l'enregistrement, **ne reconvertir/écrire que si le texte a réellement changé**. Sinon on **réécrit la valeur SI stockée d'origine, inchangée** (jamais `parse(display(SI))`). Détail par champ en Phase D.
- **Vide / invalide** en saisie → `null` (pas de `0` implicite).
- **Découplage i18n** : plus aucune unité (`kg`/`km`/`cm`/`lb`/`mi`) dans les **valeurs i18n** ni le **JSX** des surfaces §1. Exception explicite : la clé `settings.units.hint` (texte pédagogique qui *décrit* le réglage) reste littérale.

## Constantes & conversions (déjà dans `units.ts`)

`LB_PER_KG = 2.2046226218`, `MI_PER_KM = 0.6213711922`. Existant : `kgToLb`/`lbToKg`, `kmToMi`/`miToKm`, `unitSymbol` (`{metric:{weight:'kg',distance:'km'}, imperial:{weight:'lb',distance:'mi'}}`), `UnitSystem`, `displayWeight`/`displayDistance` (conservés tels quels, non locale-aware — **non utilisés par le hook**). Nouvelle constante à ajouter : `CM_PER_IN = 2.54`.

## Structure des fichiers

**Shared :** MAJ [`packages/shared/src/units.ts`](../../packages/shared/src/units.ts) (+ ajouts au `units.test.ts`) ; export via `index.ts` déjà en place (barrel — vérifier que les nouveaux symboles sont réexportés).
**Hook mobile :** Create `apps/mobile/src/hooks/useUnits.ts` (+ `apps/mobile/src/hooks/__tests__/useUnits.test.tsx`).
**i18n :** MAJ `apps/mobile/src/i18n/locales/fr.json` **et** `en.json` (miroir).
**Écrans (affichage) :** `app/workout.tsx`, `app/workout-summary.tsx`, `app/history/[id].tsx`, `app/progress/index.tsx`, `app/programs/[id].tsx`, `app/run/active.tsx`, `app/run/summary.tsx`, `app/(tabs)/nutrition.tsx`, `app/nutrition-profile.tsx`, `app/nutrition-stats.tsx`.
**Écrans (saisie) :** `app/workout.tsx` (charge de série), `app/(onboarding)/infos.tsx`, `app/profile.tsx`, `app/nutrition-profile.tsx` (poids + taille), `app/run/summary.tsx` (distance manuelle).

---

## Phase A — `packages/shared` : logique pure (TDD)

Tout est **pur et testé Vitest** avant toute ligne mobile. Commande de test : `npm run test -w @wellness/shared` (ou `npx vitest run` depuis `packages/shared`).

### Task 1 : Taille — conversions cm ↔ ft/in

**Files:** Modify `packages/shared/src/units.ts` ; Test `packages/shared/src/units.test.ts`.

- [ ] **Step 1 — Tests rouges.** Ajouter dans `units.test.ts` :

```ts
import { cmToFtIn, ftInToCm, CM_PER_IN } from './units';

describe('taille cm <-> ft/in', () => {
  it('cmToFtIn arrondit au pouce le plus proche', () => {
    expect(cmToFtIn(178)).toEqual({ feet: 5, inches: 10 }); // 178/2.54 ≈ 70.08 in -> 70 in
    expect(cmToFtIn(152.4)).toEqual({ feet: 5, inches: 0 }); // 60 in pile
  });
  it('gère le passage à 12 pouces (report sur le pied)', () => {
    // 5 ft 11.6 in doit arrondir à 6 ft 0 in, pas 5 ft 12 in
    expect(cmToFtIn(182)).toEqual({ feet: 5, inches: 12 - 1 }); // 71.65 -> 72 in -> 6 ft 0 in
  });
  it('ftInToCm est l’inverse (aux arrondis près)', () => {
    expect(ftInToCm(5, 10)).toBeCloseTo(177.8, 1);
    expect(ftInToCm(6, 0)).toBeCloseTo(182.88, 1);
  });
  it('round-trip cm -> ft/in -> cm tolère l’arrondi au pouce (<= ~1.3 cm)', () => {
    for (const cm of [150, 165, 172, 178, 190]) {
      const { feet, inches } = cmToFtIn(cm);
      expect(Math.abs(ftInToCm(feet, inches) - cm)).toBeLessThanOrEqual(1.3);
    }
  });
});
```

> ⚠️ Le 2ᵉ test ci-dessus est volontairement piégeux : `cmToFtIn(182)` → 71.65 in → **arrondi 72 in → 6 ft 0 in** (report). Corriger l'assertion attendue en `{ feet: 6, inches: 0 }` lors de l'écriture (la version `12 - 1` est un leurre à remplacer). Le report de retenue (`inches === 12 → feet+1, inches=0`) est le cœur du test.

- [ ] **Step 2 — Vérifier l'échec.** `npm run test -w @wellness/shared` → FAIL (`cmToFtIn is not a function`).
- [ ] **Step 3 — Implémentation** dans `units.ts` :

```ts
export const CM_PER_IN = 2.54;

/** cm -> pieds + pouces (pouce le plus proche, avec report de retenue à 12). */
export function cmToFtIn(cm: number): { feet: number; inches: number } {
  const totalInches = Math.round(cm / CM_PER_IN);
  return { feet: Math.floor(totalInches / 12), inches: totalInches % 12 };
}

/** pieds + pouces -> cm (SI). */
export function ftInToCm(feet: number, inches: number): number {
  return (feet * 12 + inches) * CM_PER_IN;
}
```

- [ ] **Step 4 — Vérifier le succès.** `npm run test -w @wellness/shared` → PASS (après correction de l'assertion leurre en `{feet:6,inches:0}`).
- [ ] **Step 5 — Commit.** `test(shared): conversions taille cm <-> ft/in (units)`

### Task 2 : Allure — conversion s/km ↔ s/unité + formatage M:SS

**Files:** Modify `packages/shared/src/units.ts` ; Test `units.test.ts`.

- [ ] **Step 1 — Tests rouges :**

```ts
import { paceToSystem, formatPaceMMSS } from './units';

describe('allure', () => {
  it('metric : s/km inchangé', () => {
    expect(paceToSystem(300, 'metric')).toBe(300);
  });
  it('imperial : s/mi = s/km / MI_PER_KM (plus de temps au mile)', () => {
    // 5:00/km (300 s) -> ~8:03/mi (482.8 s)
    expect(paceToSystem(300, 'imperial')).toBeCloseTo(300 / 0.6213711922, 1);
  });
  it('formatPaceMMSS formate M:SS, pad des secondes', () => {
    expect(formatPaceMMSS(300, '—')).toBe('5:00');
    expect(formatPaceMMSS(order => order, '—')); // leurre à retirer
    expect(formatPaceMMSS(483, '—')).toBe('8:03');
  });
  it('formatPaceMMSS renvoie le placeholder si null/<=0/NaN', () => {
    expect(formatPaceMMSS(null, '—')).toBe('—');
    expect(formatPaceMMSS(0, '—')).toBe('—');
    expect(formatPaceMMSS(Number.NaN, '—')).toBe('—');
  });
});
```

> Retirer la ligne « leurre » `order => order` lors de l'écriture (placeholder pour forcer la relecture).

- [ ] **Step 2 — Échec** attendu (`paceToSystem is not a function`).
- [ ] **Step 3 — Implémentation :**

```ts
/** Allure s/km -> s par unité affichée (mile en impérial). */
export function paceToSystem(sPerKm: number, system: UnitSystem): number {
  return system === 'imperial' ? sPerKm / MI_PER_KM : sPerKm;
}

/** Secondes par unité -> "M:SS" ; placeholder si nul/négatif/NaN. */
export function formatPaceMMSS(secondsPerUnit: number | null, noData: string): string {
  if (secondsPerUnit == null || !Number.isFinite(secondsPerUnit) || secondsPerUnit <= 0) {
    return noData;
  }
  const total = Math.round(secondsPerUnit);
  const m = Math.floor(total / 60);
  const s = total % 60;
  return `${m}:${s.toString().padStart(2, '0')}`;
}
```

- [ ] **Step 4 — Succès.**
- [ ] **Step 5 — Commit.** `test(shared): conversion et formatage d'allure (min/km, min/mi)`

### Task 3 : Parseurs de saisie (poids, distance, taille) — texte → SI

**Files:** Modify `packages/shared/src/units.ts` ; Test `units.test.ts`.

- [ ] **Step 1 — Tests rouges :**

```ts
import { parseWeightToKg, parseDistanceToKm, heightPartsToCm } from './units';

describe('parseurs de saisie -> SI', () => {
  it('poids metric : accepte virgule et point, renvoie des kg', () => {
    expect(parseWeightToKg('72,5', 'metric')).toBeCloseTo(72.5, 5);
    expect(parseWeightToKg('72.5', 'metric')).toBeCloseTo(72.5, 5);
  });
  it('poids imperial : lb saisis -> kg stockés', () => {
    expect(parseWeightToKg('160', 'imperial')).toBeCloseTo(160 / 2.2046226218, 4);
  });
  it('distance imperial : mi saisis -> km stockés', () => {
    expect(parseDistanceToKm('3,1', 'imperial')).toBeCloseTo(3.1 / 0.6213711922, 4);
  });
  it('vide / invalide -> null (pas de 0)', () => {
    expect(parseWeightToKg('', 'metric')).toBeNull();
    expect(parseWeightToKg('   ', 'metric')).toBeNull();
    expect(parseWeightToKg('abc', 'metric')).toBeNull();
    expect(parseDistanceToKm('', 'imperial')).toBeNull();
  });
  it('heightPartsToCm : deux champs ft/in -> cm ; vide -> null', () => {
    expect(heightPartsToCm('5', '10', 'imperial')).toBeCloseTo(177.8, 1);
    expect(heightPartsToCm('178', '', 'metric')).toBeCloseTo(178, 5); // metric = champ unique cm dans le 1er arg
    expect(heightPartsToCm('', '', 'imperial')).toBeNull();
    expect(heightPartsToCm('', '', 'metric')).toBeNull();
  });
  it('round-trip kg -> lb affiché (1 déc) -> kg, dérive <= 0.1 kg', () => {
    const kg = 72.5;
    const lbRounded = Number((kg * 2.2046226218).toFixed(1)); // ce que l'UI afficherait
    expect(Math.abs(parseWeightToKg(String(lbRounded), 'imperial')! - kg)).toBeLessThanOrEqual(0.1);
  });
});
```

- [ ] **Step 2 — Échec.**
- [ ] **Step 3 — Implémentation :**

```ts
/** Parse un nombre tolérant (virgule ou point) ; null si vide/invalide/<=0 non requis ici. */
function parseNumberLoose(text: string): number | null {
  const t = text.trim().replace(',', '.');
  if (t === '') return null;
  const n = Number(t);
  return Number.isFinite(n) ? n : null;
}

/** Texte saisi dans l'unité `system` -> kg (SI) ; null si vide/invalide. */
export function parseWeightToKg(text: string, system: UnitSystem): number | null {
  const n = parseNumberLoose(text);
  if (n === null) return null;
  return system === 'imperial' ? lbToKg(n) : n;
}

/** Texte saisi dans l'unité `system` -> km (SI) ; null si vide/invalide. */
export function parseDistanceToKm(text: string, system: UnitSystem): number | null {
  const n = parseNumberLoose(text);
  if (n === null) return null;
  return system === 'imperial' ? miToKm(n) : n;
}

/**
 * Deux champs de taille -> cm (SI).
 * - metric  : `a` = cm (champ unique), `b` ignoré.
 * - imperial: `a` = pieds, `b` = pouces.
 * Renvoie null si aucune valeur exploitable.
 */
export function heightPartsToCm(a: string, b: string, system: UnitSystem): number | null {
  if (system === 'metric') {
    return parseNumberLoose(a); // déjà en cm
  }
  const ft = parseNumberLoose(a);
  const inch = parseNumberLoose(b);
  if (ft === null && inch === null) return null;
  return ftInToCm(ft ?? 0, inch ?? 0);
}
```

- [ ] **Step 4 — Succès.**
- [ ] **Step 5 — Commit.** `feat(shared): parseurs de saisie unités -> SI (poids, distance, taille)`

> **Note revue #1 (taille métrique) :** confirmé — **pas de helper dédié** au parsing cm métrique ; `heightPartsToCm` réutilise `parseNumberLoose` (le champ métrique est un simple entier cm). Le hook exposera `heightPartsToCm` uniformément.

---

## Phase B — Hook mobile `useUnits()`

### Task 4 : `useUnits()` — liaison réglage + locale, formateurs & parseurs liés

**Files:** Create `apps/mobile/src/hooks/useUnits.ts`.

Le hook **ne contient aucune conversion** : il lie `units.ts` au réglage (`useSettings().units`, défaut `'metric'`) et à la locale (`i18n.language`), applique le **formatage locale** via `Intl.NumberFormat`, et renvoie l'API consommée par les écrans.

- [ ] **Step 1 — Implémentation :**

```ts
import { useMemo } from 'react';
import { useTranslation } from 'react-i18next';
import {
  unitSymbol,
  kgToLb,
  kmToMi,
  cmToFtIn,
  paceToSystem,
  formatPaceMMSS,
  parseWeightToKg as parseWeightToKgPure,
  parseDistanceToKm as parseDistanceToKmPure,
  heightPartsToCm as heightPartsToCmPure,
  type UnitSystem,
} from '@wellness/shared';
import { useSettings } from '@/data/repositories/settings-repository';

export function useUnits() {
  const { settings } = useSettings();
  const { t, i18n } = useTranslation();
  const system: UnitSystem = settings?.units ?? 'metric';
  const locale = i18n.language;

  return useMemo(() => {
    const symbols = unitSymbol[system];
    const nf = (min: number, max: number) =>
      new Intl.NumberFormat(locale, { minimumFractionDigits: min, maximumFractionDigits: max });
    const nf1 = nf(1, 1); // poids : 1 décimale
    const nf2 = nf(2, 2); // distance : 2 décimales
    const nf0 = nf(0, 0); // taille cm : entier
    const noData = t('running.active.noData');

    return {
      system,
      weightSymbol: symbols.weight,
      distanceSymbol: symbols.distance,

      /** kg (SI) -> "72,5 kg" / "159,6 lb". */
      formatWeight: (kg: number | null | undefined): string => {
        if (kg == null) return '—';
        const v = system === 'imperial' ? kgToLb(kg) : kg;
        return `${nf1.format(v)} ${symbols.weight}`;
      },
      /** km (SI) -> "5,20 km" / "3,23 mi". */
      formatDistance: (km: number | null | undefined): string => {
        if (km == null) return '—';
        const v = system === 'imperial' ? kmToMi(km) : km;
        return `${nf2.format(v)} ${symbols.distance}`;
      },
      /** cm (SI) -> "178 cm" / "5 ft 10 in". */
      formatHeight: (cm: number | null | undefined): string => {
        if (cm == null) return '—';
        if (system === 'imperial') {
          const { feet, inches } = cmToFtIn(cm);
          return `${feet} ft ${inches} in`;
        }
        return `${nf0.format(cm)} cm`;
      },
      /** s/km (SI) -> "5:00 /km" / "8:03 /mi". */
      formatPace: (sPerKm: number | null | undefined): string => {
        const secs = sPerKm == null ? null : paceToSystem(sPerKm, system);
        const mmss = formatPaceMMSS(secs, noData);
        return mmss === noData ? mmss : `${mmss} /${symbols.distance}`;
      },

      // Parseurs de saisie -> SI, déjà liés au système :
      parseWeightToKg: (text: string) => parseWeightToKgPure(text, system),
      parseDistanceToKm: (text: string) => parseDistanceToKmPure(text, system),
      heightPartsToCm: (a: string, b: string) => heightPartsToCmPure(a, b, system),

      /** Pré-remplissage des champs d'édition depuis une valeur SI (chaîne affichable, sans symbole/locale). */
      weightInputValue: (kg: number | null | undefined): string =>
        kg == null ? '' : String(Number((system === 'imperial' ? kgToLb(kg) : kg).toFixed(1))),
      distanceInputValue: (km: number | null | undefined): string =>
        km == null ? '' : String(Number((system === 'imperial' ? kmToMi(km) : km).toFixed(2))),
      heightPartsFromCm: (cm: number | null | undefined): { a: string; b: string } => {
        if (cm == null) return { a: '', b: '' };
        if (system === 'imperial') {
          const { feet, inches } = cmToFtIn(cm);
          return { a: String(feet), b: String(inches) };
        }
        return { a: String(Math.round(cm)), b: '' };
      },
    };
  }, [system, locale, t]);
}
```

> **`weightInputValue` / `heightPartsFromCm`** servent à pré-remplir les champs de saisie dans l'unité affichée (et à mémoriser la **chaîne initiale** pour l'anti-dérive). Ils n'utilisent PAS `Intl` (un `<TextInput>` veut une valeur parsable, pas un séparateur localisé) → `toFixed` + `Number()` pour retirer les zéros superflus.

- [ ] **Step 2 — Typecheck.** `npm run typecheck` → PASS.
- [ ] **Step 3 — Commit.** `feat(mobile): hook useUnits() (formateurs + parseurs liés au réglage et à la locale)`

### Task 5 : Smoke test `jest-expo` du hook

**Files:** Create `apps/mobile/src/hooks/__tests__/useUnits.test.tsx`.

But : garantir les valeurs attendues **metric/imperial × FR/EN**. On mocke `useSettings` (unité) et `i18n.language` (locale) pour rester unitaire (pas de PowerSync).

- [ ] **Step 1 — Test :**

```tsx
import { renderHook } from '@testing-library/react-native';
import { useUnits } from '../useUnits';

// Mocks : on isole le hook de PowerSync et fige la locale.
const settingsMock = { units: 'metric' as 'metric' | 'imperial' };
jest.mock('@/data/repositories/settings-repository', () => ({
  useSettings: () => ({ settings: settingsMock, isLoading: false }),
}));
const langMock = { language: 'fr' };
jest.mock('react-i18next', () => ({
  useTranslation: () => ({ t: (k: string) => k, i18n: langMock }),
}));

describe('useUnits', () => {
  it('metric + FR : virgule décimale, symboles SI', () => {
    settingsMock.units = 'metric'; langMock.language = 'fr';
    const { result } = renderHook(() => useUnits());
    expect(result.current.formatWeight(72.5)).toBe('72,5 kg');
    expect(result.current.formatDistance(5.2)).toBe('5,20 km');
    expect(result.current.formatHeight(178)).toBe('178 cm');
    expect(result.current.formatPace(300)).toBe('5:00 /km');
    expect(result.current.parseWeightToKg('72,5')).toBeCloseTo(72.5, 5);
  });

  it('imperial + EN : point décimal, conversions lb/mi/ft-in', () => {
    settingsMock.units = 'imperial'; langMock.language = 'en';
    const { result } = renderHook(() => useUnits());
    expect(result.current.formatWeight(72.5)).toBe('159.8 lb');
    expect(result.current.formatDistance(5.2)).toBe('3.23 mi');
    expect(result.current.formatHeight(178)).toBe('5 ft 10 in');
    expect(result.current.formatPace(300)).toBe('8:03 /mi');
    expect(result.current.parseWeightToKg('160')).toBeCloseTo(72.57, 1);
  });
});
```

> Ajuster les valeurs attendues exactes (`159.8 lb`, `3.23 mi`, `8:03`) au 1ᵉʳ run selon l'arrondi réel puis figer.

- [ ] **Step 2 — Run.** `npm run test -w @wellness/mobile` (ou `npx jest` dans `apps/mobile`) → PASS.
- [ ] **Step 3 — Commit.** `test(mobile): smoke test useUnits (metric/imperial x FR/EN)`

---

## Phase C — i18n : sortir les unités des chaînes (FR + EN miroir)

### Task 6 : Refonte des clés porteuses d'unité

**Files:** Modify `apps/mobile/src/i18n/locales/fr.json` **et** `en.json` (chaque changement en **miroir**).

Table exhaustive des clés à modifier (garde-fou revue #2). Réf. lignes = `fr.json` (mêmes clés dans `en.json`).

| Clé | Avant (FR) | Après (FR) | Après (EN) | Détail |
|---|---|---|---|---|
| `workout.weight` (L43) | `"Kg"` | `"Charge"` | `"Load"` | JSX ajoute `({weightSymbol})` — en-tête de colonne. |
| `workout.set.weight` (L477) | `"Charge (kg)"` | `"Charge"` | `"Load"` | Suffixe via hook. |
| `workout.set.weightPlaceholder` (L478) | `"Ex. 60"` | *(remplacé)* | | Voir 2 clés ci-dessous. |
| `workout.set.weightPlaceholderMetric` (**nouvelle**) | — | `"Ex. 60"` | `"Ex. 60"` | Choix par `system`. |
| `workout.set.weightPlaceholderImperial` (**nouvelle**) | — | `"Ex. 130"` | `"Ex. 130"` | |
| `onboarding.infos.weight` (L292) | `"Poids (kg)"` | `"Poids"` | `"Weight"` | Suffixe via hook. |
| `onboarding.infos.height` (L293) | `"Taille (cm)"` | `"Taille"` | `"Height"` | Suffixe/2 champs via hook. |
| `onboarding.infos.heightFeet` (**nouvelle**) | — | `"Pieds"` | `"Feet"` | Label champ ft (impérial). |
| `onboarding.infos.heightInches` (**nouvelle**) | — | `"Pouces"` | `"Inches"` | Label champ in (impérial). |
| `history.detail.repsWeight` (L523) | `"{{reps}} × {{kg}} kg"` | `"{{reps}} × {{weight}}"` | idem | `weight` = `formatWeight(kg)` (symbole inclus). |
| `history.detail.weightOnly` (L525) | `"{{kg}} kg"` | `"{{weight}}"` | idem | |
| `*.weight` (L435) | `"{{kg}} kg"` | `"{{weight}}"` | idem | Vérifier le namespace exact au moment de l'édition (poids nutrition). |
| `*.volumeKg` (L503 & L517) | `"{{volume}} kg"` | `"{{volume}}"` | idem | `volume` = `formatWeight(volumeKg)`. **Renommer la clé** en `volume` (sans « Kg ») dans les 2 namespaces + MAJ des appels. |
| `strength.stats.kg` (L541) | `"kg"` | *(supprimée)* | *(supprimée)* | Remplacé par `weightSymbol` dans le JSX. |
| `running.active.kmUnit` (L639) | `"km"` | *(supprimée)* | *(supprimée)* | Remplacé par `distanceSymbol`. |
| `running.active.paceUnit` (L643) | `"/km"` | *(supprimée)* | *(supprimée)* | Intégré à `formatPace`. |

**Non modifiées (exemptions) :** `settings.units.hint` (décrit le réglage), tous les libellés `max_weight` = « Charge max » / `best_volume` = « Volume série » / `volume` = « Volume » (labels, pas d'unité).

- [ ] Appliquer les 2 fichiers en miroir ; **ne supprimer une clé qu'après** avoir migré tous ses appels (fait en Phase D — coordonner ou committer Phase C+D par écran).
- [ ] **Commit.** `refactor(i18n): sort les symboles d'unité des chaînes (FR + EN miroir)`

> ⚠️ Suppression de clés = risque de crash i18next si un appel subsiste. **Recommandé :** committer chaque écran de la Phase D avec la MAJ i18n correspondante (petits commits par écran) plutôt qu'un gros commit i18n isolé. La table ci-dessus est la **référence de vérité** des clés.

---

## Phase D — Écrans : brancher `useUnits()` (affichage + saisie)

Pour **chaque** écran : `import { useUnits } from '@/hooks/useUnits';`, appeler `const units = useUnits();` dans le composant, remplacer les unités codées en dur. **Un commit par écran** (ou par petit groupe cohérent), avec `npm run typecheck` vert avant commit.

### Task 7 : `app/workout.tsx` — affichage + **saisie** de la charge de série

**Files:** Modify `apps/mobile/src/app/workout.tsx` ; i18n (clés `workout.weight`, `workout.set.weight*`).

- [ ] En-tête de colonne (L139) : `{t('workout.weight')}` → `` `${t('workout.weight')} (${units.weightSymbol})` ``.
- [ ] Champ de saisie (L155-159) : `value` = `units.weightInputValue(set.weightKg)` ; `onChangeText={(v) => void updateSet(set.id, { weightKg: units.parseWeightToKg(v) })}`.
  - **Anti-dérive (inline, naturellement sûr) :** `updateSet` ne se déclenche que sur frappe utilisateur → un set non touché n'est jamais réécrit. Rien de plus à faire.
- [ ] Placeholder : `placeholder={t(units.system === 'imperial' ? 'workout.set.weightPlaceholderImperial' : 'workout.set.weightPlaceholderMetric')}`.
- [ ] `toNum` local devient inutile pour le poids → utiliser `units.parseWeightToKg` (garder `toNum` pour les reps si encore utilisé).
- [ ] Typecheck + **Commit.** `feat(mobile): unités sur la saisie de charge (workout)`

### Task 8 : `app/workout-summary.tsx` + `app/history/[id].tsx` — volume & séries

**Files:** Modify les 2 écrans ; i18n (`volumeKg`→`volume`, `repsWeight`, `weightOnly`).

- [ ] `workout-summary.tsx` : volume total → `t('...volume', { volume: units.formatWeight(volumeKg) })`. Chaque valeur de série (`{{reps}} × {{kg}} kg`, `weightOnly`) → passer `weight: units.formatWeight(set.weightKg)` au lieu de `kg`.
- [ ] `history/[id].tsx` (L248, L252, L285) : remplacer `{ reps, kg: set.weightKg }` par `{ reps, weight: units.formatWeight(set.weightKg) }` ; idem `weightOnly` et le bloc record (L285).
- [ ] Typecheck + **Commit.** `feat(mobile): unités sur résumé de séance et détail d'historique`

### Task 9 : `app/progress/index.tsx` + `app/programs/[id].tsx` — courbes & détail programme

**Files:** Modify les 2 écrans.

- [ ] `progress/index.tsx` : lire l'écran, remplacer tout affichage de charge/volume (`… kg`, labels d'axe, tooltips) par `units.formatWeight(...)` / `units.weightSymbol`. Les **données de courbe restent en SI** ; ne convertir que le **rendu** (valeurs d'axe, étiquettes). Si un axe affiche des valeurs numériques, convertir la valeur affichée via `system` (mais laisser les points de données bruts au chart si celui-ci ne fait pas d'unités).
- [ ] `programs/[id].tsx` : charges cibles affichées → `units.formatWeight(...)` ; en-têtes/suffixes → `units.weightSymbol`.
- [ ] Typecheck + **Commit.** `feat(mobile): unités sur progression et détail de programme`

### Task 10 : `app/run/active.tsx` + `app/run/summary.tsx` — distance, allure, **saisie distance manuelle**

**Files:** Modify les 2 écrans ; i18n (suppression `kmUnit`/`paceUnit`).

- [ ] `run/active.tsx` :
  - Supprimer le `formatPace` **local** (L40) → utiliser `units.formatPace(sPerKm)` (inclut déjà `/km`|`/mi`). Adapter le JSX : la valeur `paceValue` = `units.formatPace(...)` sans `paceUnit` séparé (retirer les `<Text>{t('running.active.paceUnit')}</Text>`).
  - Distance (L104, L165-167) : `distanceKm = (distanceM/1000)` → afficher `units.formatDistance(distanceM / 1000)` **ou** garder la grande valeur numérique + `units.distanceSymbol` pour le suffixe (préserver la mise en page « gros chiffre + unité »). Choix : valeur = nombre converti/formaté locale, unité = `units.distanceSymbol`.
- [ ] `run/summary.tsx` :
  - Supprimer `formatPace` local (L41) → `units.formatPace(run.avgPaceSPerKm)`.
  - Affichages distance (L214-215, L233-234, L243, L276) → `units.formatDistance(...)` / `units.distanceSymbol`.
  - **Saisie distance manuelle (L171-174) :** `parseFloat(manualDistanceKm.replace(',', '.'))` → `units.parseDistanceToKm(manualDistanceText)` (renvoie des **km**), puis `setManualRunDistance(id, km * 1000)`.
    - **Anti-dérive :** pré-remplir le champ avec `units.distanceInputValue(run?.distanceM != null ? run.distanceM / 1000 : null)` ; n'appeler `setManualRunDistance` que si le texte a changé (déjà le cas : action au bouton « valider »).
  - Placeholder distance adapté (`Ex. 5,0` km / `Ex. 3,1` mi) via clés i18n ou `units.system`.
- [ ] Typecheck + **Commit.** `feat(mobile): unités sur course en direct et résumé (distance, allure, saisie)`

### Task 11 : Nutrition — `app/(tabs)/nutrition.tsx`, `app/nutrition-profile.tsx`, `app/nutrition-stats.tsx`

**Files:** Modify les 3 écrans ; i18n (clé poids nutrition L435).

- [ ] **Affichage** poids corporel / stats : partout où `weightKg` ou `heightCm` est rendu (`{{kg}} kg`, taille, courbe/pesée) → `units.formatWeight(...)` / `units.formatHeight(...)`.
- [ ] `nutrition-stats.tsx` : courbe de poids → convertir le **rendu** (valeurs d'axe/étiquettes) via `system`, données brutes en SI.
- [ ] **Saisie** dans `nutrition-profile.tsx` (poids + taille) : voir Task 12 pour le patron anti-dérive (taille = 1 champ cm / 2 champs ft+in).
- [ ] Typecheck + **Commit.** `feat(mobile): unités sur les écrans nutrition (poids, taille)`

### Task 12 : Saisie poids + **taille** — `app/(onboarding)/infos.tsx`, `app/profile.tsx`, `app/nutrition-profile.tsx`

**Files:** Modify les 3 écrans ; i18n (`onboarding.infos.weight/height`, `heightFeet`, `heightInches`).

Patron commun (le seul vrai changement d'UI de saisie).

- [ ] **Poids :**
  - Label : `label={` `${t('onboarding.infos.weight')} (${units.weightSymbol})` `}`.
  - `value` initialisé via `units.weightInputValue(profile?.weightKg)` (state local, comme aujourd'hui) ; conserver `initialWeight` = cette même chaîne.
  - À l'enregistrement (**anti-dérive, revue #3**) :
    ```ts
    weightKg: weight === initialWeight ? (profile?.weightKg ?? null) : units.parseWeightToKg(weight),
    ```
- [ ] **Taille — conditionnel sur `units.system` :**
  - `const h0 = units.heightPartsFromCm(profile?.heightCm);` → state `heightA` (cm ou ft), `heightB` (in) initialisés ; mémoriser `initialHeightA/B`.
  - **metric** : un seul `TextField` `label={t('onboarding.infos.height') + ' (cm)'}` (ou `+ ' (' + 'cm' + ')'` — `cm` est le symbole métrique, non traduit), `keyboardType="number-pad"`, `value={heightA}`.
  - **imperial** : deux `TextField` côte à côte — `label={t('onboarding.infos.heightFeet')}` (`value={heightA}`) et `label={t('onboarding.infos.heightInches')}` (`value={heightB}`), suffixes `ft`/`in`.
  - À l'enregistrement :
    ```ts
    heightCm:
      heightA === initialHeightA && heightB === initialHeightB
        ? (profile?.heightCm ?? null)
        : units.heightPartsToCm(heightA, heightB),
    ```
- [ ] `onboarding/infos.tsx` : champs vides au départ (pas de valeur stockée) → `initial*` = `''` ; saisie normale via `units.parse*`. Anti-dérive sans objet (rien de stocké) mais le même code fonctionne (`'' === ''` → écrit `null` si non touché, cohérent).
- [ ] Typecheck + **Commit.** `feat(mobile): saisie poids + taille (cm ou ft/in) selon le réglage d'unités`

---

## Phase E — Vérification & Definition of Done

### Task 13 : Garde-fou grep + qualité globale

**Files:** aucun (vérification).

- [ ] **Grep garde-fou** — plus aucune unité codée en dur dans le JSX ni les valeurs i18n des surfaces §1 (hors exemptions) :
  ```bash
  # Doit ne rien retourner de significatif hors settings.units.hint et symboles pilotés par le hook :
  grep -rnE "\{\{kg\}\}|\{\{volume\}\} kg| kg\"| km\"|/km\"|Taille \(cm\)|Poids \(kg\)|Charge \(kg\)" apps/mobile/src
  grep -rnE ">[^<]*\b(kg|km|cm|lb|mi)\b" apps/mobile/src/app  # revue manuelle des restes
  ```
- [ ] `npm run typecheck` → PASS.
- [ ] `npm run lint` → PASS.
- [ ] `npm run test` → PASS (Vitest shared + jest-expo mobile).
- [ ] **Commit** éventuel des derniers ajustements. `chore(1.15): garde-fou grep + qualité verte`

### Task 14 : DoD manuelle sur device (checklist de recette)

**Files:** aucun (test manuel — nécessite un build `preview`).

- [ ] Basculer **Métrique ↔ Impérial** dans Réglages → **toutes** les surfaces §1 se mettent à jour **sans redémarrage** (réactif).
- [ ] Vérifier en **FR** puis en **EN** (séparateur décimal virgule/point).
- [ ] Round-trip saisie : saisir un poids en lb, enregistrer, rouvrir → valeur cohérente ; **taille en ft/in** saisissable.
- [ ] **Anti-dérive :** rouvrir un profil et enregistrer **sans modifier** → la valeur SI stockée **ne change pas** (vérifiable en base locale ou via ré-affichage stable après plusieurs sauvegardes).
- [ ] Course : distance + allure affichées dans la bonne unité ; saisie de distance manuelle en mi → stockée en km.
- [ ] Offline inchangé (aucune régression sync — on n'a pas touché au stockage).

---

## Ordre de build & commits

1. Phase A (Tasks 1-3) — shared pur, 3 commits.
2. Phase B (Tasks 4-5) — hook + smoke test, 2 commits.
3. Phase C+D **entrelacées** (Tasks 6-12) — **un commit par écran** avec sa MAJ i18n, pour ne jamais laisser une clé supprimée sans son appel migré.
4. Phase E (Tasks 13-14) — garde-fou + recette.

Chaque commit passe par la skill **`/commit`** (CHANGELOG + TODO + message conventionnel FR + push `dev`). **Ne jamais stager** `apps/mobile/eas.json` (modif locale hors sujet) ni de secret.

## Definition of Done (spec §7)

- `units.test.ts` étendu (ft/in, allure, parseurs, round-trips) ✔ Phase A.
- Smoke test `jest-expo` de `useUnits()` (metric/imperial × FR/EN) ✔ Task 5.
- DoD device (metric↔imperial, FR+EN, saisie round-trip, ft/in, anti-dérive, offline) ✔ Task 14.
- Garde-fou grep vert ✔ Task 13.
- `typecheck` / `lint` / `test` verts ✔ Task 13.
