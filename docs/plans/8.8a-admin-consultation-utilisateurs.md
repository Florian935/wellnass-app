# US 8.8a — Consultation des utilisateurs — Plan d'implémentation

> **Pour l'exécutant :** implémenter tâche par tâche. Étapes cochables (`- [ ]`). L'admin n'a **pas**
> de runner de tests (Vitest est sur `packages/shared`) → vérif = `typecheck` + `lint` + `build`
> côté `apps/admin`, puis recette. Commits conventionnels FR.

**Objectif :** donner aux rôles `super_admin`/`moderator` une consultation sobre des comptes
(liste + recherche email + fiche lecture seule), en lecture pure.

**Architecture :** vue SQL `public.admin_users` protégée (`security_invoker=false` + `WHERE
public.can_manage_users()`) exposant des colonnes sobres de `auth.users`+`profiles`+`user_settings` ;
écrans admin (Vite/React) requêtant la vue via PostgREST (clé anon) ; nouveau gate `canManageUsers`.

**Tech Stack :** Supabase (SQL migration, vue, fonction SECURITY DEFINER), PostgREST/supabase-js,
React + react-router, i18n FR maison (`apps/admin/src/i18n/fr.ts`).

**Spec :** [docs/specs/functional/us/8.8a-admin-consultation-utilisateurs.md](../specs/functional/us/8.8a-admin-consultation-utilisateurs.md)

**⚠️ Ordre imposé :** la migration doit être **appliquée + `db:types` régénéré AVANT** d'écrire la
couche data (`users.ts` type sur `Database['public']['Views']['admin_users']['Row']`). Task 2 est un
**checkpoint humain** (apply cloud).

**Fichiers touchés :**
- Créer : `supabase/migrations/<ts>_admin_users_view.sql`
- Modifier : `supabase/MIGRATIONS.md`, `packages/shared/src/database.types.ts` (régénéré)
- Modifier : `apps/admin/src/auth/rolesContext.ts`, `apps/admin/src/auth/RolesProvider.tsx`, `apps/admin/src/App.tsx`, `apps/admin/src/components/AdminLayout.tsx`
- Créer : `apps/admin/src/data/users.ts`, `apps/admin/src/screens/UsersScreen.tsx`, `apps/admin/src/screens/UserDetailScreen.tsx`
- Modifier : `apps/admin/src/i18n/fr.ts`

---

## Task 1 : Migration SQL — fonction `can_manage_users()` + vue `admin_users`

**Files :**
- Create : `supabase/migrations/<timestamp>_admin_users_view.sql` (via `npm run db:new admin_users_view`)

- [ ] **Step 1 : Générer le fichier de migration**

Run : `npm run db:new admin_users_view` (crée le fichier horodaté dans `supabase/migrations/`).

- [ ] **Step 2 : Écrire le SQL**

```sql
-- US 8.8a — Consultation des utilisateurs (back-office).
-- Fonction de gate can_manage_users() (super_admin OU moderator) + vue admin_users
-- (lecture seule, sobre RGPD) joignant auth.users + profiles + user_settings.
-- Table/vue web-admin uniquement : HORS publication PowerSync, aucune sync rule.
-- Réf. : docs/specs/functional/us/8.8a-admin-consultation-utilisateurs.md §3.

-- Gate : a le rôle super_admin OU moderator (actif). Même style que is_admin/is_super_admin
-- (SECURITY DEFINER pour contourner la RLS de user_roles ; STABLE ; search_path figé).
create or replace function public.can_manage_users()
  returns boolean
  language sql
  stable
  security definer
  set search_path = public
as $$
  select exists (
    select 1 from public.user_roles
    where user_id = auth.uid()
      and role in ('super_admin', 'moderator')
      and deleted_at is null
  );
$$;

-- Vue de consultation. security_invoker reste false (défaut) → exécutée avec les droits
-- du propriétaire (rôle de migration) : lit auth.users et contourne la RLS de
-- profiles/user_settings. La SEULE barrière serveur est le WHERE can_manage_users().
-- Colonnes sobres : aucune donnée de santé, aucun secret (pas de hash).
create or replace view public.admin_users
  with (security_barrier = true)
as
  select
    u.id,
    u.email,
    u.created_at,
    u.last_sign_in_at,
    (u.banned_until is not null and u.banned_until > now()) as is_banned,
    p.first_name,
    p.main_goal,
    p.onboarding_completed_at,
    s.active_pillars,
    s.language
  from auth.users u
  left join public.profiles p on p.user_id = u.id and p.deleted_at is null
  left join public.user_settings s on s.user_id = u.id and s.deleted_at is null
  where public.can_manage_users();

-- Privilèges explicites (défense en profondeur : la protection tient déjà via le WHERE).
revoke all on public.admin_users from anon;
grant select on public.admin_users to authenticated;
```

- [ ] **Step 3 : Prévisualiser**

Run : `npm run db:push:dry`
Expected : la nouvelle migration figure dans la liste des migrations à jouer.

- [ ] **Step 4 : Commit (SQL seulement, apply au checkpoint suivant)**

```bash
git add supabase/migrations/
git commit -m "feat(admin): migration vue admin_users + fonction can_manage_users (US 8.8a)"
```

---

## Task 2 : [CHECKPOINT 🔴 — apply cloud] db:push + db:types + registre

> **Étape humaine / contrôleur** (dev directement sur le cloud, pas de Docker). À faire après revue du
> SQL de Task 1. Écrit dans le schéma partagé → ne pas déléguer en autonomie.

- [ ] **Step 1 : Pousser sur le cloud** — `npm run db:push`
- [ ] **Step 2 : Régénérer les types** — `npm run db:types` (peuple `Database['public']['Views']['admin_users']`)
- [ ] **Step 3 : Vérif** — dans le SQL Editor cloud : `select * from public.admin_users limit 5;` en tant que super_admin renvoie des lignes ; un compte normal renvoie **0 ligne**. Si « relation inconnue » via l'API → `NOTIFY pgrst, 'reload schema';`.
- [ ] **Step 4 : Registre** — cocher la migration dans [supabase/MIGRATIONS.md](../../supabase/MIGRATIONS.md) (case + date).
- [ ] **Step 5 : Commit**

```bash
git add packages/shared/src/database.types.ts supabase/MIGRATIONS.md
git commit -m "chore(db): apply admin_users + db:types (US 8.8a)"
```

---

## Task 3 : Gate `canManageUsers` (contexte + provider + garde de route)

**Files :**
- Modify : `apps/admin/src/auth/rolesContext.ts`, `apps/admin/src/auth/RolesProvider.tsx`, `apps/admin/src/App.tsx`

- [ ] **Step 1 : Étendre le contexte**

Dans `rolesContext.ts`, ajouter au type `RolesContextValue` :
```ts
  /** Peut gérer les utilisateurs (`super_admin` ou `moderator`) → US 8.8. */
  canManageUsers: boolean;
```

- [ ] **Step 2 : Peupler dans le provider**

Dans `RolesProvider.tsx`, objet `value` :
```ts
      canManageUsers:
        roles.includes('super_admin') || roles.includes('moderator'),
```

> ⚠️ **Task 3 = contexte + provider UNIQUEMENT.** La fonction `RequireCanManageUsers` et les routes
> `/users` sont **définies en Task 6** (dans le même commit que l'écran qui les utilise). Motif : eslint
> `@typescript-eslint/no-unused-vars` flaguerait une fonction top-level définie mais non encore
> utilisée → `npm run lint` rouge entre commits. On ne touche donc PAS `App.tsx` ici.

- [ ] **Step 3 : Vérif** — `npm run typecheck` (côté admin). Commit :

```bash
git add apps/admin/src/auth/rolesContext.ts apps/admin/src/auth/RolesProvider.tsx
git commit -m "feat(admin): capacité canManageUsers (super_admin ou moderator)"
```

---

## Task 4 : i18n FR (`fr.users`)

**Files :**
- Modify : `apps/admin/src/i18n/fr.ts`

- [ ] **Step 1 : Ajouter l'objet `users`** (au même niveau que `foods`, `roles`, `audit`). `layout.nav.users` (« Utilisateurs ») existe **déjà** — ne pas le dupliquer.

```ts
  users: {
    title: 'Utilisateurs',
    nav: 'Utilisateurs',
    search: 'Rechercher par e-mail…',
    loading: 'Chargement des utilisateurs…',
    empty: 'Aucun utilisateur.',
    emptySearch: 'Aucun résultat pour cette recherche.',
    error: 'Erreur de chargement des utilisateurs.',
    colEmail: 'E-mail',
    colRegistered: 'Inscrit le',
    colLastSignIn: 'Dernière connexion',
    colPillars: 'Piliers',
    colStatus: 'Statut',
    statusActive: 'Actif',
    statusBanned: 'Banni',
    never: 'Jamais',
    none: '—',
    prev: 'Précédent',
    next: 'Suivant',
    pageInfo: 'Page {page} / {total}',
    pillars: { strength: 'Muscu', running: 'Course', nutrition: 'Nutrition' },
    detail: {
      back: '← Retour à la liste',
      accountSection: 'Compte',
      configSection: 'Configuration',
      profileSection: 'Profil',
      email: 'E-mail',
      registered: 'Inscrit le',
      lastSignIn: 'Dernière connexion',
      status: 'Statut',
      pillars: 'Piliers actifs',
      language: 'Langue',
      firstName: 'Prénom',
      goal: 'Objectif',
      onboarding: 'Onboarding terminé',
      yes: 'Oui',
      no: 'Non',
      notFound: 'Utilisateur introuvable.',
    },
    goals: { muscle: 'Prise de masse', weightloss: 'Perte de poids', performance: 'Performance', health: 'Santé' },
  },
```

- [ ] **Step 2 : Vérif** — `npm run typecheck` (côté admin). Commit :

```bash
git add apps/admin/src/i18n/fr.ts
git commit -m "i18n(admin): libellés consultation utilisateurs (fr.users)"
```

---

## Task 5 : Couche data `users.ts`

**Files :**
- Create : `apps/admin/src/data/users.ts`

> Prérequis : Task 2 faite (type `Views.admin_users` disponible).

- [ ] **Step 1 : Écrire le module**

```ts
import { supabase } from '../lib/supabase';
import type { Database } from '@wellness/shared';

/**
 * Couche data de la consultation des utilisateurs (US 8.8a). Lecture seule via la
 * vue `public.admin_users` (protégée par `can_manage_users()` côté serveur ; clé anon).
 * Aucune écriture, aucun logAudit (la consultation n'écrit rien).
 */
export type AdminUserRow = Database['public']['Views']['admin_users']['Row'];

// ⚠️ Toutes les colonnes d'une VUE sont typées `T | null` par `db:types` (Postgres ne prouve pas le
// non-null à travers une vue). Donc `id`, `email`, `created_at`, `active_pillars` (Json | null), etc.
// sont TOUS nullables → les écrans doivent guarder chaque valeur (voir Task 6/7).

export const USERS_PAGE_SIZE = 25;

/** Liste paginée + recherche par e-mail (ilike). Tri par inscription décroissante. */
export async function listUsers(opts?: {
  search?: string;
  page?: number;
  pageSize?: number;
}): Promise<{ rows: AdminUserRow[]; count: number; error: unknown }> {
  const page = opts?.page ?? 0;
  const pageSize = opts?.pageSize ?? USERS_PAGE_SIZE;
  const term = (opts?.search ?? '').trim();

  let query = supabase
    .from('admin_users')
    .select('*', { count: 'exact' })
    .order('created_at', { ascending: false });

  if (term) query = query.ilike('email', `%${term}%`);

  const from = page * pageSize;
  query = query.range(from, from + pageSize - 1);

  const { data, count, error } = await query;
  if (error) return { rows: [], count: 0, error };
  return { rows: data ?? [], count: count ?? 0, error: null };
}

/** Détail d'un compte (par id). `null` si absent ou non visible (RLS/vue). */
export async function getUser(
  id: string,
): Promise<{ user: AdminUserRow | null; error: unknown }> {
  const { data, error } = await supabase
    .from('admin_users')
    .select('*')
    .eq('id', id)
    .maybeSingle();
  if (error) return { user: null, error };
  return { user: data ?? null, error: null };
}
```

- [ ] **Step 2 : Vérif** — `npm run typecheck`. Commit :

```bash
git add apps/admin/src/data/users.ts
git commit -m "feat(admin): couche data consultation utilisateurs (listUsers/getUser)"
```

---

## Task 6 : Écran liste `/users` + conversion de la navigation

**Files :**
- Create : `apps/admin/src/screens/UsersScreen.tsx`
- Modify : `apps/admin/src/components/AdminLayout.tsx`, `apps/admin/src/App.tsx`

- [ ] **Step 1 : `UsersScreen.tsx`**

Calquer le style sur [FoodsScreen.tsx](../../apps/admin/src/screens/FoodsScreen.tsx) (panel, table, input, styles, `formatDate` JJ/MM/AAAA). Spécificités :
- État : `rows`, `count`, `loading`, `error`, `search`, `page`.
- **Recherche + pagination côté serveur** : `useEffect` (déclenché par `search`/`page`) → `listUsers({ search, page })`. Débounce léger de la recherche (~300 ms) et **reset `page` à 0** quand `search` change.
- Colonnes : Email · Inscrit le · Dernière connexion · Piliers · Statut (badge `is_banned ? statusBanned : statusActive`).
- ⚠️ **Nullabilité (colonnes de vue)** : `AdminUserRow` a **toutes ses colonnes nullables**. Le
  `formatDate(iso: string)` réutilisé de FoodsScreen exige un `string` → **guarder** :
  ```ts
  const fmt = (iso: string | null) => (iso ? formatDate(iso) : fr.users.none);
  // Email : {row.email ?? fr.users.none} ; Inscrit le : {fmt(row.created_at)} ;
  // Dernière connexion : {row.last_sign_in_at ? formatDate(row.last_sign_in_at) : fr.users.never}
  ```
- ⚠️ **`renderPillars`** : `active_pillars` est typé `Json | null` → **guarder avant `.map`** :
  ```ts
  function renderPillars(value: AdminUserRow['active_pillars']): string {
    if (!Array.isArray(value)) return fr.users.none;
    const labels = value
      .map((k) => fr.users.pillars[k as keyof typeof fr.users.pillars])
      .filter(Boolean);
    return labels.length ? labels.join(' · ') : fr.users.none;
  }
  ```
- Ligne cliquable → `navigate('/users/${row.id}')` (guarder `row.id` non-null : la clé de ligne et la nav ; en pratique `id` est toujours présent, mais TS le voit nullable → `row.id ?? ''` ou early-skip).
- ⚠️ **Pagination sans interpolation** : l'i18n admin **n'interpole pas** `{page}`/`{total}`. Composer
  à la main : `fr.users.pageInfo.replace('{page}', String(page + 1)).replace('{total}', String(totalPages))`
  avec `totalPages = Math.max(1, Math.ceil(count / USERS_PAGE_SIZE))`. Boutons Précédent/Suivant désactivés aux bornes.
- États : `loading` / `error` (role="alert") / vide (distinguer `empty` vs `emptySearch` selon `search`).

- [ ] **Step 2 : Garde de route `RequireCanManageUsers` + routes** dans `App.tsx`
  - Définir (sur le modèle de `RequireSuperAdmin`, l.146) :
    ```tsx
    /** Restreint une route à la gestion des utilisateurs (super_admin/moderator). */
    function RequireCanManageUsers({ children }: { children: React.ReactElement }) {
      const { canManageUsers } = useRoles();
      if (!canManageUsers) return <Navigate to="/" replace />;
      return children;
    }
    ```
  - Importer `UsersScreen` (et `UserDetailScreen` viendra en Task 7 — l'ajouter alors) et déclarer :
    ```tsx
    <Route path="/users" element={<RequireCanManageUsers><UsersScreen /></RequireCanManageUsers>} />
    ```
  > ⚠️ Ne PAS réutiliser `RequireContentEditor`/`RequireAdmin` (un `content_editor` serait admis).

- [ ] **Step 3 : Convertir la navigation** dans `AdminLayout.tsx`
  - Récupérer `canManageUsers` depuis `useRoles()` (ajouter à la déstructuration ligne 19).
  - Ajouter un `NavLink to="/users"` conditionné `{canManageUsers && (...)}` (même style, icône 👤, libellé `fr.layout.nav.users`).
  - **Supprimer `NAV_SOON`** (ligne 10) **et son bloc de rendu** (lignes ~112-119) : « Utilisateurs » était son seul élément → devient du code mort. (Ne pas laisser `NAV_SOON = []`.)

- [ ] **Step 4 : Vérif** — `npm run typecheck && npm run lint && npm run build` (admin). Commit :

```bash
git add apps/admin/src/screens/UsersScreen.tsx apps/admin/src/components/AdminLayout.tsx apps/admin/src/App.tsx
git commit -m "feat(admin): écran liste des utilisateurs + garde de route + navigation"
```

---

## Task 7 : Écran fiche `/users/:id` (lecture seule, sobre)

**Files :**
- Create : `apps/admin/src/screens/UserDetailScreen.tsx`
- Modify : `apps/admin/src/App.tsx`

- [ ] **Step 1 : `UserDetailScreen.tsx`**

- `useParams<{ id: string }>()` → `getUser(id)` dans un `useEffect` ; états `loading`/`error`/`notFound`.
- Lien retour `fr.users.detail.back` → `navigate('/users')`.
- 3 sections (panels, style FoodsScreen). ⚠️ **Toutes les colonnes sont nullables** → guarder :
  - **Compte** : email (`?? none`), inscription (`created_at ? formatDate : none`), dernière connexion (`last_sign_in_at ? formatDate : never`), statut (badge Actif/Banni sur `is_banned`).
  - **Configuration** : piliers actifs (`renderPillars`, même helper que Task 6 — l'extraire dans un module partagé `apps/admin/src/data/users.ts` ou un util, ou le dupliquer localement), langue (`language ?? none`).
  - **Profil** : prénom (`first_name ?? none`), objectif — ⚠️ **cast de clé littérale** :
    ```ts
    const goalLabel = main_goal
      ? (fr.users.goals[main_goal as keyof typeof fr.users.goals] ?? fr.users.none)
      : fr.users.none;
    ```
    onboarding terminé (`onboarding_completed_at ? fr.users.detail.yes : fr.users.detail.no`).
- **Aucune** donnée de santé, **aucun** bouton d'action (ban = 8.8b).
- `notFound` (getUser renvoie `user: null`) → message `fr.users.detail.notFound` + lien retour.

- [ ] **Step 2 : Câbler la route** dans `App.tsx` : import `UserDetailScreen`, route `/users/:id` sous `RequireCanManageUsers` (le garde existe déjà depuis Task 6).

- [ ] **Step 3 : Vérif** — `npm run typecheck && npm run lint && npm run build`. Commit :

```bash
git add apps/admin/src/screens/UserDetailScreen.tsx apps/admin/src/App.tsx
git commit -m "feat(admin): fiche utilisateur en lecture seule (sobre RGPD)"
```

---

## Task 8 : Vérification finale & recette

- [ ] **Step 1 : Suite admin verte** — `npm run typecheck && npm run lint && npm run build`.
- [ ] **Step 2 : Suivi & commit final** — CHANGELOG + TODO (US 8.8a livrée, reste recette + relecture Damien ; noter 8.8b à cadrer), push `dev` via `/commit`.
- [ ] **Step 3 : Recette (Florian)** :
  - Avec un compte **super_admin** : entrée « Utilisateurs » visible → liste peuplée (email, dates, piliers, statut) ; recherche par email ; pagination ; clic → fiche sobre correcte.
  - Avec un compte **moderator** : même accès.
  - Avec un compte **content_editor** : entrée « Utilisateurs » **absente**, `/users` en URL directe → redirige vers l'accueil, et la vue renvoie 0 ligne.
  - Compte sans profil (onboarding non fait) → apparaît, champs profil « — ».
  - Dernière connexion « Jamais » pour un compte jamais connecté.

---

## Notes

- **Lecture seule, aucun `service_role`, aucune sync rule** (vue web-admin hors PowerSync).
- **Checkpoint 🔴** = Task 2 (apply cloud) : écrit dans le schéma `auth`/`public` partagé → étape humaine.
- **Ordre critique** : Task 1 → Task 2 (db:types) **avant** Task 5 (le type de la vue doit exister).
- **8.8b (bannissement)** = US séparée : RPC `ban_user`/`unban_user` SECURITY DEFINER sur
  `auth.users.banned_until`, table `user_bans` (motif), actions UI, audit — à cadrer avec Damien.
- Pas de tests unitaires côté admin (pas de runner) → vérif = typecheck/lint/build + recette.
