# Running R3a — Profil coureur + types de séance — Plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Poser le **profil coureur** (table synchronisée `running_profiles`) et la **logique pure d'allures cibles** par type de séance (dérivées de l'allure de réf 5 km, via VMA), affichées dans un écran de profil.

**Architecture:** Logique pure testée dans `@wellness/shared` (`running-paces.ts` + parseur/format d'allure dans `units.ts`) ; table `running_profiles` (migration + RLS + sync rules + schéma PowerSync) sur le patron **`nutrition_profiles`** ; `running-profile-repository` réactif ; écran `running-profile.tsx` (patron `nutrition-profile.tsx`) avec section « Mes allures ». Offline-first ; **checkpoint 🔴 cloud** (activation avec Damien).

**Tech Stack:** `@wellness/shared` (TS + Vitest), Supabase (Postgres/RLS), PowerSync, `apps/mobile` (Expo Router, `useUnits`, i18next).

**Spec :** [5.1-5.8-5.11-running-r3a-profil-types.md](../specs/functional/us/5.1-5.8-5.11-running-r3a-profil-types.md) (validée). **Patrons à copier :** `nutrition_profiles` (migration `supabase/migrations/20260706140000_nutrition_tables.sql` + RLS `…140001`), `nutrition-repository.ts`, `nutrition-profile.tsx`, `powersync/schema.ts`.

**Branche :** `feature/running-r3a-profil-types`, depuis `dev` (spec commitée `eda61bc`).

> **Cloud** : Florian applique le cloud (migration + sync rules + `db:types`) avec Damien après le code. **Timestamp de migration** : `20260712090000` (> dernière `20260711140000`) — **à confirmer avec Damien** avant application.

---

## Phase A — `@wellness/shared` : allures (TDD)

### Task 1 : `running-paces.ts` — enums + calcul d'allures

**Files:** Create `packages/shared/src/running-paces.ts` (+ `.test.ts`) ; MAJ `index.ts`.

- [ ] **Enums + schémas Zod** (patron `nutrition.ts`) :
```ts
export const RUNNER_OBJECTIVES = ['5k','10k','semi','marathon','perte_poids','endurance'] as const;
export const runnerObjectiveSchema = z.enum(RUNNER_OBJECTIVES);
export type RunnerObjective = z.infer<typeof runnerObjectiveSchema>;

export const RUNNER_LEVELS = ['debutant','regulier','confirme'] as const;
export const runnerLevelSchema = z.enum(RUNNER_LEVELS);
export type RunnerLevel = z.infer<typeof runnerLevelSchema>;

export const SESSION_TYPES = ['endurance','fractionne','sortie_longue','recuperation','course_libre'] as const;
export const sessionTypeSchema = z.enum(SESSION_TYPES);
export type SessionType = z.infer<typeof sessionTypeSchema>;
```
- [ ] **Constante + dérivation VMA** :
```ts
/** Allure 5 km ≈ 95 % de l'allure à VMA (vitesse). Ajustable. */
export const VMA_COEFFICIENT = 0.95;
/** Allure (s/km) à 100 % VMA, dérivée de l'allure de réf 5 km. */
export function derivedVmaPace(ref5kPaceSPerKm: number): number {
  return ref5kPaceSPerKm * VMA_COEFFICIENT;
}
```
- [ ] **Plages d'allure par type** :
```ts
export type PaceRange = { minSPerKm: number; maxSPerKm: number };
/** Plage d'allure cible pour un type, depuis l'allure de réf 5 km (s/km). null pour course_libre. */
export function sessionTargetPace(type: SessionType, ref5kPaceSPerKm: number): PaceRange | null {
  switch (type) {
    case 'endurance':     return { minSPerKm: ref5kPaceSPerKm + 60, maxSPerKm: ref5kPaceSPerKm + 90 };
    case 'sortie_longue': return { minSPerKm: ref5kPaceSPerKm + 30, maxSPerKm: ref5kPaceSPerKm + 60 };
    case 'recuperation':  return { minSPerKm: ref5kPaceSPerKm + 90, maxSPerKm: ref5kPaceSPerKm + 120 };
    case 'fractionne': {
      const vma = derivedVmaPace(ref5kPaceSPerKm);        // 100 % VMA (plus rapide)
      return { minSPerKm: vma, maxSPerKm: vma / 0.95 };    // 95 % VMA (plus lent) = ref5k
    }
    case 'course_libre':  return null;
  }
}
```
- [ ] **Tests** (rouge→vert) : endurance/longue/récup = réf + offsets ; fractionné min=vma(=réf×0.95), max=réf ; `course_libre`→null ; `min < max` pour chaque type non-nul ; VMA plus rapide que la réf. Ex. réf=300 s/km : endurance {360,390}, longue {330,360}, récup {390,420}, fractionné {285,300}.
- [ ] Export via `index.ts`. **Commit** : `feat(shared): allures cibles par type de séance + dérivation VMA (running-paces)`

### Task 2 : parse/format d'allure (saisie) dans `units.ts`

**Files:** MAJ `packages/shared/src/units.ts` (+ `units.test.ts`).

L'US 1.15 couvrait poids/distance mais **pas la saisie d'allure**. Ajouter :
- [ ] `parsePaceToSPerKm(text: string, system: UnitSystem): number | null` — parse « M:SS » (ou « M:SS.d ») → secondes par **unité affichée**, puis convertit en **s/km** (`imperial` → sPerMi × MI_PER_KM). Vide/format invalide → `null`. **Garde de plausibilité** : si l'allure résultante est hors de **[150, 720] s/km** (≈ 2:30–12:00 /km), renvoyer `null` (rejette les valeurs aberrantes ; borne documentée). Secondes ≥ 60 dans « M:SS » aussi → invalide.
- [ ] `formatPaceValue(sPerKm: number, system): string` — s/km → « M:SS » **dans l'unité courante** (sans suffixe), pour pré-remplir un champ. (Réutilise `paceToSystem` + `formatPaceMMSS`.)
- [ ] **Tests** : « 5:00 » metric → 300 ; « 8:03 » imperial → ~300 (±1) ; « » → null ; « abc »/« 5:99 » (secondes ≥ 60) → null ; **hors plage** « 1:00 » (60 s/km, trop rapide) → null et « 15:00 » (trop lent) → null ; round-trip format→parse.
- [ ] **Commit** : `feat(shared): parse/format d'allure pour la saisie (min/km, min/mi)`

> Le hook `useUnits` exposera `parsePace(text)` / `paceInputValue(sPerKm)` liés au système (Phase E).

---

## Phase B — Backend (données)

### Task 3 : Migration `running_profiles` + RLS + sync rules + schéma PowerSync

**Files:** Create `supabase/migrations/20260712090000_running_profiles.sql` ; MAJ [powersync-sync-rules.yaml](../specs/technical/powersync-sync-rules.yaml) ; MAJ `apps/mobile/src/powersync/schema.ts`.

- [ ] **Migration** (patron `nutrition_tables.sql`) :
```sql
create table public.running_profiles (
  id uuid primary key,
  user_id uuid not null unique references auth.users (id) on delete cascade,
  objective text check (objective in ('5k','10k','semi','marathon','perte_poids','endurance')),
  level text check (level in ('debutant','regulier','confirme')),
  ref_5k_pace_s_per_km numeric,
  weekly_frequency integer check (weekly_frequency between 1 and 7),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  deleted_at timestamptz
);
create trigger set_updated_at before update on public.running_profiles
  for each row execute function public.set_updated_at();
alter publication powersync add table public.running_profiles;
-- RLS
alter table public.running_profiles enable row level security;
create policy running_profiles_select on public.running_profiles for select using (user_id = auth.uid());
create policy running_profiles_insert on public.running_profiles for insert with check (user_id = auth.uid());
create policy running_profiles_update on public.running_profiles for update using (user_id = auth.uid()) with check (user_id = auth.uid());
```
(colonnes **scalaires** → aucune sérialisation JSON.)
- [ ] **Sync rules** : ajouter au bucket `user_data` : `select * from running_profiles where user_id = bucket.user_id and deleted_at is null`.
- [ ] **Schéma PowerSync local** (`schema.ts`) : déclarer et ajouter à `AppSchema` :
```ts
const running_profiles = new Table({
  user_id: column.text, objective: column.text, level: column.text,
  ref_5k_pace_s_per_km: column.real, weekly_frequency: column.integer,
  created_at: column.text, updated_at: column.text, deleted_at: column.text,
});
```
- [ ] **`db:types`** : régénération **différée au checkpoint cloud** (nécessite la migration appliquée sur le cloud — Task 8). Le code n'en dépend pas (le repo définit son propre `RunnerProfileDbRow`).
- [ ] Typecheck. **Commit** : `feat(db): table running_profiles (migration + RLS + sync rules + schéma PowerSync)`

---

## Phase C — Repository

### Task 4 : `running-profile-repository.ts`

**Files:** Create `apps/mobile/src/data/repositories/running-profile-repository.ts`.

Mirror `nutrition-repository.ts` :
- [ ] `RunnerProfileDbRow` (snake_case) + type domaine `RunnerProfile` (camelCase) + `RunnerProfileInput` (Pick des champs modifiables).
- [ ] `rowToRunnerProfile(row)` (snake→camel), `inputToColumns(input)` (camel→snake ; scalaires, pas de JSON).
- [ ] `SELECT_CURRENT = 'SELECT * FROM running_profiles WHERE deleted_at IS NULL LIMIT 1'`.
- [ ] `useRunnerProfile(): { runnerProfile: RunnerProfile | null; isLoading }` (`useQuery` + `isLoading = queryLoading`).
- [ ] `upsertRunnerProfile(patch)` : `getCurrentRow()` → `patch('running_profiles', id, cols)` si existe, sinon `insertWithSyncFields('running_profiles', { user_id: currentUserId(), ...cols })`.
- [ ] Typecheck. **Commit** : `feat(mobile): running-profile-repository (lecture réactive + upsert)`

---

## Phase D — i18n

### Task 5 : clés `running.profile.*` + `running.sessionType.*` (FR + EN)

**Files:** MAJ `apps/mobile/src/i18n/locales/{fr,en}.json`.
- [ ] `running.profile.{title, objective, level, ref5k, ref5kHint, ref5kPlaceholder, frequency, paces, pacesEmpty}` + libellés d'objectifs (`running.objective.*` : 5k/10k/semi/marathon/perte_poids/endurance) + niveaux (`running.level.*` : debutant/regulier/confirme) + types (`running.sessionType.*` : endurance/fractionne/sortie_longue/recuperation) + `running.paces.range` (« {{min}} – {{max}} »). Parité FR/EN, aucune chaîne en dur.
- [ ] **Commit** (peut être groupé avec Task 6) : `feat(i18n): profil coureur + types de séance (running.profile/sessionType)`

---

## Phase E — Écran + hook

### Task 6 : hook `useUnits` — allure (parse + prefill)

**Files:** MAJ `apps/mobile/src/hooks/useUnits.ts`.
- [ ] Ajouter `parsePace: (text) => parsePaceToSPerKm(text, system)` et `paceInputValue: (sPerKm) => formatPaceValue(sPerKm, system)` (liés au système). Ne pas toucher au reste.
- [ ] Typecheck. **Commit** : `feat(mobile): useUnits — saisie/pré-remplissage d'allure`

### Task 7 : `running-profile.tsx` + route + point d'entrée

**Files:** Create `apps/mobile/src/app/running-profile.tsx` ; MAJ `apps/mobile/src/app/_layout.tsx` (route modale) ; MAJ `apps/mobile/src/app/settings.tsx` (entrée « Profil coureur » si pilier `running` actif).
- [ ] Écran (patron `nutrition-profile.tsx`) : `useRunnerProfile()` + `upsertRunnerProfile` (upsert on change), `useUnits`, i18n. `OptionList` pour objectif + niveau ; `TextField` allure de réf (valeur `units.paceInputValue(ref)`, `keyboardType="numbers-and-punctuation"` pour saisir « M:SS », placeholder unité-aware). **onChange** : `const p = units.parsePace(v); if (p != null) void upsertRunnerProfile({ ref5kPaceSPerKm: p });` — n'upserte **que si le parse renvoie une valeur plausible** (sinon on laisse l'utilisateur finir sa saisie sans écrire une valeur aberrante). Gérer l'état local du champ pour l'affichage pendant la frappe. Sélection fréquence (1-7).
- [ ] **Section « Mes allures »** : pour chaque type ≠ course_libre, afficher `running.paces.range` avec `units.formatPace(range.minSPerKm)`–`units.formatPace(range.maxSPerKm)` (via `sessionTargetPace(type, ref)`). Si pas d'allure de réf → état vide `running.profile.pacesEmpty`.
- [ ] **Route** : `Stack.Screen name="running-profile"` (modale, titre `running.profile.title`) dans `_layout.tsx` (patron `nutrition-profile`).
- [ ] **Entrée** : bouton dans `settings.tsx` (« Profil coureur », `router.push('/running-profile')`) conditionné à `activePillars.includes('running')`.
- [ ] Typecheck + lint. **Commit** : `feat(mobile): écran profil coureur + allures d'entraînement`

---

## Phase F — Vérification

### Task 8 : Qualité
- [ ] `npm run typecheck` (3 workspaces) → 0 erreur ; `npm run lint` → pas de nouvelle erreur ; `npm run test` → vert (tests shared paces + parse allure).
- [ ] Parité i18n FR/EN ; 0 doublon de clé ; aucune unité codée en dur dans l'écran.
- [ ] **Commit** éventuel.

### Task 9 : 🔴 Activation cloud + recette device (avec Damien)
- [ ] **Confirmer le timestamp** de migration avec Damien (pas de collision), puis **appliquer la migration** sur le cloud Supabase + **déployer les sync rules** + `npm run db:types` (régénère `database.types.ts`, à committer).
- [ ] **Vérif device** (dev/preview build) : éditer le profil coureur (objectif/niveau/allure/fréquence) → persistance + **sync** ; « Mes allures » calculées correctes ; bascule **min/km ↔ min/mi** (via réglage unités) ; **FR + EN** ; RLS (2 comptes) ; offline (écriture hors-ligne puis sync).

---

## Ordre de build & commits

A (shared, TDD) → B (backend/data) → C (repo) → D (i18n) → E (hook + écran) → F (qualité) → **G (🔴 cloud + device, avec Damien)**. Un commit par tâche, via `/commit` (CHANGELOG + TODO + push `dev`). **Ne jamais stager `apps/mobile/eas.json`** ni de secret. La régénération `database.types.ts` se fait à l'activation cloud (Task 9).

## Definition of Done (spec §6)

`running-paces` + parse allure testés ✔A ; table + RLS + sync rules + schéma PowerSync ✔B ; repo réactif ✔C ; écran profil + section allures via `useUnits` ✔E ; i18n FR+EN + parité ✔D ; typecheck/lint/tests verts ✔F ; **🔴 activation cloud + `db:types` + vérif device** ✔G ; maquette validée avant écran ; PR relue.
