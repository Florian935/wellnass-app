# US 8.8b — Bannissement des utilisateurs — Plan d'implémentation

> **Pour l'exécutant :** tâche par tâche, étapes cochables. Admin sans runner de tests → vérif =
> `typecheck` + `lint` + `build` (`@wellness/admin`) + recette. `packages/shared` a Vitest. Commits FR.

**Objectif :** bannir/débannir un compte depuis la fiche `/users/:id` (rôles super_admin/moderator),
via RPC SQL `SECURITY DEFINER`, avec garde-fous (ni soi-même ni admin), motif obligatoire et historique.

**Architecture :** table `user_bans` (append-only) + RPC `ban_user`/`unban_user` (écrivent
`auth.users.banned_until` en droits propriétaire) + colonne `is_admin` ajoutée à la vue `admin_users` ;
UI : section Modération sur la fiche 8.8a. Clé anon (les vérifs sont dans les RPC).

**Tech Stack :** Supabase (SQL migration, RPC plpgsql SECURITY DEFINER), supabase-js `.rpc()`, React,
i18n FR maison, `@wellness/shared` (union d'audit).

**Spec :** [docs/specs/functional/us/8.8b-admin-bannissement.md](../specs/functional/us/8.8b-admin-bannissement.md)

**⚠️ Ordre imposé :** migration appliquée + `db:types` (Task 2) **avant** le data layer (Task 5, dépend
de `Tables.user_bans` + RPC typées + colonne `is_admin`). Task 2 = **checkpoint humain** (apply cloud).

**Fichiers touchés :**
- Créer : `supabase/migrations/<ts>_user_bans.sql` ; Modifier : `supabase/MIGRATIONS.md`, `packages/shared/src/database.types.ts` (régénéré)
- Modifier : `packages/shared/src/audit.ts` (+ `packages/shared/src/audit.test.ts` si la liste y est testée)
- Modifier : `apps/admin/src/i18n/fr.ts` (audit.action +2, fr.users.ban)
- Modifier : `apps/admin/src/data/users.ts`, `apps/admin/src/screens/UserDetailScreen.tsx`

---

## Task 1 : Migration SQL — table `user_bans` + RPC + colonne `is_admin`

**Files :** Create `supabase/migrations/<timestamp>_user_bans.sql` (via `npm run db:new user_bans`)

- [ ] **Step 1 : Générer** — `npm run db:new user_bans`.

- [ ] **Step 2 : Écrire le SQL**

```sql
-- US 8.8b — Bannissement des utilisateurs (back-office).
-- Table user_bans (append-only, historique) + RPC ban_user/unban_user (SECURITY DEFINER)
-- + colonne is_admin ajoutée à la vue admin_users (garde-fou UI anti-ban-admin).
-- Web-admin uniquement : HORS PowerSync, aucune sync rule.
-- Réf. : docs/specs/functional/us/8.8b-admin-bannissement.md §3-§4.

create extension if not exists pgcrypto;

-- Historique append-only des bannissements. Écrite UNIQUEMENT par les RPC (definer) ;
-- le client anon ne peut que lire (RLS select), jamais insert/update/delete.
create table public.user_bans (
  id uuid primary key default gen_random_uuid(),
  target_user_id uuid not null references auth.users (id) on delete cascade,
  action text not null check (action in ('ban', 'unban')),
  reason text,
  acted_by uuid references auth.users (id),
  created_at timestamptz not null default now(),
  constraint user_bans_reason_required
    check (action = 'unban' or (reason is not null and length(trim(reason)) > 0))
);
create index on public.user_bans (target_user_id, created_at desc);

alter table public.user_bans enable row level security;
-- Lecture réservée aux gestionnaires d'utilisateurs. AUCUNE policy write → client anon ne peut
-- pas écrire ; les RPC SECURITY DEFINER (propriétaire = postgres) contournent la RLS.
create policy user_bans_select on public.user_bans
  for select using (public.can_manage_users());

-- RPC bannir : vérifs atomiques (habilitation, motif, anti-self, anti-admin) puis
-- banned_until = 'infinity' (ban permanent) + ligne d'historique.
create or replace function public.ban_user(target_user_id uuid, reason text)
  returns void
  language plpgsql
  security definer
  set search_path = public
as $$
begin
  if not public.can_manage_users() then
    raise exception 'non autorisé';
  end if;
  if reason is null or length(trim(reason)) = 0 then
    raise exception 'motif requis';
  end if;
  if target_user_id = auth.uid() then
    raise exception 'auto-bannissement interdit';
  end if;
  if exists (
    select 1 from public.user_roles
    where user_id = target_user_id and deleted_at is null
  ) then
    raise exception 'impossible de bannir un compte administrateur';
  end if;

  update auth.users set banned_until = 'infinity' where id = target_user_id;

  insert into public.user_bans (target_user_id, action, reason, acted_by)
  values (target_user_id, 'ban', trim(reason), auth.uid());
end;
$$;

-- RPC débannir : habilitation seule (un admin n'est jamais banni ; l'anti-self est sans objet).
create or replace function public.unban_user(target_user_id uuid)
  returns void
  language plpgsql
  security definer
  set search_path = public
as $$
begin
  if not public.can_manage_users() then
    raise exception 'non autorisé';
  end if;

  update auth.users set banned_until = null where id = target_user_id;

  insert into public.user_bans (target_user_id, action, acted_by)
  values (target_user_id, 'unban', auth.uid());
end;
$$;

-- Privilèges : retirer le EXECUTE par défaut (PUBLIC) puis n'accorder qu'à authenticated
-- (la 1re instruction de chaque RPC est le contrôle can_manage_users()).
revoke execute on function public.ban_user(uuid, text) from public, anon;
revoke execute on function public.unban_user(uuid) from public, anon;
grant execute on function public.ban_user(uuid, text) to authenticated;
grant execute on function public.unban_user(uuid) to authenticated;

-- Vue admin_users : AJOUT de is_admin EN DERNIER (create or replace interdit de
-- réordonner/renommer/supprimer les colonnes existantes ; on ne peut qu'ajouter en fin).
create or replace view public.admin_users
  with (security_barrier = true)
as
  select
    u.id,
    u.email,
    u.created_at,
    u.last_sign_in_at,
    (u.banned_until is not null and u.banned_until > now()) as is_banned,
    p.first_name,
    p.main_goal,
    p.onboarding_completed_at,
    s.active_pillars,
    s.language,
    exists (
      select 1 from public.user_roles r
      where r.user_id = u.id and r.deleted_at is null
    ) as is_admin
  from auth.users u
  left join public.profiles p on p.user_id = u.id and p.deleted_at is null
  left join public.user_settings s on s.user_id = u.id and s.deleted_at is null
  where public.can_manage_users();
```

- [ ] **Step 3 : Prévisualiser** — `npm run db:push:dry` (la migration figure dans la liste).

- [ ] **Step 4 : Commit** (SQL seulement)

```bash
git add supabase/migrations/
git commit -m "feat(admin): migration user_bans + RPC ban/unban + is_admin sur admin_users (US 8.8b)"
```

---

## Task 2 : [CHECKPOINT 🔴 — apply cloud] privilège + db:push + db:types + registre

> Étape humaine / contrôleur. Écrit dans le schéma `auth`/`public` partagé.

- [ ] **Step 1 : Test de privilège isolé** (SQL Editor cloud, en `postgres`) :
  `update auth.users set banned_until = 'infinity' where id = '<uuid d'un compte test>';` puis
  `update auth.users set banned_until = null where id = '<même uuid>';`. Si « permission denied for
  table users » → **stopper** et traiter avant (les RPC échoueraient en prod). _(Attendu : OK sur le cloud actuel.)_
- [ ] **Step 2 : Pousser** — `npm run db:push`.
- [ ] **Step 3 : Régénérer les types** — `npm run db:types` (peuple `Tables.user_bans`, `Functions.ban_user/unban_user`, colonne `is_admin` de la vue).
- [ ] **Step 4 : Vérif GoTrue `'infinity'`** — ⚠️ **ne pas tester la RPC en SQL Editor `postgres`** :
  `auth.uid()` y est NULL → `can_manage_users()` renvoie `false` → `raise 'non autorisé'` (ce n'est pas
  un bug). Deux façons de tester la RPC : (a) **depuis l'app** connecté en super_admin (chemin réel), ou
  (b) simuler le JWT en SQL : `select set_config('request.jwt.claims', json_build_object('sub','<uuid
  super_admin>','role','authenticated')::text, true); select public.ban_user('<uuid compte normal>', 'test');`.
  Puis tenter un refresh de session du compte banni → doit être **refusé**. Si le refresh passe malgré
  tout → repli **`'9999-12-31T00:00:00Z'`** dans `ban_user` (pas `unban`) : éditer la migration **avant**
  apply définitif, ou migration corrective. Débannir le compte test ensuite.
- [ ] **Step 5 : Fonctionnel** (même contexte de test : app ou `set_config`) — `ban_user` sur soi-même /
  sur un compte admin → **exception** ; sur un compte normal → `is_banned=true` ; `unban_user` →
  `is_banned=false` ; `user_bans` s'incrémente ; un compte non habilité ne peut pas exécuter les RPC.
- [ ] **Step 6 : Registre** — cocher dans [supabase/MIGRATIONS.md](../../supabase/MIGRATIONS.md).
- [ ] **Step 7 : Commit**

```bash
git add packages/shared/src/database.types.ts supabase/MIGRATIONS.md
git commit -m "chore(db): apply user_bans + RPC ban/unban + db:types (US 8.8b)"
```

---

## Task 3 : Étendre l'union d'audit (shared) + libellés (admin)

**Files :** Modify `packages/shared/src/audit.ts`, `packages/shared/src/audit.test.ts` (si liste testée), `apps/admin/src/i18n/fr.ts`

- [ ] **Step 1 : `AUDIT_ACTIONS`** — ajouter `'user.ban'` et `'user.unban'` à la fin du tableau
  `AUDIT_ACTIONS` ([audit.ts:11-26](../../packages/shared/src/audit.ts)). Le type + le `z.enum` en dérivent automatiquement.
- [ ] **Step 2 : Test shared** — si `audit.test.ts` liste/compte les actions, mettre à jour l'attendu.
  Run : `npm run test` → vert.
- [ ] **Step 3 : Libellés FR** — dans `fr.ts`, objet `audit.action`, ajouter :
  `'user.ban': 'Bannissement d’utilisateur'`, `'user.unban': 'Débannissement d’utilisateur'`.
  (Sinon `AuditScreen.tsx` — `fr.audit.action[a]` sur toute l'union — casse le typecheck.)
- [ ] **Step 4 : Vérif** — `npm run typecheck` (racine) vert. Commit :

```bash
git add packages/shared/src/audit.ts packages/shared/src/audit.test.ts apps/admin/src/i18n/fr.ts
git commit -m "feat(audit): actions user.ban / user.unban (union + libellés FR)"
```

---

## Task 4 : i18n FR — libellés de modération (`fr.users.ban`)

**Files :** Modify `apps/admin/src/i18n/fr.ts` (objet `users`)

- [ ] **Step 1 : Ajouter dans `users`** un sous-objet `ban` :

```ts
    ban: {
      section: 'Modération',
      banAction: 'Bannir',
      unbanAction: 'Débannir',
      reasonPrompt: 'Motif du bannissement (obligatoire) :',
      confirmUnban: 'Débannir ce compte ?',
      cannotBan: 'Ce compte ne peut pas être banni.',
      currentReasonLabel: 'Motif',
      historyTitle: 'Historique de modération',
      historyEmpty: 'Aucun événement.',
      actionBan: 'Banni',
      actionUnban: 'Débanni',
      error: 'Action impossible (droits ou règle de sécurité).',
    },
```

- [ ] **Step 2 : Vérif** — `npm run typecheck`. Commit :

```bash
git add apps/admin/src/i18n/fr.ts
git commit -m "i18n(admin): libellés modération (bannir/débannir)"
```

---

## Task 5 : Data layer — `banUser` / `unbanUser` / `listUserBans`

**Files :** Modify `apps/admin/src/data/users.ts`

> Prérequis : Task 2 (types `user_bans` + RPC) et Task 3 (actions d'audit) faites.

- [ ] **Step 0 : Mettre à jour l'en-tête de `users.ts`** — le commentaire de tête (issu de 8.8a)
  affirme « Aucune écriture, aucun logAudit » : c'est **faux** après cette tâche. Le corriger (la
  consultation reste en lecture ; le bannissement écrit via RPC + `logAudit`).

- [ ] **Step 1 : Étendre `users.ts`**

```ts
import { logAudit } from './audit';
// ... (imports existants)

export type UserBanRow = Database['public']['Tables']['user_bans']['Row'];

/** Bannit un compte (RPC SECURITY DEFINER ; garde-fous côté serveur). */
export async function banUser(
  targetUserId: string,
  reason: string,
): Promise<{ error: unknown }> {
  const { error } = await supabase.rpc('ban_user', {
    target_user_id: targetUserId,
    reason,
  });
  if (error) return { error };
  await logAudit({
    action: 'user.ban',
    targetTable: 'auth.users',
    targetId: targetUserId,
    details: { reason },
  });
  return { error: null };
}

/** Débannit un compte (RPC SECURITY DEFINER). */
export async function unbanUser(targetUserId: string): Promise<{ error: unknown }> {
  const { error } = await supabase.rpc('unban_user', { target_user_id: targetUserId });
  if (error) return { error };
  await logAudit({
    action: 'user.unban',
    targetTable: 'auth.users',
    targetId: targetUserId,
  });
  return { error: null };
}

/** Historique de modération d'un compte (append-only), du plus récent au plus ancien. */
export async function listUserBans(
  targetUserId: string,
): Promise<{ rows: UserBanRow[]; error: unknown }> {
  const { data, error } = await supabase
    .from('user_bans')
    .select('*')
    .eq('target_user_id', targetUserId)
    .order('created_at', { ascending: false });
  if (error) return { rows: [], error };
  return { rows: data ?? [], error: null };
}
```

- [ ] **Step 2 : Vérif** — `npm run typecheck`. Commit :

```bash
git add apps/admin/src/data/users.ts
git commit -m "feat(admin): data layer bannissement (banUser/unbanUser/listUserBans)"
```

---

## Task 6 : UI — section Modération sur la fiche `/users/:id`

**Files :** Modify `apps/admin/src/screens/UserDetailScreen.tsx`

- [ ] **Step 1 : Charger l'utilisateur courant + l'historique**
  - Importer `useAuth` (`apps/admin/src/auth/useAuth`) → `const { user: currentUser } = useAuth();` (pour le garde-fou anti-self).
  - Importer `banUser`, `unbanUser`, `listUserBans`, `type UserBanRow`.
  - État : `bans: UserBanRow[]`, `busy: boolean`, `actionError: boolean`.
  - `useEffect` (déclenché par `id` **et** après une action réussie via un compteur `reloadKey`) → `listUserBans(id)` ; recharger aussi `getUser(id)`.

- [ ] **Step 2 : Garde-fous UI + actions**

```ts
const isSelf = !!currentUser && currentUser.id === user.id;
const canModerate = !user.is_admin && !isSelf;

const doBan = async () => {
  const reason = window.prompt(fr.users.ban.reasonPrompt)?.trim();
  if (!reason) return; // annulé ou vide → pas d'appel
  setBusy(true);
  const { error } = await banUser(user.id!, reason);
  setBusy(false);
  if (error) { setActionError(true); return; }
  reload(); // re-fetch getUser + listUserBans
};

const doUnban = async () => {
  if (!window.confirm(fr.users.ban.confirmUnban)) return;
  setBusy(true);
  const { error } = await unbanUser(user.id!);
  setBusy(false);
  if (error) { setActionError(true); return; }
  reload();
};
```

- [ ] **Step 3 : Rendu de la section « Modération »** (nouvelle `<section>` après Profil)
  - Si `!canModerate` → texte neutre `fr.users.ban.cannotBan` (compte admin ou soi-même).
  - Sinon, si `user.is_banned` → afficher le **motif courant** — prendre `bans.find(b => b.action ===
    'ban')?.reason` (le plus récent `ban` ; **pas** `bans[0]` qui pourrait être un `unban` à `reason`
    null), composé à la main : `` `${fr.users.ban.currentReasonLabel} : ${reason}` `` — + bouton
    **Débannir** (`doUnban`, `disabled={busy}`).
  - Sinon (non banni) → bouton **Bannir** (`doBan`, `disabled={busy}`).
  - `actionError` → bloc `role="alert"` `fr.users.ban.error`.
  - **Historique** (`fr.users.ban.historyTitle`) : liste des `bans` (date `formatDate(created_at)` ·
    action `row.action === 'ban' ? actionBan : actionUnban` · motif si présent · acteur). Vide →
    `fr.users.ban.historyEmpty`.
  - **Acteur** : `acted_by` est un UUID. Résoudre en e-mail via un lookup des acteurs uniques
    (`getUser(acted_by)` pour chaque UUID distinct, mémoïsé dans une Map) ; **fallback** = l'UUID brut
    si non résolu. (MVP : peu d'entrées.)

- [ ] **Step 4 : Vérif** — `npm run typecheck && npm run lint && npm run build -w @wellness/admin`. Commit :

```bash
git add apps/admin/src/screens/UserDetailScreen.tsx
git commit -m "feat(admin): section modération (bannir/débannir + historique) sur la fiche utilisateur"
```

---

## Task 7 : Vérification finale & recette

- [ ] **Step 1 : Suite verte** — `npm run typecheck && npm run lint && npm run test && npm run build -w @wellness/admin`.
- [ ] **Step 2 : Suivi & commit final** — CHANGELOG + TODO (8.8b livrée → US 8.8 complète ; reste recette + relecture Damien), push `dev` via `/commit`.
- [ ] **Step 3 : Recette (Florian, back-office)** :
  - super_admin : sur un **compte normal**, Bannir → saisir motif → statut « Banni » + motif affiché + historique ; Débannir → « Actif » + ligne d'historique.
  - **Garde-fous** : sur **sa propre fiche** et sur la fiche d'un **admin** → section « Ce compte ne peut pas être banni » (pas de bouton). Tenter quand même via RPC (console) → exception.
  - moderator : peut bannir/débannir un compte normal.
  - content_editor / compte normal : pas d'accès à `/users` (8.8a) → RPC injoignable de fait.
  - (Optionnel) vérifier qu'un compte banni perd l'accès à l'app/synchro au prochain refresh (~1 h) ou après re-login.

---

## Notes

- **Clé anon uniquement, aucun `service_role`, aucune sync rule.**
- **Checkpoint 🔴** = Task 2 (écrit `auth.users` + crée la table/RPC) — étape humaine, avec test de
  privilège isolé et validation du comportement `'infinity'` côté GoTrue (repli `'9999-12-31'` sinon).
- **Ordre critique** : Task 2 (db:types) avant Task 5 ; Task 3 (audit actions) avant Task 5 (logAudit).
- **US 8.8 complète** une fois 8.8b recettée (8.8a consultation + 8.8b bannissement).
