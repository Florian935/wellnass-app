# US 4.24 — Recettes, repas types, poids & stats

> Troisième US de la **V0.4 — Alimentation** (après 4.1 profil, 4.8 base & journal).
> Réf. pilier : [alimentation.md](../alimentation.md) §5 (recettes), §7 (suivi).
> Branche : `feature/4.24-recettes-poids-stats` · Date : 06/07/2026.

## 1. Périmètre livré

| # | Item | Statut |
|---|---|---|
| 4.24 | Création de recette (ingrédients + portions) | ✅ |
| 4.25 | Valeurs nutritionnelles calculées (total + par portion) | ✅ |
| 4.26 | Repas types (templates) | ✅ enregistrer un repas / réappliquer en 1 tap |
| 1.13 | Historique poids corporel | ✅ pesée/jour + tendance |
| 4.30 | Courbe poids corporel | ✅ 4 sem / 3 mois / 1 an |
| 4.31 | Évolution apports moyens | ✅ moyennes 7 / 30 j + courbe kcal |

**Différé** : 4.32 alerte déficit + fort volume (stat croisée muscu ↔ apports), 1.14 rappel de pesée & 2.5 notif repas (nécessitent `expo-notifications` = module natif), 4.27-4.29 planning repas & liste de courses (V1.1).

## 2. Architecture

- **`packages/shared`** : `recipe.ts` (`recipeRowSchema`, `recipeIngredientRowSchema`, `mealTemplateRowSchema`, items ; helpers `perServing`/`scalePortions`) + `bodyweight.ts` (`bodyWeightEntryRowSchema`, `weightTrend`, `averageIntake`). **+ tests**.
- **5 tables PowerSync** (toutes user_id) : `recipes`, `recipe_ingredients`, `meal_templates`, `meal_template_items`, `body_weight_entries`. Migrations `20260707130000/130001` + RLS + sync rules edition 3.
- **Repositories** : `recipe-repository` (totaux SQL), `meal-template-repository` (save/apply), `bodyweight-repository` (pesée/jour) + `useDailyTotals` (stats apports) dans le journal.
- **Écrans** : `recipe-edit` (nom, portions, ingrédients via food-picker mode recette, total + par portion) ; **food-picker** étendu (onglets Recettes/Repas types + mode recette) ; **nutrition-stats** (pesée + courbe poids + apports moyens, `ProgressLineChart` réutilisé) ; onglet Nutrition (icône Suivi + « enregistrer comme repas type » par repas).

## 3. Règles & offline

- Ingrédients / items de repas type = **snapshot** (spec §8). Valeur par portion = total / portions.
- Une pesée par jour (mise à jour si déjà saisie). Moyennes calculées sur les jours renseignés.
- 100 % offline (sauf recherche OFF héritée de 4.8). FR + EN.

## 4. Checkpoints 🔴 humains

Appliquer `20260707130000_recipes_bodyweight_tables.sql` + `130001_..._rls.sql` sur Supabase, redéployer les **sync rules** (5 streams ajoutés), `db:types`, **vérif device**.

## 5. Definition of Done

- [ ] typecheck + lint + tests verts.
- [ ] Recette créable + utilisable au journal ; repas type enregistrable/réapplicable ; pesée + courbes + moyennes.
- [ ] FR + EN, offline-first. Migrations + sync rules appliqués ; testé device ; PR relue.
