import { PILLARS, type Pillar } from '@wellness/shared';

/**
 * Placeholder du back-office. Le back-office web (gestion de contenu :
 * exercices, programmes, aliments) est spécifié dans
 * docs/specs/functional/administration.md et construit à partir de la V0.7.
 * Le framework (Next.js / Vite + React / …) sera figé le moment venu.
 */
export const managedPillars: readonly Pillar[] = PILLARS;
