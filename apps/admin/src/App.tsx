// App minimale (scaffold). Le routing complet (login + shell protégé) est
// posé en tâche 4 de la fondation-1.
export function App() {
  return (
    <div style={{ padding: 24 }}>
      <h1>Wellness Admin — OK</h1>
    </div>
  );
}
