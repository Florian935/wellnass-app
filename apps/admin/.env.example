# Back-office admin — variables d'environnement (Vite, exposées au client via VITE_*).
# Copier ce fichier vers apps/admin/.env puis renseigner les valeurs.
# Clé ANON uniquement (client-safe) — NE JAMAIS committer de vraie clé ni service_role.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
