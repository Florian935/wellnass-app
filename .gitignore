# ==============================================================
#  Wellness App — .gitignore
#  Monorepo React Native + Expo + TypeScript / Supabase / PowerSync
# ==============================================================

# --- Dépendances ---
node_modules/
.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# --- Secrets & environnement (JAMAIS committés) ---
.env
.env.*
!.env.example
*.key
*.pem
*.p8
*.p12
*.mobileprovision
# Config natives contenant des identifiants
google-services.json
GoogleService-Info.plist
# Keystores / signatures Android
*.keystore
*.jks
!debug.keystore

# --- Expo ---
.expo/
.expo-shared/
dist/
web-build/
expo-env.d.ts

# --- Metro ---
.metro-health-check*
.metro-cache/

# --- Android ---
android/app/build/
android/build/
android/.gradle/
android/local.properties
*.apk
*.aab

# --- iOS (portage ultérieur) ---
ios/build/
ios/Pods/
ios/.xcode.env.local

# --- Supabase (dev local) ---
supabase/.branches/
supabase/.temp/
**/supabase/.env

# --- TypeScript / build ---
*.tsbuildinfo

# --- Tests / couverture ---
coverage/

# --- Logs ---
npm-debug.*
yarn-debug.*
yarn-error.*
*.log

# --- Caches divers ---
.cache/
.turbo/
.eslintcache

# --- OS ---
.DS_Store
Thumbs.db
desktop.ini

# --- IDE ---
.idea/
*.iml
# VS Code : on ignore le dossier sauf les réglages partagés utiles à l'équipe
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# NB : le dossier .claude/ est suivi volontairement (commandes, config projet).

# --- Superpowers (brainstorming visuel, scratch local) ---
.superpowers/

# Dumps bruts des modèles IA (brainstorming, non versionnés)
_inbox-ia/

# --- Claude Design : artefacts locaux de l'outil ---
# Les maquettes de design/<fonctionnalité>/ sont bien versionnées — c'est l'étape 3 du workflow
# obligatoire. Mais l'outil dépose à côté un cache de vignette et les fichiers glissés dans
# l'éditeur : ils ne décrivent aucune maquette, dépendent du poste, et polluaient `git status`.
# `**` couvre aussi la racine de design/ (il matche zéro dossier intermédiaire).
design/**/.thumbnail
design/**/uploads/
