{
  "name": "@wellness/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Types et schémas Zod partagés (mobile · admin · back). Source de vérité du domaine côté TypeScript.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.8",
    "typescript": "~5.6.3",
    "vitest": "^2.1.8"
  }
}
